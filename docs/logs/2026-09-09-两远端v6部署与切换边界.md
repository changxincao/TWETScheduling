# 两远端v6部署与切换边界

2026-09-09只读检查两台正式计算主机，并将提交`3038d8a0`对应的`2026-09-09-v6`运行包分别部署到独立新目录。两台`solver.jar`的SHA256均为`7AC623A7A99F2234D8DA8F19604B404045962225C967874E777708A566BFE06B`，各自运行6项中点历史、probe和profile/runner配置回归，全部通过。原v5部署和`run.cmd`均未覆盖；两边只新增未执行的`run-v6.cmd`。

旧主机`100.68.243.112`的正式目录为`D:\ccx_work\考虑交付的机器调度\work1\experiments\20260905-formal-scheduling-le60`。检查时状态为`SOLVE_RUNNING`，3240项中1261项已有SUCCESS，其中NG-DSSR 421/1080、TI 420/1080、TI+SRI 420/1080。原scheduler PID 18052仍按v5运行6并发，当前6个solver未替换。v6部署位于`D:\ccx_work\考虑交付的机器调度\work1\deployments\20260909-3038d8a0-v6`。

第二主机`100.68.243.192`的正式目录为`D:\ccx\work1\experiments\20260907-formal-n80-n100`。正式批次保持`PAUSED_FOR_MIDPOINT_AB`，1620项中24项已有SUCCESS，三种算法各8项。v6部署位于`D:\ccx\work1\deployments\20260909-3038d8a0-v6`。当前唯一活动求解仍是原中点A/B的m3/wide mean重跑，scheduler/solver为25716/25820，尚无SUCCESS；未替换其classpath。

本次状态统计曾发生一次受控清理事件。旧机`run.cmd`只转发三个参数，调用selector和`--scan-only`时后者被丢弃，误启动了一个单并发NG-DSSR scheduler及一项`n040-set02/m4/family/medium/wide`求解。发现后通过`jcmd`核对完整命令行，以带命令片段保护的临时Java停止器只终止误启动PID 49072及其子进程45452；复核`jcmd`仅剩原scheduler和原6个solver。该无SUCCESS输出完整移入正式目录下`accidental-scan-20260909`，原输出路径清空，未来原scheduler轮到该runId时会新建干净目录。没有删除结果、修改原锁、停止原任务或改变pipeline状态。

上述为切换前快照。后续用户明确决定保留全部已完成结果，不重跑429项旧NG；以下实际接续方案取代此前全量重跑建议。汇总必须保留版本和来源，不能将旧结果标成v6。

## 实际切换与接续

旧机保持scheduler 18052及五个TI求解进程不动，只通过独立Java agent替换scheduler的子进程classpath选择方法：未来NG使用独立v6 jar，TI仍使用旧jar。为使当时正在运行的NG也切换，核对完整命令后停止PID49324，原输出归档为`.before-v6`，由scheduler对该runId执行一次原参数重试，v6 PID50840启动并已核验classpath。仅该次失败触发重试，其他任务不改变。重试同步等待期间不补充新任务，现有TI继续；重试结束恢复正常六并发。这是避免终止scheduler波及TI的临时切换措施，没有修改求解算法源文件或覆盖在用jar。

两机分别在各自`work1/experiments/20260909-ng-v6-continuation`建立接续控制目录。旧机控制进程9428等待原scheduler 18052结束后再扫描补漏；新机27956等待mean重跑scheduler 25716结束后再扫描并启动剩余正式任务。2026-09-09 11:58左右断开本地SSH后，两机WAITING心跳继续增长，接续不依赖本机保持开机。

接续按每个runId只选一个来源：有成功mean重试则优先复用，其次成功mean主实验，再其次原正式SUCCESS；其余NG写入接续目录`runs/<runId>`并使用v6，其余TI仍使用原部署和原输出地址。SUCCESS表示运行正常完成，包含正常TIME_LIMIT，不等于证明最优。已有结果不复制、不覆盖、不重新求解。原seed、输入、参数、单模型单线程及时间限制保持不变：60及以下10800秒，80/100为18000秒，两机各最多六并发。

切换时计划快照：旧机复用1262项、待NG659项、待TI两类1319项；新机复用原结果17项及mean7项、待NG532项、待TI两类1064项。这不是最终待跑数；等待结束后重新扫描成功标记，避免重复计算。`result-index.tsv`记录实际来源，`remaining.tsv`记录接续任务；后续应同时读取原结果及此索引，不能仅看原pipeline状态或仅统计新目录。不要重新执行原NG启动命令，也不要重复启动已持有worker.lock的接续进程。

本机模拟验证通过：普通路由验证未来NG换v6而TI不变；定向失败重试验证先归档再v6重跑；接续清单验证mean优先、旧SUCCESS跳过、NG输出迁移、TI输出不变。脚本位于`scripts/remote/*ng-v6*.ps1`及`NgV6*.java`。远端实际命令核验确认NG v6和TI旧jar；求解数值正确性仍沿用已通过的v6算法回归，本次没有重做算法实验。

清理仅删除两机未使用且容易误导的`run-v6.cmd`和本机只有56个残缺class的失败编译目录`.codex-tmp/deployment-20260909-3038d8a0`。旧jar仍供TI使用，mean实验版本仍供重跑及追溯，因此保留；完成结果、崩溃证据、切换归档及在用工具均未删除。
