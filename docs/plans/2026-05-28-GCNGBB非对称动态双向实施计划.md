# GCNGBB 非对称动态双向实施计划

本文记录基于 `docs/literature/dynamic_halfway_soft_tw.pdf` 和当前讨论形成的实现方案。目标不是修改现有正式 `GCNGBBStyleBidirectional`，而是在已有 full-domain 复制版基础上再复制一份实验版本，用动态 half-way 边界控制 forward/backward 扩展，观察它是否能更好平衡 dominance graph 与 final join 的工作量。

## 1. 背景和目标

当前正式版本使用静态 `Tmid`，forward label 存储在 `[ell,Tmid]`，backward label 存储在 `[Tmid,rho]`，最后统一 crossing-arc join。此前 full-domain 对照实验把函数定义域放宽到 `[0,pricingHorizon]` 后，发现它不是单纯增加函数段操作，而是把复杂度从 dominance graph 转移到 join：`superset visited` 下降，但 join candidates 和 function eval 大幅上升。时间上 full-domain 和 half-domain 差距不大，且 full-domain 不是单调改进。

PDF 中的 Tilk-style dynamic half-way point 提供了另一个思路：不固定一个静态 `Tmid`，而是维护两个动态边界 `HB` 和 `HF`。`HB` 表示 backward 侧已经推进到的左边界，`HF` 表示 forward 侧已经推进到的右边界，并始终保持 `HB <= HF`。在本问题里，这两个边界只用于控制 label 是否继续扩展，不进入 dominance key、visited set、reachable set 的语义定义，也不改变最终 join 的 reduced-cost 函数计算方式。

本次计划中的新版本暂定名为 `GCNGBBDynamicBidirectional`。它从 `GCNGBBStyleBidirectionalFullDomain` 复制出来，保留 full-domain 函数定义域，但用动态 `HB/HF` 替代静态 `Tmid` 作为扩展边界。因此它更准确地说是“full-domain function labels + dynamic halfway expansion boundary”的实验版本。

## 2. 动态资源和边界定义

PDF 中的 `[L,U]` 不能直接理解为 `[0,pricingHorizon]`。在当前 TWET pricing 里，动态 half-way 的资源区间应沿用现有 midpoint 逻辑中的有效左端：

```java
dynamicHB = Math.max(dynamicMinHStart, earliestSourceCompletion);
dynamicHF = pricingHorizon;
```

其中 `dynamicMinHStart` 是当前 pricing 轮的有效任务窗口最左端，`earliestSourceCompletion` 是从 source 到任一任务的最早物理完成时间，`pricingHorizon` 是当前 pricing 轮的有效右端。这样初始化后，`HB/HF` 的资源含义和当前动态 midpoint 的左、右端保持一致，不会把 `HB` 放在没有实际任务完成意义的 0 上。

forward label 的单调资源使用 `earliestForwardCompletion(label)`，即当前 label frontier 的 `head.start`。backward label 的单调资源使用 `latestBackwardCompletion(label)`，即当前 label frontier 的 `tail.end`。这两个值和当前 `TIME` 队列排序已经一致。

## 3. 队列排序和出队语义

动态版本必须强制使用当前 `TIME` 主排序，不再允许 reduced-cost 作为主排序。当前 `TIME` 排序已经满足需求：forward 按最早完成时间升序，backward 按最迟完成时间降序；tie-break 仍保留 reachable set 基数和 reduced cost。这样 forward 出队顺序从左向右推进，backward 出队顺序从右向左推进，`HB/HF` 才能解释成已经处理过的资源前沿。

出队时需要处理 stale label。因为 `HF` 会随 backward 处理不断缩小，之前生成的 forward label 可能在弹出时已经位于当前 `HF` 右侧；同理，`HB` 会随 forward 处理不断增大，之前生成的 backward label 可能在弹出时已经位于当前 `HB` 左侧。由于队列按资源单调排序，一旦队首已经越界，剩余同侧 label 也全部越界，可以直接清空该队列。

forward 出队逻辑为：

```java
ForwardLabel label = FWUL.peek();
if (earliestForwardCompletion(label) > dynamicHF) {
    FWUL.clear();
    return;
}
label = FWUL.poll();
forwardExtend(label);
dynamicHB = Math.max(dynamicHB, earliestForwardCompletion(label));
```

backward 出队逻辑为：

```java
BackwardLabel label = BWUL.peek();
if (latestBackwardCompletion(label) < dynamicHB) {
    BWUL.clear();
    return;
}
label = BWUL.poll();
backwardExtend(label);
dynamicHF = Math.min(dynamicHF, latestBackwardCompletion(label));
```

因为越界 label 会在出队前直接剪掉，更新 `dynamicHB/dynamicHF` 时不再需要 `min(..., dynamicHF)` 或 `max(..., dynamicHB)` 兜底。`HB <= HF` 由出队检查和更新顺序自然维持；若出现数值误差，可在实现中用 `Utility.compare...` 做容差判断。

## 4. 外层处理流程

当前 `GCNGBBStyleBidirectionalFullDomain.solve()` 是先完整耗尽 forward 队列，再完整耗尽 backward 队列，最后统一 join。动态 half-way 版本需要改成 forward/backward 交错处理，但 final join 仍保留现在的统一 join，不采用 streaming join。这样可以最大限度复用现有 label table、candidate top-K 和 join 逻辑，也更容易和当前 full-domain 对照版本做公平比较。

外层循环的基本结构为：

```java
while (canContinue() && (!FWUL.isEmpty() || !BWUL.isEmpty())) {
    if (shouldProcessForward()) {
        dynamicForwardExtend(lp);
    } else {
        dynamicBackwardExtend(lp);
    }
}
compactAndSortActiveLabelListsForJoin();
joinAllForwardTerminalGroups(lp);
finalizeGeneratedColumns();
```

选边策略先做两个实验配置：

1. `MORE_LABELS_FIRST`：哪个队列剩余 label 多，就优先处理哪个队列。这个策略偏向控制 backlog，避免一侧积压过多。
2. `FEWER_LABELS_FIRST`：哪个队列剩余 label 少，就优先处理哪个队列。这个策略可能更快推进某一侧边界，从而收缩另一侧的可扩展范围，但也可能造成不平衡。

如果某一侧队列为空，则处理另一侧。如果某一侧队首越界并被清空，则下一轮自然转向另一侧。选边策略只影响效率和平衡，不应影响正确性。

## 5. 扩展可行性判断

新版本仍保留 full-domain 函数定义域 `[0,pricingHorizon]`。动态边界只替代当前扩展停止判断里的静态 `tMid`。

forward direct feasibility 当前为：

```java
earliestCompletion <= tMid
```

动态版本改为：

```java
earliestCompletion <= dynamicHF
```

backward direct feasibility 当前为：

```java
rhoPrime >= Math.max(tMid, HStart)
```

动态版本改为：

```java
rhoPrime >= Math.max(dynamicHB, HStart)
```

`buildForwardReachableSet`、`buildForwardReachableSetFromParent`、`buildBackwardReachableSet` 和 `buildBackwardReachableSetFromParent` 都会通过上述 direct feasibility 使用当前 `dynamicHF/dynamicHB`。需要注意的是，label 创建时的 reachable set 是当时边界下的结果；边界后续变化后，队列里可能残留 stale label，因此出队时的队首越界检查仍然必须保留。

## 6. 保持不变的语义

该实验版本不改变以下内容：

1. 不改变 `PiecewiseLinearFunction` 的 full-domain 函数定义域。
2. 不把 `dynamicHB/dynamicHF` 写入 dominance key、visited set、reachable set signature 或 column signature。
3. 不改变 zero-dual job 过滤、active column 去重、candidate top-K、debug evaluator 复核逻辑。
4. 不改变 final join 的 crossing-arc 语义，仍然用 forward full extension、backward full extension 和 crossing arc reduced cost 生成完整列。
5. 不改变 source/sink root 的 full-domain 初始化。

这几个“不变”是正确性边界。`HB/HF` 只是扩展控制参数，不是 label 状态的一部分。

## 7. 正确性判断

正确性直觉来自资源单调处理。forward 队列按最早完成时间升序，如果队首已经大于当前 `dynamicHF`，剩余 forward label 的最早完成时间只会更大，因此它们不应再由 forward 侧扩展；backward 队列按最迟完成时间降序，如果队首已经小于当前 `dynamicHB`，剩余 backward label 的最迟完成时间只会更小，因此它们不应再由 backward 侧扩展。

这种剪枝不会把 `HB/HF` 当成 dominance 条件，也不删除已经存入 label table 的 live label；它只停止未处理队列中越过当前动态边界的同侧扩展。已经生成的 forward/backward labels 最后仍会统一参与 join，因此不会因为选边策略不同而改变完整列拼接语义。

需要特别避免的是 reduced-cost 主排序。如果 forward 不是按 earliest 升序、backward 不是按 latest 降序，则队首越界不能推出后续同侧 label 全部越界，清空队列就可能不安全。因此动态版本必须强制 `TIME` 主排序，reachable size 和 reduced cost 只能作为 tie-break。

## 8. 实施步骤

第一步，复制类和 engine。以 `GCNGBBStyleBidirectionalFullDomain` 为基准复制出 `GCNGBBDynamicBidirectional`，并新增对应 pricing engine。类名不采用过长的 `GCNGBBStyleBidirectionalDynamicHalfwayFullDomain`，避免后续使用和日志过长。

第二步，新增配置。配置至少包括：

```java
public boolean useGCNGBBDynamicBidirectionalPricing = false;
public String dynamicBidirectionalSideSelection = "moreLabels";
```

其中 `dynamicBidirectionalSideSelection` 支持 `moreLabels` 和 `fewerLabels`。动态版本启用时应强制内部使用 `TIME` 主排序，不依赖外部 `bidirectionalLabelQueueOrdering`。

第三步，改外层 solve loop。删除“先 forward 后 backward”的两段 while，改成交错处理。新增 `dynamicForwardExtend` 和 `dynamicBackwardExtend`，内部先做队首越界检查，必要时清空队列；真正 poll 并扩展后更新 `dynamicHB/dynamicHF`。

第四步，替换扩展边界。新增 `currentForwardBoundary()` 和 `currentBackwardBoundary()` 之类的小方法，避免到处直接读字段。forward direct feasibility 使用 `dynamicHF`，backward direct feasibility 使用 `dynamicHB`。如果保留原 `tMid` 字段用于统计或兼容复制代码中的旧方法，需要明确它不再是扩展边界；`dynamicHB/dynamicHF` 也不用于 join 常数延拓。第一版应保留 full-domain 版本现有 join extension 行为，避免同时修改 join 语义。

第五步，补统计。`statisticsSummary()` 需要记录 `dynamicHB/dynamicHF` 的最终值、forward/backward 队列被 boundary clear 的次数、被 clear 的 label 数量、选边策略、forward/backward 实际 pop 次数。这样才能判断动态策略到底是在减少 dominance graph 还是减少 join。

第六步，新增 comparison runner。可以复用 `GCNGBBFullDomainComparisonTest` 的结构，新增 dynamic-more 和 dynamic-fewer 两种模式，与 half-domain、full-domain static 进行对照。第一批测试仍使用 no-outsourcing 15、20、21、22、25，记录 objective、bound、valid、exact time、join candidates、function eval、superset visited。

## 8.1 可复用代码和需要补充的内容

新版本应尽量复用 `GCNGBBStyleBidirectionalFullDomain`，不要重新实现 label、dominance 或 join。可以直接复用的部分包括：

1. `ForwardLabel`、`BackwardLabel`、`FunctionLabel`、`PricingColumnCandidate`、`SinglePointStore` 等内部数据结构。
2. full-domain 的 source/sink root 初始化、base penalty cache 和 dynamic job penalty 构造，即函数定义域仍保持 `[0,pricingHorizon]`。
3. forward/backward 的实际函数递推逻辑：`extendForward()`、`extendBackward()` 中的 shift/add/normalize、dual 扣减、visited/reachable set 构造和 label 创建。
4. dominance graph 插入、single-point store、active label list、final compact/sort。
5. crossing-arc final join、forward->sink 收尾、candidate top-K、sequence 恢复、reduced-cost 反推、active column 去重。
6. 现有 `TIME` 队列比较器。动态版本只需要强制 `queueOrdering = LabelQueueOrdering.TIME`，不需要新写比较器；reachable size 和 reduced cost 继续作为 tie-break。

需要修改或新增的内容集中在外层调度和扩展边界：

1. 新增字段 `dynamicHB`、`dynamicHF`，以及统计字段，例如 `forwardBoundaryClears`、`backwardBoundaryClears`、`forwardBoundaryClearedLabels`、`backwardBoundaryClearedLabels`、`dynamicForwardPops`、`dynamicBackwardPops`、`dynamicSideSelection`。
2. 初始化时在 `precomputeDynamicPricingWindows(lp)` 之后设置 `dynamicHB = Math.max(dynamicMinHStart, earliestSourceCompletion)`、`dynamicHF = pricingHorizon`。原 `tMid` 可保留给复制代码兼容或统计，但动态版本的扩展边界不能再读它。
3. `initialize()` 中强制使用 `LabelQueueOrdering.TIME`，不要解析外部 `bidirectionalLabelQueueOrdering`。
4. `solve()` 中把“先耗尽 forward、再耗尽 backward”的两段 while 改成单个交错循环，通过 `shouldProcessForward()` 选择队列。
5. 新增 `dynamicForwardExtend(lp)` 和 `dynamicBackwardExtend(lp)`。它们先看队首资源是否越过当前边界；若越界，直接记录统计并清空该队列；否则 poll、跳过 dominated label、调用原有扩展逻辑并更新 `dynamicHB/dynamicHF`。
6. `isDirectForwardExtensionTimeFeasible()` 中把 `tMid` 替换为 `dynamicHF`；`isDirectBackwardExtensionTimeFeasible()` 中把 `Math.max(tMid, HStart)` 替换为 `Math.max(dynamicHB, HStart)`。
7. `precomputeHalfDomainEligibility()` 在动态 full-domain 版本里需要重新考虑。由于 forward/backward 不再有固定静态 `Tmid` 半域，第一版可以让所有动态 penalty 非空的 job 都保持 half eligible，即只按 penalty 是否为空判断，避免用初始化时的 `dynamicHB/HF` 提前永久排除任务。真正的动态剪枝交给 direct feasibility 和队首边界处理。
8. `isSinglePointFrontier()` 和 single-point store 的意义会弱化。full-domain 函数通常不再退化到 `[Tmid,Tmid]`，第一版可保留原逻辑但预计统计接近 0；如果保留 `tMid` 只作兼容，不能让 single-point 逻辑反过来影响动态边界。
9. `statisticsSummary()` 需要输出 `dynamicHB/dynamicHF`、选边策略和 boundary clear 统计，便于和 static full-domain 比较。

需要新增的外围文件包括：

1. `GCNGBDDynamicBidirectionalPricingEngine`，名称建议返回 `"GCNGBBDynamicBidirectionalPricing"`。
2. `TWETBPCConfig` 中新增 `useGCNGBBDynamicBidirectionalPricing` 和 `dynamicBidirectionalSideSelection`。
3. `TWETBPCContext` 中在 full-domain/static GCNGBB 分支之前识别 dynamic 版本，避免多个 exact pricing engine 同时启用。
4. 比较 runner 可以在 `GCNGBBFullDomainComparisonTest` 上扩展，也可以新建 `GCNGBBDynamicComparisonTest`。如果只为实验，建议新建 runner，避免把 static full-domain runner 继续加复杂。

实现时的最小改动原则是：除 `solve()` 外层调度、队首越界剪枝、direct feasibility 边界、配置接入和统计外，其余代码尽量保持 full-domain 复制版原样。这样后续如果 dynamic 版本效果不好，可以直接丢弃，不会影响正式 half-domain 和 static full-domain 对照版本。

## 9. 验证标准

基础验证包括 focused `javac`、`PaperDominanceGraphConsistencyTest` 和带 CPLEX 的小样例 BPC。功能验证要求：

1. 与当前 half-domain/full-domain 对照一样，所有测试 case 的 incumbent、bound 和 `valid=true` 保持一致。
2. 对 15、20、21、22、25 这些已有 no-outsourcing 样例，dynamic-more 和 dynamic-fewer 至少不能出现漏列导致 root bound 变差或 validation 失败。
3. 日志中 `dynamicHB <= dynamicHF` 始终成立；如果最终两者交叉，说明出队检查或更新时机有问题。
4. 如果某侧队列被 clear，必须能从队首资源和当前边界解释清楚，不能因为非 TIME 主排序误清。

性能验证不以单个 case 的 wall time 为唯一依据，而同时看 `join candidates`、`join function eval`、`superset visited`、`envelopeMerges`、`dominanceChecks`。前面的 full-domain 对照已经说明，单看 wall time 很容易被两个热点此消彼长误导。

## 10. 暂不做事项

第一版暂不做 streaming join。PDF 中提到可以在 backward label 处理时做 join，但当前 GCNGBB-style 已经统一到最终 join，保留这一点更利于验证 correctness。

第一版暂不把 `HB/HF` 纳入 dominance key，也不改 NG/DSSR 或 reachable-set 签名。动态边界只是扩展控制参数，不能混入路径状态。

第一版暂不实现 workload 复杂代理，比如根据 label 可扩展 job 数、frontier 段数或 graph node 数选边。先只比较“剩余队列多的一边优先”和“剩余队列少的一边优先”。如果这两个简单策略都没有明显收益，再考虑更复杂的 workload 指标。

第一版暂不把当前 full-domain 对照改成正式版本。已有统计显示 full-domain 不是单调更好；动态版本只是一个新的实验分支。

## 11. 2026-05-28 实现记录

当前实现按后续命名讨论改为“非对称”版本，而不是继续使用过长的 dynamic-halfway-full-domain 类名。新增类为 `GCNGBBAsymmetricBidirectional`，对应 engine 为 `GCNGBBAsymmetricBidirectionalPricingEngine`，配置开关为 `useGCNGBBAsymmetricBidirectionalPricing`，选边策略配置为 `asymmetricBidirectionalSideSelection`。在 `TWETBPCContext` 中，非对称版本优先于 static full-domain 版本接入，避免两个实验 exact pricing engine 同时启用。

实现上复用了 full-domain 版本的函数递推、dominance graph、single-point store、final join、candidate top-K 和 active column 去重。真正新增的逻辑集中在外层调度：`solve()` 改为在 forward/backward 两个队列之间交错弹出；队列强制使用 `TIME` 主排序，reachable size 和 reduced cost 只作为 tie-break；每次弹出 forward label 后用其最早完成时间推进 `dynamicHB`，每次弹出 backward label 后用其最迟完成时间收缩 `dynamicHF`。若 forward 队首最早完成时间已经大于 `dynamicHF`，或 backward 队首最迟完成时间已经小于 `dynamicHB`，则直接清空对应未处理队列，并记录 boundary clear 统计。为了避免浮点误差导致日志里出现极小幅度的 `HB > HF`，更新后会把两者夹到同一侧边界。

扩展可行性中，forward direct feasibility 使用 `dynamicHF` 替代静态 `Tmid`，backward direct feasibility 和 `extendBackward()` 中的下界使用 `dynamicHB` 替代静态 `Tmid`。`precomputeHalfDomainEligibility()` 在该 full-domain 动态版本中不再用 `Tmid` 永久排除 job，只按动态 penalty 是否非空判断；真正的动态剪枝交给 direct feasibility 和队首越界检查。`dynamicHB/dynamicHF` 不进入 label 状态、dominance key、reachable-set signature 或 final join 语义。

新增 `HEU.GCNGBBAsymmetricComparisonTest` 用于同时比较 `halfDomain`、`fullDomain`、`asymmetricMore` 和 `asymmetricFewer`。`asymmetricMore` 表示优先处理剩余 label 更多的队列，`asymmetricFewer` 表示优先处理剩余 label 更少的队列。wet020 单例验证结果为：四种模式 objective/bound 均为 6343 且 `valid=true`；exact pricing 时间约为 half-domain 0.253s、full-domain 0.153s、asymmetricMore 1.512s、asymmetricFewer 1.980s。此前 wet021 单例也保持 objective/bound 一致和 `valid=true`，但两个非对称版本明显慢于静态版本。当前判断是该实现适合作为实验分支保留，用于观察动态边界和不平衡调度的影响；从现有 wet020/wet021 结果看，暂时没有替代当前正式 GCNGBB half-domain 或 static full-domain 的必要。
## 12. 2026-05-28 初始边界收窄试验

针对第一版从 `[L,U]` 全宽动态空间开始导致 early label 过多的问题，改为围绕静态 `Tmid` 预收窄一半：`dynamicHB = (L + Tmid) / 2`，`dynamicHF = (Tmid + U) / 2`。这里 `L = max(dynamicMinHStart, earliestSourceCompletion)`，`U = pricingHorizon`。这个设置保留了动态 HB/HF 继续推进的空间，但避免一开始就允许 forward 扩到全局右端、backward 扩到全局左端。

wet020 上 objective/bound 仍为 6343 且 `valid=true`，但 asymmetricMore exact time 从约 1.512s 变成 1.941s，asymmetricFewer 从约 1.980s 变成 2.042s。统计显示 asymmetricMore 的 forward label 从 7919 降到 7235，dominance checks 从 111793 降到 102842，但 backward label 从 2 增到 53，join function evaluations 从 746 增到 74738，最终 join 代价抵消并超过了 graph 侧收益。

wet021 上该调整略有改善但仍明显慢于静态版本。asymmetricMore exact time 约 9.489s，asymmetricFewer 约 9.654s；相比第一版的约 12.771s 和 11.400s 有下降，但仍远高于 half-domain/full-domain 对照。当前结论是：初始边界收窄可以减少一部分单边 label 膨胀，但会让另一侧生成更多可 join 后缀，整体瓶颈仍在 forward dominance graph 和 final join 的组合。这个设置可以保留为非对称实验版当前默认，但暂时不改变“不替代静态 GCNGBB”的判断。

## 13. 2026-05-28 固定 Tmid 诊断

为了分离“动态中间边界”和“非对称队列流程”两个因素，将非对称实验版进一步改成 `dynamicHB = dynamicHF = Tmid`，并取消 label 出队后对 `HB/HF` 的推进。此时该类仍使用 full-domain 函数标签和非对称外层队列选择，但扩展边界与 static full-domain 的静态 `Tmid` 等价。

wet020 上四种模式仍保持 `obj=bound=6343` 且 `valid=true`。非对称固定 Tmid 后，asymmetricMore/asymmetricFewer 的 exact time 分别约为 0.418s/0.236s；更重要的是统计几乎回到 static full-domain 规模：`fw kept/dominated=1153/10361`、`bw kept/dominated=1395/11854`、`join function eval=274192`、`dominanceChecks=27959`。这说明此前秒级变慢的主要原因不是非对称队列流程本身，而是动态 HB/HF 放宽后生成了静态 Tmid 本会截掉的大量 label。

wet021 上固定 Tmid 后 asymmetricMore/asymmetricFewer exact time 约为 1.400s/1.468s，较动态边界版本的 9 秒量级大幅下降，且保持 `obj=bound=6829`、`valid=true`。因此当前判断调整为：非对称外层队列流程本身可以保留为诊断变量，真正有风险的是动态 HB/HF 放宽扩展边界。后续若继续做动态版，应重点限制动态边界不能明显宽于静态 Tmid，或者改成只在局部、受控条件下放宽，而不是让全局 reachable set 和 final join 直接吃到更宽边界。

## 14. 当前结论

基于 wet020、wet021 的几轮对照，当前可以把这个分支定位为诊断和备份实验，而不是正式改进方向。在当前 TWET 的 GCNGBB-style final-join 框架下，动态 half-way 的主要收益没有体现出来；相反，`HB/HF` 一旦放宽就会让 forward/backward 生成大量静态 `Tmid` 本会截掉的 label，最终同时放大 dominance graph 和 final join。即使把初始区间收窄到 `(L,Tmid)` 与 `(Tmid,U)` 的中点，也只是局部缓解，不能改变整体慢于静态版本的结论。

固定 `dynamicHB=dynamicHF=Tmid` 的诊断说明，非对称队列流程本身并不是主要慢因；它的统计规模会回到 static full-domain 附近。因此问题核心不是“非对称弹队列不对”，而是“动态边界放宽后，当前 final-join 实现承受了更多状态”。后续默认不再继续推进该动态非对称路线，正式 exact pricing 仍以当前 static `GCNGBBStyleBidirectional` 为主，`GCNGBBStyleBidirectionalFullDomain` 和 `GCNGBBAsymmetricBidirectional` 保留为对照与诊断工具。

## 15. 2026-05-28 命名调整

后续回看时需要注意，这两个实验分支本质上只是 GCBB-style 双向 labeling 的工程对照，与正式 `GCNGBBStyleBidirectional` 不是同一个含义。为避免误导，full-domain 对照类改名为 `GCBBStyleBidirectionalFullDomain`，非对称实验类改名为 `GCBBAsymmetricBidirectional`；对应 pricing engine、配置开关和比较 runner 也同步改为 `GCBB...` 前缀。正式 half-domain exact pricing 仍保留 `GCNGBBStyleBidirectional`，本次不改它的命名和默认入口。

## 16. 2026-05-28 动态 HB/HF 下的安全约束

如果后续真正恢复 `dynamicHB/dynamicHF` 的动态推进，而不是当前固定 `Tmid` 诊断特例，那么现在的 single-point special handling 不能直接沿用。原因是 `SinglePointStore` 只按 `reachableSet` 分组和比较，没有记录单点所在的时间坐标；一旦不同 label 分别退化在不同的 `HB/HF` 边界点上，它们的可拼接时间语义不同，不能只凭 reachable set 和 reduced cost 互相支配。动态版要么关闭 single-point special handling，让这类 label 走普通路径或直接保守保留；要么把 point time 纳入 key 和支配判断。

`joinExtendedFrontier` 缓存的风险取决于是否重新启用常数延拓。当前 full-domain 版本的 join extension 直接返回原始 frontier，因此缓存不依赖 `HB/HF`，动态边界推进本身不会让它失效。但如果以后为了动态半域语义重新启用“按当前边界取值再常数延拓”的旧逻辑，缓存就必须带上边界值，或者取消缓存，否则同一个 label 会复用旧边界下生成的延拓函数。

还有一个更直接的实现约束是 `reachableSet` 的时效性。当前 label 的 `reachableSet` 是 label 创建时按当时 `HB/HF` 算出来的；若以后恢复方案 A/B，让 `dynamicHB` 单调右移、`dynamicHF` 单调左移，旧 label 的 `reachableSet` 会变成相对于当前边界的过期上界。这个集合仍可作为候选列表使用，但不能再作为当前边界下可扩展性的最终判断。动态版在每次扩展 child 前必须用当前 `HB/HF` 重新做 direct feasibility，生成 child 后也应再检查 child 的边界资源：forward child 的最早完成不能超过当前 `dynamicHF`，backward child 的最迟完成不能小于当前 `dynamicHB`，通过后才能插入 dominance graph 和 active label table。否则 child 会在边界已经收紧后被插入 label table，哪怕之后出队时被 clear，也已经参与过 dominance 和 final join 统计。

这里的动态边界建议按“非回溯”语义理解：边界收紧后停止继续生成越界 child，但不回头删除此前在旧边界下已经生成并保留的 label。若要把最终 `HB/HF` 当作全局回溯过滤边界，则还必须重做 active label table 和 dominance graph 的有效性维护，否则旧 label 可能已经支配或删除过后续仍需要的 label，风险更大。第一版若继续实验动态边界，应采用非回溯语义，并把过期 `reachableSet` 当作候选上界而不是证明。

## 17. 2026-05-29 扩展前当前边界复核

本次按“旧 `reachableSet` 只作为候选上界”的判断，在 `GCBBAsymmetricBidirectional` 的动态扩展流程里补了一层当前边界过滤。forward label 出队后，不再直接扫描 `label.reachableSet` 扩展，而是先构造一个临时 `currentReachable`：对旧集合中的每个 job 重新调用 `isDirectForwardExtensionTimeFeasible(label.frontier, label.jid, nextJob)`，其中后者使用当前 `dynamicHF`。backward 侧同理，重新调用当前 `dynamicHB` 下的 `isDirectBackwardExtensionTimeFeasible(...)`。

这里特意不把 `canExtendForward(...)` 和 `canExtendBackward(...)` 混入临时边界候选构造。临时候选集只回答一个问题：旧 reachable set 里的这个 job 在当前 `HB/HF` 下是否仍满足直接时间可行性。visited、zero-dual、分支 forbidden arc 等扩展合法性继续留在真正扩展前的 `canExtend...` 里统一检查。这样语义更接近 `GCBidirectional`：先从 `reachableSet` 枚举候选，再在扩展点检查当前 direct arc 是否允许。

这里没有直接从 `label.reachableSet` 里删除 bit。原因是 label 已经插入过 dominance graph，`reachableSet` 属于 dominance 状态的一部分，事后原地修改可能让图结构里登记的状态与对象当前状态不一致。使用临时过滤集可以达到扩展前重新判定的目的，同时保持已有 dominance 状态稳定。child 生成后也补了一次边界资源检查：forward child 的最早完成时间不能超过当前 `dynamicHF`，backward child 的最晚完成时间不能小于当前 `dynamicHB`，通过后才插入 label table。

因此，现在即使以后重新启用真正动态更新 `HB/HF`，旧 label 的 `reachableSet` 偏宽也只会造成候选集保守，不会直接导致越过当前边界继续生成 child。它仍不是一个回溯过滤：此前在旧边界下已经生成并插入的 label 不会被删除，这一点和上一节的非回溯语义一致。

验证上，focused `javac` 通过；wet020 的 asymmetricMore 单例仍为 `obj=bound=6343` 且 `valid=true`，exact pricing 时间约 0.347s。当前固定 `HB=HF=Tmid` 的诊断默认下，这个复核基本不改变结果规模；它主要是为后续重新启用真正动态边界时补上安全网。

## 18. 2026-05-29 forbidden arc 与 reachable set 的边界

进一步复核 `GCBidirectional` 和非对称实验分支后，当前结论是 forbidden arc 不应写进 `reachableSet`。`reachableSet` 用于 dominance 状态，表达的是从当前 label 往后仍可能访问哪些 job；而 forbidden arc 是 direct arc 约束，只禁止当前前驱到某个后继的这一条弧。例如 `i -> j` 被分支禁止，并不意味着 job `j` 永久不可访问，后续仍可能通过另一个前驱 `k -> j` 进入。因此把 forbidden arc 用来更新 reachable set 会改变 dominance key 的含义，严重时可能把仍可通过其他路径访问的 job 误删。

实际使用上，forbidden arc 应保留在扩展和拼接的 direct arc 检查处。forward 扩展时检查 `node.isArcForbidden(label.jid, nextJob)`；backward 扩展时检查 `node.isArcForbidden(prevJob, successor)`；final crossing join 和 forward->sink 收尾也分别检查对应直连弧。`canExtend...` 中保留 zero-dual 检查只是防御性合法性闸门，并不是为了处理 `HB/HF` 更新；动态边界复核本身只需要重做 direct time feasibility。

从调用时机看，`canExtendForward/Backward` 只在 label 出队、准备生成 child label 前使用。`GCBidirectional` 和当前非对称分支的共同流程都是先从 `label.reachableSet` 枚举候选 job，再调用 `canExtend...` 判断“这一次 direct transition 是否允许”。其中 `node.isArcForbidden(from, to)` 是 O(1) 判断：先查 `Data.preprocessedArcForbidden[from][to]`，再查当前节点的 `arcState[from][to] == ARC_FORBIDDEN`。前者是全局静态不可行弧过滤，后者是分支树节点上的显式禁弧状态。required arc 分支会通过禁止同一前驱的其他出弧、同一后继的其他入弧等方式转化为这些 direct forbidden arc，因此 pricing 扩展阶段只需要查 `isArcForbidden`。

由此看，`canExtend...` 中除 forbidden arc 外的大部分判断在当前枚举方式下是冗余防线。因为循环本身已经从 `label.reachableSet` 取候选，`reachableSet.contains(job)` 正常必为真；reachable set 构造时也已经排除了 visited job、half-domain 不合格 job、zero-dual/singleton 规则不允许的 job，以及当前标签函数下直接时间不可行的 job。保留这些重复判断的好处是防止以后有人改成扫描全集、或动态边界候选构造出现偏差时漏掉合法性检查；如果追求更干净的职责边界，可以把真正扩展点的 `canExtend...` 收窄为 direct forbidden arc 检查，并保留必要断言或注释说明 reachable set 的不变量。

## 19. 2026-05-29 扩展点冗余检查清理

本次按上述判断检查了 `TWETBPC.GC` 下所有还保留 `canExtend...` 的 pricing 类：`GC`、`GCBidirectional`、`GCNGBBStyleBidirectional`、`GCBBStyleBidirectionalFullDomain` 和 `GCBBAsymmetricBidirectional`。这些类的扩展循环都不是扫描全集，而是从 `label.reachableSet.nextSetBit(...)` 枚举候选；child 的 reachable set 也都从 parent reachable set 继续收缩。因此在扩展点重复检查 `visitedSet.contains(...)`、`reachableSet.contains(...)`、zero-dual/singleton 规则并不会改变正常结果，只会增加一层防御。

代码上已把这些 `canExtend...` 收窄为 direct forbidden arc 检查，并在方法处补注释说明 reachable set 负责维护 visited、zero-dual/singleton 和时间可行性不变量。原来的 visited/reachable/zero-dual/singleton 检查没有完全删掉，而是作为注释掉的防御块保留在方法中，便于后续回看不变量或在改成全集扫描时恢复。forward 检查的是 `label.jid -> nextJob`，backward 检查的是 `prevJob -> successor`。非对称动态版本稍有不同：它先从旧 reachable set 构造当前 `HB/HF` 边界候选，再调用 `canExtend...`；因此它的 `canExtend...` 也只保留 forbidden arc 检查。

同时统一了几个双向 GC 类的类级说明：`GCBidirectional` 定位为 hybrid-B/eager join 基准，核心差异是 backward label 出队时立即 join、列返回受出队顺序影响；`GCNGBBStyleBidirectional` 虽保留历史类名，但明确为当前正式 half-domain GCBB-style final join 基准；`GCBBStyleBidirectionalFullDomain` 是 full-domain 对照版，只改变标签定义域和 join extension 语义；`GCBBAsymmetricBidirectional` 是 full-domain 基础上的非对称队列诊断分支，只改变两侧队列调度和边界候选复核。

验证上，focused `javac` 覆盖上述五个 pricing 类和 `PaperDominanceGraphConsistencyTest`；一致性测试通过 `cases=200, insertions=16000`；wet020 asymmetricMore 单例仍为 `obj=bound=6343` 且 `valid=true`。

## 20. 2026-05-29 非对称扩展边界候选的注释语义

进一步明确非对称版 `currentForwardBoundaryCandidates` / `currentBackwardBoundaryCandidates` 的目的。正常固定边界下，扩展只需要从 `label.reachableSet` 枚举候选即可；这两个 helper 是为以后恢复真正动态 `HB/HF` 时准备的。早期 label 可能在较宽边界下生成，等它出队时当前 `HF/HB` 已收紧，旧 reachable set 里的部分候选不再满足当前一跳时间可行性，此时只跳过该扩展。

这里不回写 `label.reachableSet`。原因是该集合已经作为 dominance 状态登记到 graph 中，事后修改会让图结构 key 和对象状态不一致。reachable set 偏宽只会让候选集更弱、更保守，不会漏掉可行列；真正影响分支合法性的 forbidden arc 仍在扩展点即时检查。

## 21. 2026-05-29 动态 HB/HF 复测

本次按可逆方式复测了两种边界初始化：先保持当前默认 `dynamicHB=dynamicHF=Tmid`，再临时切到方案 A 的全宽动态空间，即 `dynamicHB=max(dynamicMinHStart, earliestSourceCompletion)`、`dynamicHF=pricingHorizon`，跑完后源码恢复为固定 `Tmid`。复测只用于确认扩展前当前边界过滤加入后，真正动态 `HB/HF` 是否仍然比固定 `Tmid` 慢。

`wet020_001_2m` 上，固定 `Tmid` 时 full-domain、asymmetricMore、asymmetricFewer 均为 `obj=bound=6343` 且 `valid=true`，对应 solve/exact 时间分别为 `1.203s/0.331s`、`1.249s/0.368s`、`1.308s/0.427s`。临时全宽动态 HB/HF 下结果仍然正确，但 full-domain、asymmetricMore、asymmetricFewer 的 solve/exact 时间变为 `1.912s/0.410s`、`3.239s/1.946s`、`4.235s/2.993s`。其中 full-domain 不受非对称源码切换影响，主要作为同轮参照；真正变化的是两个 asymmetric 模式。

日志统计显示，`wet020` 的 asymmetricMore 在固定 `Tmid` 下 forward/backward kept 为 `1153/1395`，join function evaluations 为 `274192`；切到全宽动态后 forward/backward kept 变为 `7919/2`，join function evaluations 只有 `746`，但 dominance graph 侧显著膨胀，`dominanceChecks` 从 `27959` 增至 `111793`，`superset visited` 从 `211238` 增至 `3708163`。也就是说全宽动态边界不是把工作平衡到了两侧，而是让 forward 侧生成大量静态 `Tmid` 本会截掉的 label。

`wet021_001_2m` 上，固定 `Tmid` 时三种模式同样均为 `obj=bound=6829` 且 `valid=true`，full-domain、asymmetricMore、asymmetricFewer 的 solve/exact 时间分别为 `1.555s/0.593s`、`1.587s/0.598s`、`1.717s/0.747s`。临时全宽动态 HB/HF 下，full-domain、asymmetricMore、asymmetricFewer 的 solve/exact 时间为 `3.148s/1.404s`、`16.903s/14.848s`、`14.763s/13.030s`。asymmetricMore 日志中三次 exact pricing 分别约 `6.328s`、`4.937s`、`3.584s`，forward kept 约 `11k`，backward kept 只有 `2-4`，仍然是单侧 label 膨胀。

当前结论不变：新增的当前边界候选复核能保证旧 reachable set 在边界收紧后不会继续生成越界 child，但不能改变全宽动态 HB/HF 的核心问题。问题不是扩展合法性，而是初始边界放得太宽后，当前 final-join 框架承受了大量静态 `Tmid` 会截掉的状态。固定 `dynamicHB=dynamicHF=Tmid` 仍应作为当前默认诊断分支；真正动态 HB/HF 只保留为注释方案，后续若继续研究，需要重新设计工作量平衡或回溯清理语义，而不是只靠扩展前复核。

## 22. 2026-05-29 注释对齐与 root arc dual 防御检查

本次只做语义澄清和 debug 防御。`currentForwardBoundaryCandidates()` / `currentBackwardBoundaryCandidates()` 的注释已改成和当前实现一致：临时候选集来自 `label.reachableSet`，因此 visited、zero-dual 和静态时间可行性依赖 reachable set 构造不变量；helper 只额外按当前 `HB/HF` 重做 direct time feasibility。真正扩展点的 `canExtendForward/Backward` 现在只即时检查 direct forbidden arc，因为这类节点/预处理禁弧不应写入 dominance key。

另外，在 `dualProfitableWindowEnabled` 且 `Configure.debugBPCPricingColumnCheck=true` 时，新增 root arc dual 扫描。当前 `canUseDualProfitableWindow()` 的前提仍是根节点、无 active cuts；按现有模型结构，此时 arc dual 应只来自分支或 cut，因此为 0。新增检查用于防止后续如果加入 root-level arc constraints，还继续误用 `pi_j=0` 排除和 profitable window 的三角不等式假设。正常 debug 关闭时不进入该扫描，不影响运行路径。

验证：focused `javac` 通过；`wet020_001_2m` 的 `asymmetricMore` 单例仍为 `obj=bound=6343` 且 `valid=true`，exact pricing 时间约 `0.261s`。
