# Work 3 首版实现正确性审计

日期：2026-09-06

## 1. 结论

本次按 `implementation/` 下三份 2026-09-06 规格逐式核对独立工程 `D:\软件\eclipse\workspace\StochasticDueWindowScheduling`。两个基础模型的核心数学语义正确：2-index 的路径、紧 \(M\) 时间传播、窗口和 ET 目标与规格一致；P-W 的任务—位置分配、wide dummy、无条件时间递推、绝对窗口和目标展开也一致。按后续明确要求，P-W 的 \(y,u,z\) 已全部显式声明为 0–1 变量。2-index SEC、P-W 聚合 SEC、显式 \(C\) 上下界和显式 \(c\) 上界均未进入基础模型。

当前实现对应的是 README 明确指定的“共同 \(\rho,\lambda,\alpha,\beta\) 首轮双模型比较”，不是较早文档中的 W3-FullHet。该口径由 2026-09-06 实现规格覆盖 2026-08-31 的一般化方案，因此标量费率在当前原型中不是漏实现；若以后恢复 FullHet，需要另行修改数据结构和 position 表示，不能把当前 P-W 直接称为全异质模型。

审计没有发现基础模型的阻断性建模错误。初审发现的 \(\ell\) 系数边界问题已经修正；其余事项经重新区分后属于可选防回归检查或测试口径，不是模型错误。

## 2. 已修正的 \(\ell\) 数值边界问题

`InstanceData.minimumWindowAndTardinessCost` 在判断固定 \(b\) 后应采用哪个左端点时使用了

```java
locationRate >= widthRate - EPS
```

而数学规格要求精确区分 \(\rho\ge\lambda\) 与 \(\rho<\lambda\)。当 \(\lambda-\mathrm{EPS}<\rho<\lambda\) 时，代码会错误进入 \(\rho\ge\lambda\) 分支，选择较小的 \(a\)，从而可能把真实的最小费用算高。该值随后用于 A-VI、VI-1 和 VI-2；费用“下界”一旦被高估，就可能切掉合法整数解或最优解。

这是数值边界问题，不影响当前随机生成器使用的 \((\rho,\lambda)=(1,1.5)\)，但对以后读取任意算例不安全。当前已经改为：对每个候选 \(b\) 同时评价允许的最小和最大 \(a\)，再取较小目标值。因为固定 \(b\) 后目标关于 \(a\) 是线性的，最优解必在两个端点之一；该实现与按 \(\rho-\lambda\) 符号分类完全等价，同时不再依赖 `EPS` 选择分支。

## 3. 可选检查与测试口径

### 3.1 预处理断言

任务—位置最短/最长 walk、不同任务加工量界、前缀 \(H_{k\omega}\)、跨场景 \(H_k\)、任务级聚合界和弧 \(M\) 的实现方向均正确。当前代码只检查了 \(\underline C_{jk\omega}\le\overline C_{jk\omega}\)，尚未实现规格要求的以下自动断言：

\[
H_{k\omega},H_k\text{ 随 }k\text{ 非降},\qquad
\overline C_{jk\omega}\le H_{k\omega}\le H_k,
\]

\[
H_{k\omega}\le H^{\mathrm{coarse}}_{k\omega},
\]

以及小规模枚举排程对任务—位置界的核验。它们只是防止以后修改代码时出现下标和递推回归，不是理论推导仍有疑问，也不是模型成立所必需。根据当前决定，不把这些额外断言加入生产实现。

### 3.2 不额外重算 ET

现有验证器使用求解器返回的 \(E,T\) 重算目标，并检查

\[
E\ge a-C,\qquad T\ge C-b,
\]

但没有独立重算

\[
E=(a-C)^+,qquad T=(C-b)^+.
\]

在当前模型中 \(\alpha,\beta>0\)，最小化目标会使 \(E,T\) 自动取到允许的最小值，因此不需要再单独重算 \((a-C)^+\) 和 \((C-b)^+\)。当前决定保留现有结果提取方式，不增加重复验证逻辑。

### 3.3 测试的一致性判定

这里的区别是：incumbent 只是求解过程中已经找到的最好可行解；若达到时限但尚未证优，它不代表模型最优值。随机一致性入口已经从生产源码移到 `test/`，并改为只有四种配置都达到 CPLEX 最优状态后才比较目标；任一配置未证优时直接报告本次一致性验证无法完成，不再输出模型等价结论。

## 4. 已核对正确的实现

1. 2-index 中每个任务恰有一条进入和离开弧，源路径数等于终点路径数且不超过 \(M\)。严格正加工时间与激活时间传播排除整数任务环，因此当前不加 SEC 不改变整数可行集。
2. 2-index 的首任务与任务弧时间约束代数符号正确；任务完成时钟只有非负下界，预处理界没有被设置成显式变量界。
3. P-W 的 \(y,u,z\) 均为显式 0–1 变量；prefix occupancy 与 \(z\) 连接正确。空位置被唯一封闭为 \([0,H_k]\)，dummy 时间可以继承最后一个真实时钟且存在零 dummy ET 的最优代表解。
4. P-W 窗口成本的展开在真实位置恢复 \(\rho(A-L_j)+\lambda(B-A-D_j^{\min})\)，在 dummy 位置严格为 0。
5. A-S2、A-S3、A-VI、VI-1、VI-2、TLB-1、TLB-2、简单迟到下界、机器负载/位置排序和 P-U2/P-U3 的代码符号与当前规格一致。
6. 随机生成器产生场景相关加工时间、随机序列相关 setup 时间和确定 setup 金额；默认费率避开当前已记录的充分退化条件，但这只排除参数导致的必然边界结构，不保证每个随机实例都出现内部正长度窗口。

## 5. 动态验证

当前源码使用 Java 22、CPLEX 22.1.1 和 `-Xlint:all` 重新编译通过。在原有 7 组联调实例基础上，本次增加 10 组 \(n=4\) 至 8、单机至 4 机、1 至 5 场景的随机对拍，并增加 4 组定向算例，覆盖非均匀场景概率、\(\rho\ge\lambda\)、\(D_j^{\min}=0\)、单机和每任务一台机器。最后又从当前源码重新编译并运行 1 组独立随机算例。共核对 22 组实例；每组的 2-index 基础/强化和 P-W 基础/强化 incumbent 目标一致，现有验证器均返回 `true`。

把 \(u,z\) 改为显式二元并迁移测试目录后，又重新运行默认实例以及 5 组覆盖单机、每任务一机、单场景和多机多场景的随机实例，四种配置均达到 CPLEX `Optimal/OptimalTol` 状态并返回相同目标。新增的 `WindowCostOracleTest` 使用 \(\rho\) 与 \(\lambda\) 只相差 \(0.5\times10^{-9}\) 的两侧算例，验证新的双端点评价不会再发生 `EPS` 分支错误。

该结果是较强的一致性证据，但不是数学等价性的替代证明。后续修改需要重新执行独立 `test/` 下的一致性测试。

## 6. Git 隔离结论

两个工程的 Git 确实独立：

- 原 TWET/BPC 仓库根目录为 `D:\软件\eclipse\workspace\TWETScheduling`，Git 目录为其自身 `.git`，分支为 `master`，并连接原 GitHub remote。
- 新工程根目录为 `D:\软件\eclipse\workspace\StochasticDueWindowScheduling`，Git 目录为其自身 `.git`，分支为 `main`，没有 remote。
- 两个仓库都没有 object alternates；旧仓库不能解析新工程提交，新工程也不能解析旧仓库提交；新工程不在旧工程目录内，也不属于旧仓库的 worktree。

因此，今后在新工程中提交代码不会进入 BPC 仓库。需要额外区分的是：Work 3 的主讨论和实现规格仍保存在旧仓库 `docs/work3/` 中，新工程 `docs/` 内另有三份规格副本。也就是说“代码仓库和提交历史”已经分开，但“研究文档”目前是旧仓库保留主记录、新工程保留实现副本，并非所有 Work 3 文档都已从旧仓库迁走。

## 7. 本轮后续决定

1. \(\ell\) oracle 不再用 `EPS` 决定 \(\rho-\lambda\) 的符号，已改为评价两个左端点。
2. 不增加额外 \(H\) 理论断言，也不重复计算 ET；这些不是当前模型正确性的必要组成。
3. P-W 的 \(y,u,z\) 全部显式设为 0–1。
4. 随机算例一致性代码移入 `test/`，只有全部达到 CPLEX 最优状态后才报告目标一致。

本次修改完成后需要重新编译并运行基础/强化四配置对拍，再更新动态验证结果。
