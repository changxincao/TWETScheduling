# 第二远端正式80/100批次

2026-09-07：从用户当天配置记录找回第二主机 `codex-runner@100.68.243.192`（DESKTOP-42976CD），用户指定工作目录 `D:\ccx`。专用私钥沿用 `C:\Users\Changxin\.ssh\codex_solver_auto_ed25519`，本机指纹库使用 `C:\Users\Changxin\.ssh\known_hosts`，保持 BatchMode、IdentitiesOnly、StrictHostKeyChecking=yes。不能混用旧主机的目录和指纹文件。

部署目录 `D:\ccx\work1\deployments\20260907-93685d4a`；正式输出目录 `D:\ccx\work1\experiments\20260907-formal-n80-n100`。Java 为 `D:\Java\jdk-21`，CPLEX 为 `D:\software\IBM\ILOG\CPLEX_Studio2211`，未安装软件或修改系统配置。复用旧正式批次冻结的93685d4a运行包，SHA256为 `8e6a883329e09c249a451c28d44a6bd39354615f18e04038b7717e01acbeb97e`，远端核验一致。

清单取正式pricing-comparison.tsv的80/100任务：机器数3/4/5，random/family，base/medium/high，zero/narrow/wide，共540个共享seed、1620项三算法求解。每次完整求解18000秒，6并发JVM，CPLEX单线程。Cluster和successor-set关闭，其他参数沿用正式配置。另有n20验收seed及三算法短测，不计入正式结果。

541项输入SHA256核验通过；正式求解scan-only确认每种算法540项。n20三算法均完成运行且排程可行、目标重算一致34811；TI短测设2节点因而NODE_LIMIT，不能误称三算法均完成最优求解。配置快照确认TIME队列、CPLEX线程1、两类新分支关闭。

启动方式：SSH使用-f -n及上述安全选项，远端命令为 `cmd /c "cd /d D:\ccx\work1\experiments\20260907-formal-n80-n100 && pipeline.cmd >scheduler-logs\pipeline.log 2>scheduler-logs\pipeline.err.log"`。不创建计划任务或服务。启动后主动断开本地启动SSH，再重新连接确认scheduler PID17940及6个seed JVM仍存活，状态SEED_RUNNING。seed全部生成后guard验收，再复核smoke，自动接续worker的6并发正式求解；失败停在FAILED，不静默跨过验收。不要重复启动或删除lock。旧主机60及以下批次未改动。

可复现打包脚本：`scripts/remote/prepare-large-20260907.ps1`；本地生成目录 `.codex-tmp/large-20260907`，保留清单、输入校验表及启动脚本。当前只确认启动和断线存活，正式540份seed尚未全部生成。
