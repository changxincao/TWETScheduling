# 正式实验当前统一方案

本文档把 2026-08-20 至 2026-08-23 的实验讨论整理成一套可执行口径。早期记录中的 25,920 次四方法全因子、原尺度主表加时间放大补充面板、每规模 20 个源实例完整交叉，以及只保留 900 次外包定向面板等方案均为讨论过程，不再作为当前执行方案。当前纯调度和外包性能实验使用同一组基础环境；纯调度改变时间尺度，外包实验固定原尺度并以三档外包价格替换三档时间尺度。数量折扣面板按最新每格 5 个实例的计算预算记录。加入 `n=60` 后，正式求解总量为 `4860+3240+90=8190` 次，不含共享初始列生成和小规模正确性验证。外包性能部分暂不运行 compact CPLEX，只比较两种 BPC 外包表示。

## 1. 共同数据生成口径

### 1.1 源任务集合

正式规模使用 `n=20/40/50/60/80/100`。每个规模固定选择 5 个任务集合，不根据求解时间、gap 或任何算法结果筛选。

`n=20` 和 `n=40` 从同一批 5 个 Tanaka 40-task 文件构造。对每个父实例使用固定随机种子排列任务，前 20 个任务形成 n20，全部任务形成 n40。`n=50` 从原生 Tanaka 50-task 文件中固定随机选择 5 个。`n=60/80/100` 从同一批 5 个 Tanaka 100-task 文件构造，固定排列后的前 60、前 80 和全部任务分别形成 n60、n80 和 n100。由此既保留 Tanaka 的 processing time 与 TWET 权重，也能在 20/40 和 60/80/100 之间做嵌套规模比较。

所有抽样种子、父文件编号、任务排列和保留下标必须写入 metadata。正式数据生成不得读取历史求解结果。

### 1.2 机器数和 due-window

`n<=60` 使用 `m=2/3/4`，共同参考机器数为 `m_ref=3`；`n>=80` 使用 `m=3/4/5`，共同参考机器数为 `m_ref=4`。同一任务集合在不同机器数下共用 processing time、TWET 权重、due-window center、setup time 和 setup cost，只改变机器数量，避免把机器数效应与另一套随机数据混在一起。

对子集实例，先按保留 workload 比例缩放父实例的 Tanaka due date，再按参考机器数生成共同中心：

\[
d_j^S=\operatorname{round}\left(d_j\frac{\sum_{k\in S}p_k}{\sum_{k\in J}p_k}\right),
\qquad
c_j=\left\lceil\frac{d_j^S}{m_{ref}}\right\rceil.
\]

窗口半宽为 `W=0/100/300`，任务 j 的 due window 为 `[max(0,c_j-W),c_j+W]`。W 是统一半宽，不对单个任务再加随机扰动。W0 保留为零宽边界情形；W100 和 W300 分别代表中等和较宽窗口。

### 1.3 Setup time 和 setup cost

每个任务集合生成 random 和 family 两套 sequence-dependent setup。两者最终都校准到

\[
\operatorname{avg}_{i\ne j}(s_{ij})/\bar p\approx 0.5,
\qquad
s_{ij}\le \lfloor\bar p\rfloor,
\]

因此 setup 总强度保持为相同的中等背景条件，主要比较随机结构与 family 聚集结构，而不是把 setup 大小再增加为一个实验因子。

具体令

\[
\mu=0.5\bar p,
\qquad
C=\lfloor\bar p\rfloor.
\]

新上限使单条 setup 不超过平均 processing time；取整前的区间 `[0,\bar p]` 关于 `\mu` 对称，`floor` 带来的小偏差由后续位置参数校准吸收。random 原始弧采用截断正态，目标均值为 `\mu`，标准差为 `\mu/2`。这里的目标均值是截断后的条件均值；实现时通过一维数值求解确定截断前正态位置参数，再用逆累积分布函数采样，不能把位置参数直接当成截断后的实际均值。

family 数随 `n=20/40/50/60/80/100` 分别取 `3/3/4/4/5/6`。对每个任务集合先用固定随机种子打乱任务，再按 round-robin 分配到 family，使任意两个 family 的任务数最多相差1，且分组不使用processing、due date、权重或求解结果。

random连续弧完成真实任务Floyd闭包、总体均值校准和整数化后得到基础有向度量 `D`。family不再独立抽样或再次Floyd，而是在同一个 `D` 上定义

\[
s^F_{ij}=\left\lceil\min\left\{\gamma
\left(D_{ij}+\Delta\mathbf1\{f(i)\ne f(j)\}\right),C\right\}\right\rceil,
\qquad \Delta=2\bar p.
\]

虚拟起点弧不增加换族项，终止列仍为0。`gamma` 是family矩阵唯一的总体尺度，用于使job-to-job平均setup重新接近 `mu=0.5\bar p`。family指示距离满足三角不等式，因此它与基础有向度量的非负加权和仍是有向度量；统一缩放、向上取整和上限截断也保持三角性质。生成后仍执行独立三角和二次Floyd审计，但不再用Floyd修改family矩阵。

正式30个base family任务集合的最终类间/类内均值比为 `4.6336--5.0959`，平均为 `4.7178`，并未随任务规模衰减；random和family的总体均值仍保持一致。setup cost 最后固定为

\[
\kappa_{ij}=20s_{ij}.
\]

生成包的 `instances/setup-audit.tsv` 集中记录 `mu`、`C`、`q`、截断分布位置参数、原始均值、第一次 Floyd 后均值、首次 Floyd 改写弧数、最终校准系数、最终均值及目标误差、最大 setup、上限命中率、三角违例数、审计 Floyd 改写弧数、最终类内/类间均值和 family 分配。各 `.dat` 只保存求解所需的任务与 setup 数据，避免把审计字段混入求解输入格式。

这里的 `0.5\bar p` 不是 Bulhoes et al. (2020) 的原算例档位。该文实际使用 Kramer (2015) 在 Sen and Bulbul (2015) 算例上增加的两档 setup：令 `\bar s=\eta\bar p`，取 `\eta=0.25/0.75`，并从 `[0,2\bar s]` 内的正态分布生成。本文取 `\eta=0.5`，可以理解为两档之间的中等强度插值；但本文还增加了真实任务 Floyd、最终均值校准、单弧上限和配对的 family 度量，因此生成方法并不与该文完全相同。论文中只能表述为“setup 强度尺度参考 Kramer 算例并取中间水平”，不能表述为复现其 setup 数据。

### 1.4 时间尺度

纯调度实验使用原始、中等放大和高放大三个尺度。对每个规模用固定种子生成 5 个中等倍率 `g_L in [5,15]`，并令 `g_H=g_L+10 in [15,25]`。同一源任务集合的所有算法、机器数、窗口和 setup 结构共用相同倍率。

放大时 processing time、due-window center、窗口半宽、setup time 及其他时间量统一乘以 g，TWET 单位时间权重不变，setup cost 重新按放大后的 `20s_ij` 计算。W0 放大后仍为 0；W100/W300 表示原尺度半宽，写入放大文件时同步变成 `100g/300g`。

时间尺度不再是 solver 参数。所有放大实例必须在生成实验包时写成独立 `.dat`，manifest 直接引用最终文件；runner 读取数据后不会再执行 `timeScale` 变换。

### 1.5 最终数据生成方法总表

| 数据项 | 最终生成方法 | 配对与落盘口径 |
|---|---|---|
| 源任务集合 | 从 Tanaka 的 40/50/100-task 目录分别按固定种子无放回选择5个父实例，不读取算法结果 | `20/40` 和 `60/80/100` 分别使用同一随机排列的前缀形成嵌套任务集；n50保留原顺序 |
| 加工时间与TWET权重 | 直接继承被选中的 Tanaka 任务 | 时间放大时只放大 processing，早到/迟到单位权重不变 |
| 机器数 | `n<=60` 使用 `m=2/3/4`；`n>=80` 使用 `m=3/4/5` | 同一任务集跨机器数共用任务、center和setup，只修改机器数 |
| due-window center | 先按抽取后 workload/父实例 workload 缩放 Tanaka due date，再除以 `m_ref=3/4` 并向上取整 | 不同实际机器数共用同一 center |
| due-window 半宽 | 原尺度取 `W=0/100/300`，任务窗口为 `[max(0,c_j-W),c_j+W]` | W由manifest固定传入；放大尺度使用 `gW`，不复制三份相同任务数据 |
| random setup | 截断正态的目标均值为 `0.5\bar p`、尺度为 `0.25\bar p`、范围为 `[0,floor(\bar p)]`；真实任务 Floyd 后统一校准并向上取整 | 形成满足有向三角不等式的整数基础度量 `D` |
| family setup | 固定种子均衡分为 `3/3/4/4/5/6` 个 family；在同一 `D` 上对跨族真实任务弧增加 `2\bar p`，再整体校准到均值 `0.5\bar p` | 不再次 Floyd；最终类间/类内均值比为 `4.6336--5.0959` |
| setup cost | `kappa_ij=20s_ij` | 数据加载后由最终 setup time 确定 |
| 时间尺度 | base倍率为1；每个规模的5个中等倍率从整数 `[5,15]` 固定无放回抽取，高倍率为中等倍率加10 | processing、center和setup写入独立尺度文件；权重不缩放，solver无 `timeScale` 参数 |
| 外包标准报价 | `q_j=p_j max{w_j^E,w_j^T}`，价格档 `b_j(lambda)=lambda q_j`，`lambda=0.5/1/2` | 外包实验仅使用base尺度；两种外包表示共享同一经济场景 |
| 数量折扣 | 从不重复n50任务集的 `sum_j q_j` 中位数一次确定 `Q1=0.25Q50_ref`、`Q2=0.50Q50_ref`；边际费率为 `(1,0.85,0.70)` | 断点跨规模固定；无折扣对照为 `(1,1,1)` |
| 共同初始列 | 每个场景使用3个由场景ID和重复编号确定的随机流，各运行ALNS最多60秒，选择成本最好且编号优先的完整快照 | 所有待比较BPC共享同一列/incumbent/外包集合快照及SHA-256 fingerprint；该时间单独记录 |

以上是当前实际生成器、正式manifest和落盘数据共同采用的口径。历史讨论中的其他setup比例、窗口、固定倍率、报价公式和删减面板均不是最终数据规则。

## 2. 实验一：无外包的三种 BPC 比较

本实验关闭外包，比较三种固定算法：`NG_DSSR`、`TIME_INDEXED` 和 `TIME_INDEXED_SRI`。compact CPLEX 不进入正式性能主矩阵，避免在明显超出其能力的时间域重复超时。

三种算法完整交叉 6 个规模、每规模 5 个任务集合、3 个窗口、2 类 setup、3 个机器数和 3 个时间尺度：

\[
6\times5\times3\times2\times3\times3\times3=4{,}860
\]

次正式求解。每一个 `instance/setup/W/m/scale` 场景先用 3 个预先固定、仅由场景 ID 和重复编号生成的随机数种子独立运行初始 ALNS。每次最多 60 秒，按真实 incumbent 成本选择最好的一次；目标相同时固定选择重复编号较小者。只保存获胜 run 的完整 `FixedInitialColumnSeed`，不把三个 run 的列集合并，否则会改变初始 RMP 大小。该快照包含进入初始 RMP 的全部列序列，以及获胜 incumbent 使用的机器序列；这里的 `FixedInitialColumnSeed` 是固定初始列快照，随机数 seed 则是三次 ALNS 的随机流起点，两者不能混称。

三种 BPC 随后读取同一个快照，并用当前实例重新评价列成本。SHA-256 fingerprint 用于确认三个 run 读到的序列快照完全相同。seed 生成文件记录三次随机数 seed、各自 incumbent 成本、实际用时、初始列数、incumbent 列数和 fingerprint，以及最终获胜编号和总生成时间。最多前处理时间为 `3*60=180` 秒；这部分只生成一次并被三种算法共用，单独报告，不计入任何一种 BPC 的求解时间。这样算法间差异不会来自 ALNS 墙钟停止或随机轨迹造成的不同起点。

NG_DSSR 固定保留完整 time-indexed root preprocessing，因此论文中应把它明确写成该方法的组成部分，而不是把结果解释为纯 ng-DSSR。

本节首先报告 solved count、总时间、最终 gap、root bound/gap、节点数、列数、cut 数、master LP、pricing、cut separation、strong branching 和 repair 时间。timeout 必须保留最终 incumbent、bound 和 gap，不能只统计成功实例。

root 下界保留现有两个正式口径即可。`rootBoundBeforeCuts` 是 root 的 exact pricing 已闭合、但尚未分离 cut 时的下界；`rootBound` 是 root 的全部 pricing/cut 循环结束后的最终下界。无 cut 方法通常两者相同，rank-1/SRI 方法的后者可能更强。NG_DSSR 启用 time-indexed root preprocessing 时，预处理先写回安全的时间窗和禁弧，再由 ng-DSSR 完成正式 root pricing，因此这里记录的 ng-DSSR root bound 是预处理后的正式 root 下界；临时预处理 LP 只用于缩域，只单独报告预处理耗时和状态。

time-indexed 额外报告 horizon、dense/active states、图节点与图弧、root compact-window、arc fixing 动作数以及真正新增的 `nodeTimeArcNewlyFixed`。NG_DSSR 额外报告 exact/heuristic pricing 时间、label 数、DSSR 轮数、completion-bound 构造与剪枝、midpoint probe、join、正反向扩展及正反不平衡。

本节总体比较 ng-DSSR 与 time-indexed 两类定价方式的适用范围，并分析任务规模、机器数、窗口宽度、setup 结构和时间尺度对求解效率的影响。rank-1/SRI 作为 time-indexed 的加强版本，用于判断更强 root/tree bound 能否抵消 cut 和带 cut pricing 的额外成本。

## 3. 实验二：两种外包表示的性能比较

外包实验只使用原时间尺度。BPC 内部机器调度统一使用 `NG_DSSR`，不根据实例临时切换。当前比较两种方法：外包集合也列化的 `columns` formulation，以及在 master 中显式保留外包和 tariff 变量的 `masterVariables` formulation。compact CPLEX 暂不进入正式批次。

此前暂定的任务标准报价量为

\[
q_j=\bar p\max\{w_j^E,w_j^T\}.
\]

该式量纲正确，但它使同一实例中加工时间为 1 和 100、权重相同的任务具有完全相同的外包报价。当前 Tanaka 40/50/100 样本的 processing time 实际范围正是 1--100，因此该式会把释放内部产能最多的长任务相对定价得过低，可能人为诱导长任务外包。正式生成建议改为

\[
q_j=p_j\max\{w_j^E,w_j^T\}.
\]

其中 `p_j` 反映外部加工 workload，`max` 表示外部承运商按任务较严格的一侧服务代价报价。使用 `max` 本身可以解释为保守的服务等级价格，但它会使高 TWET 权重同时提高内部违约成本和外包报价，因此不能把“高权重任务更容易外包”预设为结论。如果不希望外包商报价依赖企业内部惩罚权重，更中性的备选才是 `q_j=p_j*w_ref`，其中 `w_ref` 是跨任务固定的代表性权重。当前主方案采用 `p_j*max`，同时在论文中说明上述业务含义。

令任务的未折扣外包支付为 `b_j(lambda)=lambda*q_j`。如果任务在 due window 的某一侧偏离 `delta`，该侧单位惩罚为 `w_j^x`，忽略 setup 节省以及对其他任务的连锁影响时，外包比内部违约更便宜的近似条件为

\[
\delta>
\lambda p_j\frac{\max\{w_j^E,w_j^T\}}{w_j^x}.
\]

当违约发生在权重较大的一侧时，阈值简化为 `delta>lambda*p_j`。因此 `lambda=0.5/1/2` 可分别解释为：只有偏离约半个、一个或两个 processing time 后，单任务直接违约成本才超过外包报价；`lambda=0.5` 并不表示任务只要越窗就必然外包。若违约发生在权重较小的一侧，阈值更高。真实最优决策还会同时考虑移除任务后节省的 setup、对其他任务完成时间的影响和数量折扣，因此该关系只是价格档的标度解释，不是逐任务判定规则。

Dabia、Lai 和 Vigo（2019）的设置与此不同。其 tariff 直接作用于所有外包客户的总物理 demand，不为单个客户随机生成报价，也不把外包费率与客户时间窗、私人路线成本或惩罚权重用公式关联。作者按空间类别人为指定三套确定性费率：R 为 `5/4.5/4/3.5`，RC 为 `3.5/3/2.5/2`，C 为 `2/1.5/1/0.5`，统一使用绝对 demand 区间 `0--200/200--400/400--600/>=600`，理由仅是客户越随机分散，承运商报价越高，并未通过 pilot 把外包比例调到均衡。我们的 `p_j*max` 因而不是对该文 demand tariff 的直接复制，而是一种调度特定的“加工量乘服务重要性”报价。它可以保留，但论文必须明确假设 `w_j^E,w_j^T` 同时反映任务对外部服务等级的要求；如果这些权重只表示企业内部会计惩罚，则该报价机制的业务解释会变弱。

调度文献能够直接支持的是报价随外部加工量增加，而不是 `max` 这一具体权重组合。Qi（2009）的 *Two-stage production scheduling with an option of outsourcing from a remote supplier* 和 Lee 与 Choi（2011）的 *Two-stage production scheduling with an outsourcing option* 均采用按外包 processing time 计费的基本结构；Kim、Lee 和 Wang（2022）的 *Scheduling of two-machine flowshop with outsourcing lead-time* 也把这一类与外包加工时间成比例的模型作为直接相关基础。Mokhtari 和 Nakhai Kamal Abadi（2013）则说明调度模型也可以允许 job/subcontractor-dependent 的一般外包成本。本文据此以 `p_j` 作为外包报价的加工量基数，再乘 `max{w_j^E,w_j^T}` 表示任务要求较严格一侧的服务重要性。前半部分有既有文献依据，后半部分是本文为当前 TWET 场景规定的实例生成规则，论文不能把完整公式误写成直接照搬某篇文献。

当前正式口径就冻结为 `q_j=p_j*max{w_j^E,w_j^T}`、`b_j(lambda)=lambda*q_j` 和 `lambda=0.5/1/2`，不再把纯加工量报价或其他报价公式加入正式实验维度。论文中的准确表述应为“参考既有调度外包研究按加工时间计费的做法，以 processing time 为报价基数，并结合本问题的服务重要性构造任务报价”，而不是声称既有文献采用了同一个 `max` 公式。

现有研究中的“倍率”也没有统一标准。Lee 和 Choi（2011）使用 stage-specific 常数 `alpha_i`，目标中的外包项为 `alpha_i*sum p_ij`，主要按 `alpha_i` 相对单位调度时间成本的区间讨论复杂性，并非通过样本校准一个公认市场倍率。采用 job-dependent 成本的计算论文往往根本不保持与加工时间成比例：Lee 和 Sung（2008）的基准中 `p_j~U[1,10]`、`o_j~U[1,30]`，并进一步比较 `o_j~U[1,10]/U[10,20]/U[20,30]`；Kim、Lee 和 Wang（2022）的基准中 `p_j~U[1,30]`、`o_j~U[1,10]`，并比较 `o_j~U[10,30]/U[20,40]/U[30,50]`。Safarzadeh 和 Kianfar（2019）先独立生成 operation outsourcing cost，再为不同 problem class 通过预实验选择目标函数中的外包成本权重，使结果形成低、中、高外包率；其权重按规模变化，约在 `0.005--0.35`，不是统一倍率。因此文献真正提供的是“按加工量计费”或“通过价格/权重档覆盖不同外包率”两种实验范式，而不是一个可以直接照搬的数值。本文的 `lambda=0.5/1/2` 应依靠前述 break-even 含义解释，并在正式结果中检查是否确实产生低、中、高且非退化的外包水平，而不能声称这些数值来自某篇文献。

在正式 n50 样本上计算 `Q_all=sum_j q_j` 的中位数 `Q50_ref`，只校准一次绝对断点

\[
Q_1=0.25Q_{50}^{ref},\qquad Q_2=0.50Q_{50}^{ref}.
\]

断点随后跨规模、机器数、窗口和价格保持不变。价格档为 `lambda=0.5/1/2`，默认采用连续 incremental discount，三段边际费率为 `(1,0.85,0.70)`：

\[
G_\lambda(B)=\lambda\left[\min(B,Q_1)+0.85\min((B-Q_1)^+,Q_2-Q_1)+0.70(B-Q_2)^+\right].
\]

外包性能实验沿用实验一的全部基础环境，只删除时间尺度维度并固定为原始尺度，再用三个外包价格替换三个时间尺度。也就是说，6 个规模、每规模 5 个任务集合、3 个窗口、2 类 setup、3 个机器数、3 个价格和 2 种 BPC formulation 全部配对：

\[
6\times5\times3\times2\times3\times3\times2=3{,}240
\]

次正式求解。这个口径使外包与无外包在原始时间尺度下拥有完全相同的 `instance/m/W/setup` 场景，也能直接比较加入外包后问题变容易还是变困难。计算量较大，但不再通过删掉 W0、机器档或大规模价格档破坏配对。

外包场景同样先运行三随机种子的初始 ALNS，并让 `columns` 和 `masterVariables` 共享获胜方案。V2 快照同时保存内部机器序列和获胜 incumbent 的外包任务集合；恢复时按目标场景的 `q_j` 和 tariff 函数重新计算外包支付。`columns` formulation 据此加入对应外包集合 seed 列，`masterVariables` formulation 据此恢复同一个 incumbent 外包向量。恢复入口还会检查内部序列与外包集合是否恰好覆盖全部任务，避免列集合与 UB 不一致。

本节除通用 BPC 指标外，还报告内部 pricing 与外包 pricing 时间、内部列和外包列数量、RMP 迭代、Phase-I repair、分支类型、外包任务比例、物理外包 workload 比例、标准报价量比例、实际支付、折扣节省和成本分解。物理 workload 为 `sum_{j in O} p_j`，表示外包释放了多少内部加工时间；标准报价量为 `sum_{j in O} q_j`，是 tariff 函数的输入。两者不能混用：相同 processing time 的两个任务释放相同产能，但若服务权重不同，其标准报价贡献可以不同。主要判断 `columns` 与 `masterVariables` 的效率是否随规模和外包率变化，并观察允许外包后问题是因为内部任务减少而变容易，还是因额外变量、tariff 与分支结构变困难，或者在中等外包率附近最难。后续管理分析固定使用整体更稳定的一个 BPC formulation。

## 4. 实验三：允许外包的价值及价格、产能和窗口效应

本节原则上不新增算法方法，只复用实验一和实验二中同源、原时间尺度的解。对于同一个任务集合、m、W 和 setup，比较禁止外包与允许外包。低/中/高价格用于观察外包响应曲线，机器数表示内部产能背景，W 表示 due-window 压力，random/family 表示 setup 的结构形态。

分析分为四层。成本层报告 earliness、tardiness、setup、外包支付和总成本；服务层报告早到、窗内和迟到任务比例；决策层报告外包任务数、外包 workload/标准报价量、机器负荷和 tariff 分段；结构层比较外包前后的内部相邻弧、平均 setup、机器序列和拥挤时段。

本节重点不是重复“外包越便宜，外包越多”这一显然趋势，而是检验不同生产环境对价格变化的响应是否不同，并判断外包是否会移除 due-window 不协调、高 setup burden 或高 TWET burden 的任务，使剩余内部序列更连贯。任务特征只做探索性后处理，只有跨实例、价格和环境稳定出现的规律才写成一般性管理结论。

由于实验二与实验一的原尺度场景完整配对，本节可以在全部六个规模、三个机器档、三个窗口和两类 setup 上分析允许外包的价值及价格响应。不过管理结论仍应按规模分层，不能把同一任务集合在不同 m/W 下的重复观察当作独立样本。

## 5. 实验四：有无数量折扣

本节固定 n50、原时间尺度、中等价格 `lambda=1` 和实验二选出的 BPC formulation，比较无折扣 `(1,1,1)` 与默认折扣 `(1,0.85,0.70)`。断点均使用统一的 `Q1/Q2`，不按实例重新计算。

与最新“每格 5 个实例”的计算预算保持一致时，使用实验一和实验二中的同一批 5 个 n50 任务集合，完整交叉 m2/m3/m4、W0/W100/W300 和 random/family，共 90 个环境、180 个有/无折扣结果。实验二已经包含全部 90 个中等价格默认折扣结果，因此只需新增 90 个无折扣求解。

早期文档曾规定使用 20 个 n50 源实例，对应 360 个环境和 720 个有/无折扣结果。该版本与后来为控制总计算量采用的每格 5 个实例不一致。当前按 5 个执行，总体口径与两个性能实验一致；若以后增加到20个，应作为管理分析样本扩充，而不能反向改变已经预注册的算法性能矩阵。

本节报告外包集合 Jaccard、外包任务数和报价量、跨越 Q1/Q2 的实例数、折扣节省、内部成本和机器序列变化。需要区分两种机制：折扣是否只降低原本就会外包的任务支付，还是使一组具有相近 due-window 或共同 setup 结构的任务跨过断点并共同外包。

## 6. 正确性验证、运行方式和输出

小规模 compact/columns/masterVariables 对拍属于正式长跑前的正确性验证，不作为论文性能主表，当前也不生成 compact 批量任务。以后如恢复该验证，可在 n8/n10/n12 上检查三种模型的最优目标、调度、外包集合、成本分解和 validator 一致；其数量不进入性能实验和 90 次折扣增量。

每次正式求解固定 CPLEX 单线程、时限 10,800 秒、最大节点数 100,000。批处理由 scheduler 同时维持 4 个独立子 JVM；任一任务结束后立即启动下一个依赖已满足的任务。每个场景先生成并落盘共享 seed，然后算法任务读取同一个 seed。NG_DSSR 保留 time-indexed root preprocessing；三种 BPC 的其余参数由 `BestBpcProfiles` 固定，结果记录 profile version。

当前实际执行不是“每个 BPC 自己检查本地 seed，不存在就自动跑 ALNS”，而是 manifest 把每个场景拆成一个独立 seed 任务和若干依赖它的 solve 任务。通过 `ExperimentBatchScheduler` 运行时，seed 任务输出目录若已有 `SUCCESS` 标记，该任务被视为完成并跳过；否则启动 `FormalExperimentRunner --action=seed`，重新加载同一场景三次，使用三个确定随机流各运行一次最多 60 秒的 ALNS，按真实 incumbent 成本选择最好者并写入唯一 V2 快照。seed 子进程正常退出后，scheduler 写入 `SUCCESS`，随后才允许依赖该任务的 BPC 子进程启动。各 BPC 使用 `--action=solve` 直接读取同一快照，不再运行 ALNS；快照缺失或损坏时 solve 直接失败，不会自行补生成。

以一个具体场景为例，实际时序只有两段。第一段先把同一个落盘 `.dat` 读取三次，分别运行 repetition 0、1、2 的 ALNS；三次之间不传递列，也不合并列池，只比较最终可行 incumbent 的真实目标值，并把最好一次的初始 RMP 列、incumbent 内部机器序列和外包集合写成一个 `.seed` 文件。第二段才分别启动待比较的方法：纯调度时为 NG_DSSR、TIME_INDEXED 和 TIME_INDEXED_SRI，外包性能实验时为 columns 和 masterVariables。它们彼此独立，不是先运行 NG_DSSR 再把结果交给 time-indexed；每一种方法都从同一个 `.seed` 文件重新建立自己的求解过程，并写入各自结果目录。三次 ALNS 的时间单列为共享起点生成时间，不计入任何一种方法的 BPC 求解时间。

续跑时也按这个边界处理：若 seed 任务已有 `SUCCESS`，三次 ALNS 不再运行；某个算法任务已有自己的 `SUCCESS`，该算法也不再运行；只有尚未成功的算法任务会继续使用原来的共享 `.seed` 启动。因此断点续跑不会为不同算法重新生成不同初始解。

manifest 是批处理任务清单，不是算例数据，也不是求解器本身。TSV 固定包含六列：`runId` 是任务唯一编号；`mainClass` 是子 JVM 入口；`args` 保存该入口所需的完整参数，例如 `action`、实例路径、算法、共享 seed 路径、输出目录、时限和外包设置；`outputDir` 是该任务自己的日志与结果目录；`dependsOn` 是逗号分隔的前置任务 ID；`block` 只用于生成不同实验小节的子 manifest，scheduler 本身不依赖该列做求解决策。当前 seed 与全部 BPC 行统一使用 `HEU.FormalExperimentRunner`。compact CPLEX 暂不求解，因此既不生成 manifest 行，也不修改 `ArcFlowModel` 或 runner。manifest 不保存 ALNS 随机数、初始列内容、求解过程或最终结果，这些分别由 seed metadata、`.seed` 快照和各任务输出目录保存。

`action` 只是传给 `FormalExperimentRunner` 的普通命令行参数。`action=seed` 时，runner 只运行三次 ALNS、择优并写出共享 `.seed`，随后退出，不进入 BPC；`action=solve` 时，runner 读取实例和指定 `.seed`，按 `algorithm` 装配对应 profile，执行正式 BPC 并输出结果，不再运行 ALNS。scheduler 不理解这两个 action 的算法含义，只负责把 `args` 原样传给子 JVM。

整个实验包由普通 Java 类 `Common.FormalExperimentSuiteGenerator` 生成。它根据预先写入代码的实验因子循环构造场景和 `RunRow`，用确定命名得到实例、seed 和结果路径，再把每个 `RunRow` 转成一行 TSV 并通过 `Files.write()` 落盘；`manifests/`、`seeds/` 和 `runs/` 等目录通过 `Files.createDirectories()` 创建。根目录的 `manifest.tsv` 是一个文件，旁边的 `manifests/` 是存放分组任务清单的目录，二者不是父子表结构或数据库。所谓结构化只来自固定列定义、命名规则和依赖关系，不依赖额外工作流软件。

生成器为每个具体场景写一条 `action=seed` 行和若干条 `action=solve` 行。seed 行的 `dependsOn` 为空；同一场景的所有 solve 行都把该 seed 行的 `runId` 写入 `dependsOn`。文件中通常把 seed 行放在其 solve 行之前以便人工阅读，但真正保证顺序的是依赖关系，不是文本行号。scheduler 启动后一次性读取全部行，检查必需列、重复 `runId`、未知依赖和循环依赖，然后维护 pending、running、successful 和 failed 四类状态。最多同时启动 4 个独立子 JVM：没有依赖的 seed 任务可以直接运行；solve 任务只有在其 seed 成功或因已有 `SUCCESS` 被跳过后才会启动；seed 失败时相应 solve 被标记为 `BLOCKED`。scheduler 只负责组织任务，不读取算法目标值，也不会在算法之间传递结果。

每个实际启动的任务先在自己的输出目录写 `args.txt` 和 `status.txt`，再组装 `java -cp ... HEU.FormalExperimentRunner ...` 命令；子进程的标准输出和错误分别重定向到 `stdout.log` 和 `stderr.log`。子进程返回 0 时写入 `SUCCESS` 并标记 `SUCCEEDED`，非零时标记 `FAILED`。再次运行同一 manifest 时，已有 `SUCCESS` 的任务记为 `SKIPPED` 并被视为依赖成功，因此可以继续调度尚未完成的后继任务。某个任务结束后，scheduler 立即用空出的并发槽扫描并启动新的就绪任务，直至 manifest 中全部任务成功、跳过、失败或因依赖失败而阻塞。

这里必须区分“算法在给定时限内未证明最优”和“Java 子进程异常失败”。正常达到 10,800 秒时，BPC 返回 `TWETSolveStatus.TIME_LIMIT`，保留当时的 incumbent、best bound、gap、节点和组件统计；runner 正常退出 0，scheduler 因而写 `SUCCESS` 并继续下一任务。即使 root 尚未闭合，只要内部时限检查正常返回，也属于这种可报告的 timeout，而不是基础设施失败。只有未捕获异常、JVM/native 崩溃、启动失败或其他非零退出才标记 `FAILED`；普通 solve 任务彼此没有依赖，因此一个算法失败不会阻止同场景的其他算法或其他场景，只有 seed 任务失败才会使依赖该共享 seed 的 solve 任务全部 `BLOCKED`。失败任务目录仍保留 `args.txt`、`status.txt`、`stdout.log` 和 `stderr.log`，批次结束时 scheduler 汇总所有失败并返回异常。

当前 10,800 秒属于求解器内部的协作式时限，scheduler 本身没有硬 watchdog。PC、Tree 和主要 pricing engine 会接收同一个 `TimeLimitChecker`；普通 time-indexed 在时间层循环检查，rank-1 在标签和拼接循环分批检查，启发式 pricing 也在迭代间检查。但 ng-DSSR 当前只在 DSSR 轮次边界和 forward/backward/join 大阶段之间检查，完整扩展与 join 热循环内部没有周期检查；CPLEX/native 调用也不是由 scheduler 强制中止。因此单次 pricing 或 native 调用爆炸时，子进程可能明显超过 10,800 秒，scheduler 的 `process.waitFor()` 会一直占用一个并发槽。这是正式服务器长跑前仍需修复的超时可靠性缺口：内部热循环应低频轮询以便生成有效 `TIME_LIMIT` 结果，scheduler 还应增加总时限加宽限期的外部进程 watchdog，防止任何失控子进程永久占槽。

外部 watchdog 不能替代内部正常收尾。强制终止只能保留此前已经刷入磁盘的 `live.log/stdout.log/stderr.log/status.txt`，当前仍在内存中的最新 incumbent、bound 和最终 CSV/Markdown 汇总可能丢失，日志末尾也可能因缓冲尚未刷出而不完整。合理顺序应是：内部求解时限仍为 10,800 秒；先补齐 ng-DSSR 热循环检查并给每次 CPLEX 调用传入剩余时间，使绝大多数任务自行返回 `TIME_LIMIT`；再由 scheduler 使用明显更晚的 watchdog 截止时间，只处理代码失控或 native 调用不返回的异常情形。若最终加入强杀，还应周期性原子写出最近一次可靠 incumbent、bound、gap、node 和 elapsed checkpoint，并把这类任务单独标记为 `HARD_TIMEOUT`，不能伪装成具有完整最终结果的普通 timeout。

当前决定暂不实现上述内部热循环检查和 scheduler watchdog。现阶段基于 DSSR 逐轮收紧的流程，先假设一次完整 labeling/join 能在可接受时间内返回，允许单轮跨过 10,800 秒检查点后才正常结束；该假设不是复杂度保证，DSSR 仍可能在困难节点产生标签或 join 长尾。正式批次开始前先用小批量运行记录配置时限、实际退出时间和最后阶段；只要发现显著超时长尾或进程长期不退出，再恢复本节方案。当前不因尚未观察到长尾而增加强杀逻辑，避免丢失最终结果。

scheduler 自身对单个算例的直接计算开销可以忽略：等待子进程的线程阻塞在操作系统事件上，不进行忙轮询，状态文件和日志重定向也不进入 pricing 热循环。真正可能改变墙钟时间的是并发资源竞争，而不是 `waitFor()`：四个单线程求解进程会竞争物理核心、内存容量、内存带宽、磁盘和散热。如果服务器至少有四个可用物理核心且内存充足，影响通常有限；若内存紧张或 pricing 具有很强的带宽压力，同一算例会比独占机器运行更慢。正式比较必须固定并发度和机器环境，不能把四进程结果与单进程历史时间直接当作严格 A/B。

正式实验包只需要三个 solve manifest，而不是为论文四个小节各建一个求解入口：`manifests/pricing-comparison.tsv` 对应实验一；`manifests/outsourcing-performance.tsv` 对应实验二，同时包含 `columns/masterVariables` 与低中高三个价格档，原先分开的 formulation 和 price block 合并；`manifests/outsourcing-discount.tsv` 对应实验四，只补实验二尚未覆盖的无折扣任务。实验三不生成 solve 行，也没有独立 manifest，它只配对读取实验一的无外包结果和实验二的外包结果，生成管理分析表图。根目录 `manifest.tsv` 是上述三个 solve manifest 的去重并集，每个共享 seed 任务只保留一次。

三个 solve manifest 不是三套求解代码。NG_DSSR、TIME_INDEXED、TIME_INDEXED_SRI 以及外包的 `columns/masterVariables` 当前都使用 `HEU.FormalExperimentRunner --action=solve`，只改变实例文件、算法 profile、外包 formulation、价格或折扣、共享 seed 路径和输出目录，最终统一调用现有 `TWETBPCSolver`。compact CPLEX 当前不生成任务，也没有改动 `ArcFlowModel`。当前尚未实现实验三所需的跨算例结果聚合程序；它只读取既有 CSV/metadata 做后处理，不重新调用求解器。

实验包并不要求一次把全部实验跑完。运行根目录 `manifest.tsv` 时，scheduler 会持续处理全部求解任务；运行任一子 manifest 时，只处理对应小节及其必需 seed。不同子 manifest 复用相同 seed 输出目录和 `SUCCESS`，所以分阶段执行不会重复已经成功的共享初始解。正式服务器适合先运行纯调度比较并验收，再运行外包性能，最后补数量折扣；任一阶段中断后重新运行同一 manifest 即可续跑。

这里还有两个不同含义的 seed。随机 seed 是 ALNS 的随机流起点，当前不是手工列出三组固定整数，而是用场景 `runId` 确定性生成。设固定初值 `offset=0xcbf29ce484222325`、乘数 `prime=0x100000001b3`，先计算 `value=offset XOR salt`，再按 `runId` 字符顺序反复执行 `value=(value XOR char)*prime`，最后返回

```
value XOR (0x9e3779b97f4a7c15 * (repetition + 1))
```

其中 repetition 固定为 `0/1/2`。每次 repetition 同时建立两条独立随机流：`EngineALNS` 使用 salt `0x41c64e6d`，`Utility` 使用 salt `0x9e3779b9`。因此同一个场景重跑时三组数值完全一致，不同场景因 `runId` 不同而得到不同数值，ALNS 主搜索和其他随机操作也不会共用同一个 `Random` 状态。实际数值及三次目标、时间和获胜编号均写入 seed metadata。最终共享的 `FixedInitialColumnSeed` 则不是随机数，而是三次 ALNS 中成本最好者的初始列、incumbent 机器序列和外包集合快照。三次列池不合并，三种 BPC 只共享这个获胜快照。

这里的 salt 只是生成随机种子时加入的固定“流标签”。如果只对 `runId+repetition` 做一次哈希，再把同一个结果同时交给 `EngineALNS` 和 `Utility`，两条随机流会从完全相同的内部状态开始，产生人为相关的随机序列。两个不同 salt 使相同场景和 repetition 映射为两个不同的 long seed，从而把 ALNS 主搜索与其他随机操作分开。salt 的十六进制常数本身没有业务含义，也不是经过算例调优得到的参数；换成其他固定且不同的非零常数同样可行。当前值一旦用于正式实验就必须保持不变，因为修改 salt 会改变全部 ALNS 轨迹和共享初始列。该哈希只服务于可复现和随机流分离，不用于密码学安全，也不表示某组种子比另一组更优。

恢复已有实验时，scheduler 当前检查的是 seed 任务目录中的 `SUCCESS`，而不是同时验证 `.seed` 文件。正常成套保存时两者一致；如果人工删除或损坏 seed 文件但保留 `SUCCESS`，scheduler 会跳过 seed 生成，后续 solve 才在读取快照时失败。反过来，若 `.seed` 文件存在但 `SUCCESS` 缺失，seed 任务会重新执行三次 ALNS并覆盖快照。因此正式迁移和续跑必须把 seed 文件、对应 seed 任务输出目录及其 `SUCCESS/run.properties` 一起保留；后续可以再把“跳过前同时校验快照存在及 fingerprint”作为调度器稳健性修复，但这不影响当前全新正式批次的正常流程。

求解过程写入 `live.log`、`stdout.log`、`stderr.log` 和 `status.txt`。结束后输出 `core-summary.csv`、`components.csv`、`nodes.csv`、`columns.csv`、`pool-summary.csv`、`pool-columns.csv`、`cuts.csv`、`outsourcing.csv`、`incumbent-schedule.csv`、配置快照和 Markdown 摘要。实时日志只在阶段边界更新，高频 label/arc/PWLF 诊断默认关闭；完整列池和调度表在求解结束后生成，不进入算法热路径。

## 7. 当前实现状态

旧的 `experiment-suite/formal-v1` 基础设施 pilot 已于 2026-08-23 删除。它原先包含 243 个 seed 和 945 个 solve，但仍使用旧规模、旧窗口、旧时间尺度及占位外包参数，容易被误当成论文正式输入，因此不再保留。正式包现已生成到 `experiment-suite/formal`，其中只包含 `pricing-comparison`、`outsourcing-performance` 和 `outsourcing-discount` 三份 solve manifest；formulation 与 price 已合并，实验三和 compact 均不生成求解任务。

当前基础设施已完成时间尺度落盘能力，runner 不再接受 `timeScale`。`FormalExperimentRunner` 的 seed action 已固定运行 3 次最多 60 秒的独立 ALNS，每次重新加载 Data 并分别重置 ALNS 与 Utility 随机流，按真实 incumbent 成本择优且只保存获胜 run。`FixedInitialColumnSeed` 已升级为 V2，除内部列外保存 incumbent 的外包任务集合；恢复时按目标场景重新计算 baseline 与 tariff，V1 无外包快照仍可读取。外包正式报价已改为 `q_j=p_j*max(wE_j,wT_j)`；生成器从 n=50 不重复任务集合计算固定 `Q1/Q2` 并传给 seed/solve，默认折扣为三段边际费率 `1/0.85/0.70`。求解结束后的 `outsourcing.csv` 已同时输出外包任务比例、物理 processing workload、标准报价量比例、实际外包支付及逐任务的 processing、窗口和权重信息。

正式数据生成已拆分为任务抽样、due-window中心、setup生成、setup独立审计、时间尺度和文件写入组件，并由总装器生成 `n=20/40/50/60/80/100`、每规模5个集合、random/family及实例级 `[5,15]/[15,25]` 倍率的落盘数据。当前正式包包含30个任务集合和540个 `.dat`；三份 manifest 共含3330个共享seed任务和8190个正式求解任务。生成后独立程序重新读取全部 `.dat`，检查setup均值、上限、三角性、二次Floyd改写和跨机器副本一致性。当前540个文件全部通过；采用 `D+Delta*delta` family setup 后，全尺度最大绝对均值误差为0.284810127，对应最大相对误差仅0.062086%，三角违例、二次Floyd改弧、上限超出和family内外关系反转均为0。任务字段、源数据映射、时间倍率、random/family配对、跨机器副本和manifest参数的第二套全量检查也为0个问题，详见 `docs/input/2026-08-23-正式实验数据生成与setup审计.md` 第8节。

## 8. 后续待讨论的两个研究议题

### 8.1 VRP 分支定价中更前沿的方法

后续需要单独核查近年来 VRP 精确算法中比传统列生成、ng-route/DSSR 和常规弧分支更前沿的技术。当前用户特别提到一个暂称为 “deluxing” 的方法以及 cluster 类分支，但这两个名称尚未核对原始文献，现阶段只作为检索线索记录，不能直接写入论文或认定与当前实现对应。

后续讨论应先把候选方法按作用位置分开：定价松弛与状态空间控制、分支规则、列管理与枚举、对偶稳定和 cut/定价协同。对每种方法需要确认准确名称、代表论文、适用的 VRP 结构、核心数学机制、公开计算证据，以及移植到本文并行机 due-window 定价后是否仍保持正确性和可能的计算收益。最终目标不是为了堆叠算法组件，而是判断是否存在一个具有明确理论依据、能够补足当前 BPC 瓶颈且实现成本合理的方法。该项暂不改变当前主线和正式实验配置。

### 8.2 ng-DSSR 与 time-indexed 的适用范围

正式算法比较希望回答的核心问题不是哪一种方法在所有算例上更快，而是哪些实例特征决定了 time-indexed 更合适，哪些情况下 ng-DSSR 更合适，并由此说明两种定价方法各自的意义。当前可检验的机制假设是：较小整数时间域、较紧窗口、较强图 fixing 以及 rank-1/SRI 能显著缩树时，time-indexed 可能更有优势；时间域很大、时间单位被细化或同比放大、窗口较宽且时空图随 horizon 快速增长时，ng-DSSR 可能表现出更好的数值尺度稳定性。这些只是待验证假设，不能在正式结果出来前写成结论。

后续分析需要使用相同数据、共享初始列和固定 profile，将总时间和最终 gap 与机制指标配对。time-indexed 侧重点包括 horizon、初始及 fixing 后的有效节点/弧、每轮 pricing 扫描量、root bound 和 cut 收益；ng-DSSR 侧重点包括 label 数、正反向扩展、completion bound、DSSR 轮数、join、启发式与 exact pricing 时间。任务规模、机器数、due-window 宽度、setup 结构和时间尺度均作为解释因素，但不能只挑对某一算法有利的场景。理想结果是形成一张可解释的适用区域或方法选择图，而不只是汇报平均求解时间。NG_DSSR 正式 profile 包含 time-indexed root preprocessing，解释这种适用范围时必须把该预处理时间和缩域收益同时计入。
