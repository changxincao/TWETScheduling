# 第二机新增 n50 F=3 NG-DSSR 接续

2026-09-14：第二机 `100.68.243.192` 原批次 `D:\ccx\work1\experiments\20260913-ng-fixing-cutset-le50` 仍为 `SOLVE_RUNNING`，原 `solve.tsv` 已由调度器在启动时读入。按用户要求保留原 F=4 的 n50 任务及结果，不停旧进程、不改旧清单。在 `D:\ccx\work1\experiments\20260914-balanced-family-ng-n50` 新建独立 F=3 批次。数据取自已经部署的 `D:\ccx\work1\instances\family-count-balanced-20260914`，只增加 n50 family 的 135 份输入，每份共享一个新 seed，分别运行 F-A/B/C/D 四种 NG-DSSR 配置，共 540 个求解项；每项限时 10800 秒、100000 节点，最多 6 个独立 JVM。四组的分支、CutSet、fixing 参数逐项与旧 n50 family 清单一致，仅输入、seed、runId、输出目录改为 F=3 独立版本，求解 jar 仍为 `20260913-ng-cutset-supernode-v8`。生成脚本为 `scripts/remote/prepare-balanced-family-ng-n50-20260914.ps1`。

远端 `instances.tsv` SHA-256 为 `02EAA7DC0AE6DE83D8059C57FAB4B064DC42CE2ED0BCD548F1BE034C61E3AED2`；135 份将读取的输入逐文件 SHA-256 与本地清单一致。新 `seed.tsv/solve.tsv` 分别有 135/540 项，四组各 135 项，m2/m3/m4 各 180 个求解项；参数及元数据非空，旧新四组的非路径求解开关逐项一致。远端 `run.cmd ... --scan-only` 均通过，当前新任务 `localSuccess=0`，没有进行 smoke 或正式求解。旧批次仍处于 `SOLVE_RUNNING`，原 F-D 崩溃单项重试等待器处于 `WAITING_FOR_BATCH`。

新批次的 `wait-balanced-family-ng-n50-20260914.ps1` 已在第二机启动并跨 SSH 连接核实存活，状态为 `WAITING_FOR_OLD_AND_RETRY`，PID 30440，`worker.lock` 尚不存在。它先等待旧调度器写入结束状态和 `worker.exit-code`，再等原 F-D 单项重试写入 `SUCCEEDED` 或 `FAILED`，两者结束后延迟 30 秒才调用新 `worker.cmd`。新 worker 先生成 135 份 seed，成功后才以 6 并发求解 540 项；旧输入、旧结果和旧调度器均不受影响。后续需核查 `continuation.status`、`experiment.status`、各项 `SUCCESS` 及失败项；当前不能称新批次已经开始求解或完成。

2026-09-15：按用户要求将执行顺序改为新 F=3 批次优先。切换前由 `jcmd -l` 确认旧批次恰好一个调度器和六个求解器，六个在途目录均无 `SUCCESS`；停止这些明确 PID 和旧 F=3 等待器后，将六个半成品完整移入旧批次 `interrupted-for-balanced-f3-priority-20260915-1555`。旧批次已有成功结果、manifest、seed 和日志未删除，状态改为 `PAUSED_FOR_BALANCED_F3`。原 F-D 单项重试等待器没有停止，仍保持 `WAITING_FOR_BATCH` 和心跳。

新增 `scripts/remote/resume-old-after-balanced-family-ng-n50-20260915.ps1`。它等待新批次进入 `FINISHED` 或 `FAILED`，延迟 30 秒释放六个进程槽，再调用旧批次原 `run.cmd solve.tsv 6`；旧 `SUCCESS` 自动跳过，中断项从已清理的正式输出路径重算，结束后写回旧 `worker.exit-code`，供原 F-D 重试等待器接续。新旧结果继续使用独立输入、seed、runId 和输出目录，新 runId 均带 `f3`。

第一次优先启动命令把 `if not exist scheduler-logs mkdir ... && call worker.cmd` 写在同一条件链中；因目录已存在，`worker.cmd` 被跳过，等待器又在 `experiment.status` 尚不存在时退出。该次没有启动任何 Java 任务，错误日志已保留为 `resume-old-wrapper.first-failed.*`。修正等待器为状态文件不存在时继续等待，并直接调用 `worker.cmd` 后，跨 SSH 复核为新批次 `SEED_RUNNING`、一个调度器和六个 seed 子进程，旧批次 Java 为零；`resume-old.status=WAITING_FOR_BALANCED_F3`，原 F-D 等待器心跳正常，三个新错误日志均为空，复核时已有四个 seed 成功。当前只确认优先级切换和自动接续已正确启动，不代表 540 项求解完成。
