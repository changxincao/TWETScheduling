# 平衡 family 数算例的两机 TI 切换（2026-09-14）

本次只切换两台正在执行 60 任务以下纯调度 TI/TI+SRI 实验的电脑。新输入来自 `experiment-suite/family-count-balanced-20260914/instances`，仅 n50/n60 的 family 数由原来的 4 改为 3；n40 输入没有变化。用户确认立即中断两台旧调度器，因此第一台当时正在运行的旧 n50 family、第三台当时正在运行的旧 n40 family 子任务均未等到自然结束。旧实验目录和已有 `SUCCESS` 保留，未修改旧 `solve.tsv`、旧输入或旧结果；中断时未产生成功标记的输出不能当作已完成结果。未变更数据的任务由下述续跑清单自动补齐。

第一台旧批次为 `D:\ccx_work\考虑交付的机器调度\work1\experiments\20260905-formal-scheduling-le60`，已核对 PID 45832 为读取 `ti-only-20260913.tsv` 的调度器且其 6 个子进程均属于该目录。`taskkill` 被拒绝后，用 `tskill 45832` 停止；复查该批次 Java 进程数为 0。第三台旧批次为 `D:\ccxWork\work1\experiments\20260911-cluster-le60`，已核对 PID 8408 为读取 `ti-only-20260913.tsv` 的调度器且 6 个子进程均属于该目录。该机 `taskkill` 被拒绝、`tskill` 不可用，改用 `Stop-Process -Id 8408 -Force`；复查该批次 Java 进程数为 0。调度器启动时一次性读取清单，修改在用 TSV 无法改变未来派发，因此没有覆盖旧清单或原位替换数据。

新批次两机均命名 `20260914-balanced-family-ti-le60`，分别位于上述各自 `work1\experiments` 下，具有独立的 `seed.tsv`、`solve.tsv`、`seeds/`、`runs/` 和日志。清单由 `scripts/remote/prepare-balanced-family-ti-le60-20260914.ps1` 根据已验收的 `instances.tsv` 生成：每台 270 个 n50/n60 family 输入，270 个新 seed，随后 540 个 TI/TI+SRI 求解项。`runId` 含 `f3`，不与旧 family 数的结果混用；优先处理 m4 的 `F<m` 实例。每个求解项仍限 10800 秒、100000 节点，最多 6 个独立 JVM，每个求解器 1 线程。第一台沿用部署 `20260905-93685d4a` 的 TI/TI+SRI 与 Arc 分支；第三台沿用部署 `20260911-15570109-cluster-v6` 的 TI/TI+SRI、Cluster 分支和原 `theta=0.5` 参数。没有部署新求解器代码，也没有加入 NG-DSSR。

启动前，本地逐项确认两套清单均为 270/540、`runId` 无重复、参数列非空；远端各自的调度器 `--scan-only` 均识别 270 seed 和 540 solve，TI 与 TI+SRI 各 270。270 份将被读取的新输入在两台逐文件 SHA-256 与本地一致。两台以原先验证过的后台 SSH 方式启动 `worker.cmd`，先生成 seed，全部成功后自动进入求解。断开启动 SSH 后再次连接，两台均保持 `SEED_RUNNING`、恰好 1 个新批次调度器和 6 个 seed 子进程，错误日志为空；已观察到第三台至少 6 个、第一台至少 12 个 seed 的 `SUCCESS`。当前只证明接续已启动，不能把新批次写成求解完成；后续应核对 270 个 seed 全部成功、自动转入 `SOLVE_RUNNING`，并按每项 `SUCCESS` 和求解状态汇总。

原切换只安排了新 n50/n60 family，遗漏了原批次其余任务的继续执行。现已从两台原始 `ti-only-20260913.tsv` 各提取 1620 项未变更数据任务：n20/n40 的 family 与 random、n50/n60 的 random；仅剔除旧 F=4 的 n50/n60 family 共 540 项。新 F=3 批次的 540 项与续跑的 1620 项合计仍为每台 2160 项。续跑沿用原任务的 `runId`、输入、seed、输出目录和参数，故原结果目录的 `SUCCESS` 会自动跳过，中断但未成功的任务会补跑；没有覆盖旧结果或重新计算已成功的同数据任务。

续跑清单由 `scripts/remote/prepare-balanced-family-unchanged-resume-20260914.ps1` 从两台原始 TSV 原样过滤生成，保存在各自新批次目录的 `unchanged-resume.tsv`。上传后逐文件 SHA-256 与本地一致。远端 `--scan-only` 均识别 1620 项，其中 TI/TI+SRI 各 810 项；第一台当时已有 1242 项 `SUCCESS`、待续 378 项，第三台已有 426 项 `SUCCESS`、待续 1194 项。两台均已启动 `resume-after-new.cmd`，在新批次结束（`FINISHED` 或 `FAILED`）后自动执行原数据续跑，最多 6 个独立 JVM；断开本地启动连接后均显示 `resume.status=WAITING`、`experiment.status=SEED_RUNNING`、错误日志为空。新批次若失败，续跑仍会进行，但失败的新 family 项必须单独处理。当前只能确认接续已配置和等待，不能把 2160 项写成完成。
