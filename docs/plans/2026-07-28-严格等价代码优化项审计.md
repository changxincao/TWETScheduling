# 严格等价代码优化项审计

## 1. 审计边界

本次只检查附件中九类优化建议与当前代码的对应关系，不修改算法代码，也不启动求解。严格等价的含义仍是：pricing engine 调用顺序、DSSR 的 Tmid/轮次/top-C/K/更新顺序、返回列及排序、restricted column ID 集合及顺序、strong branching 评分和分支树、certificate/fixing/writeback 均不改变。

需要补充一个边界：ALNS 使用墙钟时间终止时，即使只增加诊断计时，也可能轻微改变最后执行的迭代数。因此 VND operator 计时必须由诊断开关控制；若要逐项比较列和分支树，应使用非绑定总时限或固定迭代数的诊断入口。

## 2. 当前结论

### 2.1 应优先实施

1. **拆分 LP 建模、求解和解读取时间。** 当前 `LP.solveRelaxation()` 把 `buildModel()`、`cplex.solve()` 和 dual/value 读取记在同一个 master LP 时间中；`Tree` 中名为 `*_rmp_build` 的统计只覆盖 `LP.construct()`，没有覆盖 CPLEX 矩阵构造。因此现有日志无法判断 strong trial 慢在 Java/Concert 建模还是 simplex。应先按 variables/objective、coverage、machine/branch、cut、repair、solve、extract 分段计时。该修改只增加粗粒度计时，不改变调用顺序。

2. **复用普通 time-indexed 的大数组 scratch。** `TimeIndexedGraphSolver` 每次 pricing 都重新分配 `dist`、`predState` 和 `predAddedJob`，在大 horizon 下单次可产生十几 MB 的短命数组。engine-local buffer 按当前 `stateCount` 完整重置即可保持严格等价，同时明显降低分配和 GC；不应复用任何旧 dual 下的数值。

3. **引入最小版不可变列结构共享。** 当前 temporary `TWETColumn` 进入 `Pool` 后，sequence、signature、jobs 和 jobVisitCounts 会再次构造；同 sequence 成本改善时也重新构造全部结构。建议先引入只包含这四项的 `ColumnPattern`，temporary candidate、Pool column 和成本改善 header 共享它，并缓存一次只读 sequence view。不要第一步就把全部 arc posting、adjacency 和 cut 系数塞入该对象。

4. **若继续重点优化 time-indexed SRI，则实现 packed residual。** 当前每个 rank-1 label 扩展都会 clone `byte[activeCuts]`，并逐 cut 扫描。现有 residual 严格为 0/1，适合用 `long`/`long[]` 表示。该修改能降低状态复制和逐 cut 判断，但不能消除 horizon 与 residual 组合造成的 label 膨胀。实现时必须分别保持 forward、backward、join、dominance 的现有更新顺序，并按 active cut index 递增累加 dual，避免改变浮点求和顺序。

### 2.2 先完成 LP 计时，再决定范围

5. **LP 系数缓存有价值，但不能一次做成全局 posting 系统。** coverage 已使用 O(1) 的 jobVisitCounts，主要问题只是 `n × restricted columns` 扫描；arc/adjacency 行仍会反复扫描 sequence；SRI 行还会为每个 `(cut,column)` 重建 scope/memory membership 并扫描 sequence。建议顺序为：
   - 先把 cut 编译成可复用的 scope/memory 表，去掉每次系数计算的 boolean 数组分配；
   - 在 `ColumnPattern` 中按紧凑 primitive 数组缓存 directed arc multiplicity/adjacency；
   - 只有 LP 分阶段统计仍证明行构造占主导时，再增加稀疏 `(cutId,columnId)` 系数缓存。

   暂不建立全局 `arcKey -> columnIds` posting。它会增加列池常驻内存、restricted 交集和顺序恢复逻辑，当前没有证据表明优于按 restricted column 顺序读取列内缓存。cut ID 当前不可变，因此 `(cutId,columnId)` 缓存语义可行，但必须稀疏或有界。

6. **有界堆只替换“大 M、小 K”的位置。** `LP.resetRestrictedColumnsByCurrentReducedCost()`、外包对应入口和 time-indexed root seed 选择可以从全排序改为 bounded heap，并在最后按原 comparator 排序。Arc strong candidate 最多约 O(n^2)，而且 `Tree` 当前需要完整 candidateCount；phase1/phase2 数量更小，不值得为它们增加统一堆框架。root seed 还要先排除已经是 root seed 的同 signature 列，否则只保留 K 个候选会改变“跳过重复后继续补足 K 个”的语义。

### 2.3 可做但优先级低

7. **time-indexed candidate 扫描只做小改。** dual-window evaluator 失败前不必先构造 signature；重复 job 可在回溯时用 epoch 数组同步判断；最终可直接复用 `ColumnPattern`。不建议给每个时空 state 增加 `depth` 数组，只为精确预分配少量返回 sequence 的容量会额外增加 O(nH) 内存，收益不抵成本。

8. **ng-DSSR repeated-segment scratch 暂不改。** 当前每条 witness 分配两个 `n+1` 整数数组；一轮可能扫描到 top-C 上限，但找到 K 个有效更新后即停止，且 `n` 通常为 40--100。其中 `middleMarks` 已用 mark 避免清零，`lastPosition` 的短数组填充很便宜。只有后续 allocation profile 证明该处显著时，才值得改为 solver-local epoch scratch。

### 2.4 当前不建议实施

9. **不缓存 Node 的 time-indexed arc lookup。** 当前 lookup 只在存在时空禁弧时构造一个 O(n^2) 的 `BitSet[]` 只读视图，已经避免了热循环中的 HashMap 查询。跨 pricing 缓存需要覆盖 forbid/replace/merge/copy/writeback 的全部失效路径，复杂度高于当前可见收益。

10. **不把 static time-indexed tables 提升到全局 Context。** 普通 no-cut 与 rank-1 主线本来互斥；rank-1 已与其 no-cut delegate 共享同一 `StaticPricingData`。pre-heuristic 当前默认关闭，root preprocessor 又是短生命周期独立 context。继续全局化主要会延长大 penalty table 的存活时间，实际重复构造很有限。

## 3. 当前代码中已经完成的相关优化

ng-DSSR top-C witness 池已经是 `SequenceSignature` 哈希表加有界 `TreeSet`，不存在附件之外所说的线性 sequence 去重和每次全排序；no-SRI 主线也已经在分配 SRI 容器前返回。初始列构造已经用本次 build 的 `evaluatedSequenceIds` 避免同一历史 sequence 重复 evaluator。这三项不应重复修改。

## 4. 推荐实施顺序

1. LP/VND 诊断计时，其中 VND 统计默认关闭。
2. time-indexed primitive scratch 复用。
3. 最小版 `ColumnPattern`，先只消除 candidate 到 Pool 的重复结构构造。
4. 根据 LP 计时结果，优先做 compiled cut coefficient 和列内 arc/adjacency 缓存；再判断是否需要稀疏 cut-column cache。
5. 仅在保留 time-indexed SRI 为核心实验方法时，实现 packed residual。
6. 最后考虑目标明确的 bounded heap 和 candidate 小优化。

每项应独立提交。除 wall-clock 诊断外，结构等价测试至少比较：每轮 pricing 返回 signature/成本/顺序、restricted IDs 及顺序、active cuts、strong candidate/side score、节点创建和关闭顺序、最终 objective/bound。

## 5. 各项优化的原流程、目标流程和收益来源

### 5.1 LP 分阶段计时

原流程中，外层只记录一次 `LP.solveRelaxation()` 总时间，而该方法内部依次重建 CPLEX model、调用 `cplex.solve()`、读取 dual 和变量值。另一方面，strong trial 日志中的 `*_rmp_build` 只覆盖 Java 侧的 `LP.construct()`，它主要设置 node、restricted IDs 和 active cuts，并未构造 CPLEX 矩阵。因此当前日志中的 master LP 时间无法区分“逐行添加 Concert term 很慢”和“CPLEX simplex 很慢”。

目标流程是在现有调用位置周围增加粗粒度计时，分别记录 variables/objective、coverage、machine/branch、cut、repair、`cplex.solve()` 和 solution extraction。该项本身不加速，只负责决定后续优化方向：若 build 占主导，列结构和系数缓存有意义；若 solve 占主导，在保持模型、basis 和算法流程不变的条件下，Java 缓存不会解决主要时间。

### 5.2 time-indexed primitive scratch

原流程每次创建 `TimeIndexedGraphSolver` 时，都按 `stateCount=(n+1)(H+1)` 新建一个 `double[] dist` 和两个 `int[] predecessor` 数组。一次 pricing 完成后 solver 被丢弃，下一次 pricing 又分配相同数量级的数组。以 `n=60`、`H=20000` 为例，这三组数组的裸数据约为 19.5 MB，数百次 pricing 会形成大量短命内存和 GC 压力；而 DP 本来就必须重新覆盖这些状态。

目标流程由 pricing engine 持有可扩容 scratch。新一轮若容量足够就复用，否则扩容；每次仍对当前有效前缀完整重置，绝不读取上一轮 dual 下的距离和 predecessor。它省掉的是重复分配和回收，不省略 DP 扫描，也不改变状态顺序。

### 5.3 最小版 ColumnPattern

原流程中，pricing 先构造一个 `id=-1` 的 temporary `TWETColumn`。构造过程复制 sequence、构造 `SequenceSignature`、构造 jobs bitset 并统计 jobVisitCounts。`LP.addOrImproveColumn()` 随后只把 sequence 等字段传给 Pool；Pool 先重新构造 signature 做查询，新增列时又构造一个正式 `TWETColumn`，把上述结构再做一遍。同 sequence 获得更低成本时，Pool 也会重建全部结构，尽管序列本身没有变化。

目标流程把不随成本变化的 sequence、signature、jobs 和 jobVisitCounts 放进不可变 `ColumnPattern`；列对象只保存 id、cost、source、seed 等 header。temporary candidate 进入 Pool 后复用同一 pattern，成本改善只创建新 header。收益来自每条入池列少一次 sequence 复制、signature 构造、bitset 构造和计数扫描，并减少 GC。第一步不加入 arc、adjacency 和 cut cache，避免把一个局部结构共享修改扩大为全局索引重构。

### 5.4 packed rank-1 residual

原流程为每个 rank-1 label 保存一个长度等于 active-cut 数量的 `byte[] residual`。每次扩展先 clone 整个数组，再逐 cut 判断 memory、scope 和 residual；join 与 dominance 也逐 cut 扫描。当前 residual 只可能是 0 或 1，所以一个 cut 实际只需要一个 bit。

目标流程在 active cuts 不超过 64 时使用一个 `long`，更多时使用 `long[]`；按 job 和 arc 预计算 retain/toggle masks。扩展时先用位运算更新 residual，只对发生 carry 或 dominance correction 的 bit 按 active-cut 顺序累加 dual。这样可把一次 label state 复制从几十或上百字节降到 8 字节或少量 `long`，并减少无关 cut 的分支判断。它只能降低每个 label 的处理成本，不能减少 horizon、cut-state 组合或 label 总数，因此不能单独解决 SRI 图膨胀。

### 5.5 LP 系数缓存

原流程每次重建 RMP/strong trial model 时都重新生成矩阵。coverage 已经使用 jobVisitCounts，但仍按 job 扫描所有 restricted columns；每条 active branch arc 会再次扫描所有列的 sequence；每个 SRI `(cut,column)` 不仅扫描 sequence，还会重新构造 scope/memory membership 数组。strong branching 反复使用大量相同列和 inherited cuts，因此这些工作会重复。

目标流程分三步：先把每个 immutable cut 编译成可复用 membership；再让 ColumnPattern 保存紧凑 arc/adjacency 数据；只有计时仍证明 cut row 构造很重时，才增加有界或稀疏的 `(cutId,columnId)` coefficient cache。构造每一行时仍按 restricted column 顺序添加 term，保证 CPLEX 输入顺序不变。全局 arc posting 暂不采用，因为它需要额外做 restricted-set 交集、顺序恢复和大规模常驻索引。

### 5.6 bounded top-K

原流程在若干入口先收集全部 M 个候选并完整排序，再只使用前 K 个，复杂度为 `O(M log M)`。目标流程扫描时维护容量 K 的 worst-first heap，扫描结束后只对 K 个幸存者按原 comparator 排序，复杂度变为 `O(M log K)`。

该修改只适合 root seed 或 restricted-column reset 这种 M 很大、K 很小的入口。strong branching 的 arc candidate 通常只有 `O(n^2)`，而且 Tree 仍需要完整 candidateCount；phase1/phase2 列表更小，因此替换收益很低。root seed 还必须先正确处理已存在 seed 的 signature，否则简单的 K 容量堆会改变“跳过重复后继续补足 K 条”的结果。

### 5.7 其余小项

time-indexed candidate 可把 signature 构造移动到 dual-window evaluator 通过之后，并在 sequence 回溯时顺便判断重复 job；这些只减少少量最终候选扫描。不能为了精确 sequence 容量给所有时空 state 增加 depth，因为它会新增一个 `O(nH)` 整数数组。

Node arc lookup 当前只是每轮构造一个 `O(n^2)` 的引用数组，已经避免热循环 HashMap；跨轮缓存需要管理所有 fixing/writeback 失效路径。static pricing data 在 rank-1 与 no-cut delegate 之间已经共享。ng repeated-segment 的数组只有 `n+1`，优先级均明显低于上述项目。

## 6. 逐项实施与测试

### 6.1 P0：RMP 分阶段计时

2026-07-28 首先只实现 RMP 诊断计时，没有修改建模顺序、CPLEX 参数、pricing、repair 或 strong branching 流程。诊断由系统属性 `twet.bpc.masterLpPhaseTiming=true` 单独开启；关闭时不执行各阶段的 `nanoTime()` 和字符串格式化。记录项为 model initialization、variables/objective、coverage/machine rows、branch rows、cut rows、repair rows、`cplex.solve()` 和 solution extraction，并明确区分 rebuild 与 resolve。

在 `wet040_001_2m`、ALNS 关闭、`maxNodes=1` 的小规模诊断中，共记录 72 次 RMP。内部 `totalMs` 合计 7657.920 ms，现有 PC 外层计时合计 7659.292 ms，仅相差 1.372 ms，说明分段基本闭合。40 次 `strong_branching_light_repair_rmp` 合计 7101.788 ms，其中：

1. CPLEX solve 为 4799.726 ms，占 67.6%；
2. model rebuild 为 2227.642 ms，占 31.4%；
3. rebuild 内 coverage/machine rows 为 1836.184 ms，占 rebuild 的 82.4%；
4. solution extraction 为 71.939 ms，其他未归类时间仅 2.483 ms。

同配置关闭计时后，objective、bound、pool、strong trial 次数和有效性均未变化；总时间分别为 22.364 s 与 20.655 s。单次 wall time 存在约 1.7 s 波动，不能据此认定为诊断开销，但诊断默认关闭，因此不影响正式实验。该结果已经证明后续若优化 strong-trial RMP，首要 Java 侧目标应是 coverage row 构造；同时约三分之二时间仍在 CPLEX solve 内，严格保持模型与求解流程时无法由 Java 缓存消除。

### 6.2 P0：VND 算子计时

新增默认关闭的 `twet.alns.vndOperatorTiming` 诊断。一次 ALNS 开始时清零，结束时汇总每个 VND 算子的 `searchBest()` 时间、`commit()` 时间、调用次数和实际改进次数；开关关闭时不读时钟、不维护统计容器，也不输出日志。算子构造、shuffle、调用顺序、最佳 move 和接受逻辑均未改变。

在 `wet040_001_2m` 上使用固定随机种子、ALNS 无 wall-clock 截止、`maxNoImprove=5`、不进入 BPC 节点的诊断中，开关开启和关闭均得到初始目标 22750，时间分别为 2.428 s 和 2.504 s。开启统计时，VND 算子累计约 2309.6 ms，其中 `CrossExchangeOperator.searchBest()` 为 1382.3 ms，`PathInsertOperator.searchBest()` 为 753.0 ms，两者合计占已记录算子时间的约 92.5%；所有 commit 合计不足 60 ms。该项只用于定位，不据此修改 VND 算子。

### 6.3 time-indexed primitive scratch：测试后不保留

曾实现 engine-local `dist/predState/predAddedJob` 复用，并保证每轮完整重置当前 `stateCount`。结构测试中，同一 engine 连续复用和每轮重新分配得到的 certificate、列数量、列顺序、sequence 与成本逐项一致。

跨进程 root-only 对照受机器状态影响很大：首次复用/不复用为 45.782/67.084 s，但复用组复测又达到 63.831 s，不能把整轮差值归因于数组复用。同一 JVM、同一 LP、交替执行两条路径的定向基准进一步显示：

1. 每组 50 次 pricing：复用 1401.291 ms，重新分配 1381.938 ms；
2. 每组 300 次 pricing：复用 21950.830 ms，重新分配 21838.625 ms。

复用没有稳定收益，反而慢约 0.5%--1.4%，说明当前主要成本是 `O(n^2H)` 图扫描，三块 primitive 数组的 TLAB 分配并非热点。该实现、测试开关和临时 benchmark 已全部撤回，主线保持原状。

### 6.4 最小版 ColumnPattern：保留

本项只抽取列中不随 id、成本、来源和 seed 标记变化的结构：sequence、sequence signature、job bitset 和 job visit counts。pricing candidate 新增到 Pool 时，正式列直接复用 candidate 的 `ColumnPattern`；同 signature 获得更低成本时，仅替换列 header，并继续复用 Pool 中原有 pattern。arc 系数仍按原 sequence 计算，没有加入 adjacency、cut coefficient 或其他缓存。系统属性 `twet.bpc.shareColumnPattern=false` 保留了旧构造路径，用于严格 A/B。

定向结构测试验证了以下语义：共享前后 sequence、signature、job coverage、重复 job 次数和所有普通 arc visit count 完全一致；sequence 视图不可修改；成本改善仍保留原 Pool pattern，并维持原 source/seed 更新规则。`ColumnPatternSharingTest`、`TimeIndexedGraphOptimizationTest` 和 `LPRestrictedColumnMembershipTest` 均通过。

在同一 JVM 内交替执行 100,000 条长度为 12 的不同候选列入池操作时，共享路径为 111.597 ms，旧路径为 145.403 ms，旧路径耗时约为共享路径的 1.303 倍。该基准覆盖候选构造和 Pool 入池，因此收益来自消除第二次 sequence 复制、signature 构造、bitset 构造和 visit-count 扫描。

`wet040_001_2m`、ALNS 关闭、`maxNodes=1` 的完整 root 对拍结果如下：

1. 开启共享：objective 62989，bound 22490，36 轮 pricing，13482 条 RMP/Pool 列，4 次 exact，valid；
2. 关闭共享：objective 62989，bound 22490，36 轮 pricing，13482 条 RMP/Pool 列，4 次 exact，valid。

两组 root 时间分别为 14.401 s 和 14.407 s，差异可以忽略；总时间分别为 27.978 s 和 26.500 s，主要受 CPLEX 和机器状态波动影响，不能据此评价该局部优化。由于结构和完整流程严格一致，且定向热路径有稳定收益，本项默认开启并保留。

### 6.5 coverage row 列主序构造：保留

原路径对每个 job 扫描一次全部 restricted internal columns，重复读取同一列并查询 visit count。新路径先为所有 job 创建表达式，再按 `restrictedColumnIds` 顺序只扫描一次列，通过列的 job bitset 把非零 visit count 分发到对应覆盖行。每行的变量 term 仍按原 restricted-column 顺序加入；覆盖行创建顺序、外包项、右端项和 set-covering 方向均未改变。系统属性 `twet.bpc.buildCoverageRowsByColumn=false` 保留原路径供 A/B。

在 `wet040_001_2m`、ALNS 关闭、`maxNodes=1`、开启 RMP 分阶段计时的两轮相反顺序 A/B 中：

1. 第一轮旧/新 coverage 累计为 2576.795/1741.153 ms，新路径下降 32.4%；
2. 第二轮旧/新 coverage 累计为 2498.131/1508.990 ms，新路径下降 39.6%；
3. 两轮平均 coverage 从 2537.463 ms 降至 1625.072 ms，下降约 35.9%；
4. 两轮平均 model build 从 3242.190 ms 降至 2342.907 ms，下降约 27.7%。

四组均得到 objective 62989、bound 22490、36 轮 pricing、13482 条 RMP/Pool 列和 `valid=true`。CPLEX solve 仍有独立波动，因此不使用总 wall time 评价该 Java 构造优化。focused 编译、`ColumnPatternSharingTest` 和 `LPRestrictedColumnMembershipTest` 通过。

### 6.6 SRI cut/column 系数缓存：保留分页缓存

40-2 time-indexed + SRI、ALNS 关闭、`maxNodes=1` 的初始诊断中，共有 46 次 model rebuild。cut rows 累计 71893.718 ms，占 build 78554.976 ms 的 91.5%；coverage 和 branch rows 分别只有 4706.151 ms 和 292.620 ms。该 active-cut 场景中，cut 系数重算才是 RMP 构造的主要热点，普通 arc/adjacency branch row 不值得为了不足 0.3 秒的累计时间给全部列增加常驻索引。

首先单独测试了在 `TWETCut` 创建时编译 scope/memory membership。full、node-memory 和 arc-memory 的 2 万条随机序列对拍全部一致，200 万次 evaluator 微基准从 385.263 ms 降到 229.200 ms；但真实 no-strong root 的 cut rows 为旧/新 6186.741/6291.480 ms，完整 strong root 的新路径也没有下降。该改动没有端到端收益，已经连同测试开关和临时测试完整撤回。

随后实现严格 `(cutId,columnId)` 系数缓存。缓存位于全局 `CutPool`，因此正式 LP 与 strong-trial LP 共享；cut ID 和 column ID 均不可变，同一 sequence 的成本改善不影响系数。实现按 4096 个 column ID 分页，页只在实际访问时分配；每项使用 `short` 保存非负整数系数和未缓存哨兵，超过 `Short.MAX_VALUE` 的极端系数不缓存并继续现算。默认总容量为 16,777,216 项，约 32 MiB 裸系数；达到容量后不再分配新页，已有缓存继续命中。系统属性 `twet.bpc.cacheSubsetRowCoefficients=false` 可恢复原路径。

同一 40-2 SRI root 的结果为：

1. 关闭 strong branching：cut rows 6186.741 → 5209.097 ms，下降 15.8%；总时间 84.732 → 81.365 s；
2. 开启原有 40 次 strong trial：cut rows 71893.718 → 17237.178 ms，下降 76.0%；model build 78554.976 → 23707.400 ms，总时间 211.051 → 145.329 s，下降 31.1%。

开关两侧均为 objective 62989、bound 22526.814815、286 轮 pricing、46832 条 RMP/Pool 列和 `valid=true`。`CutPoolCoefficientCacheTest` 覆盖零系数、分页边界、全局容量、最大可缓存系数、超界退化和关闭开关；cut ID 不可变、active-memory merge、child cut 继承、列结构共享和 restricted-column membership 回归均通过。

### 6.7 bounded top-K：只保留 reduced-cost 筛列

repair 成功和 child 首次 LP 后的 restricted-column reset 原本会收集所有 reduced-cost 合格列、完整排序，再在保留全部正值列后截到 `branchSeedColumnLimit=5000`。新路径在扫描时维护容量为 5000 的 worst-first heap；由于正值列数量要到扫描结束后才完全确定，heap 先保留前 5000 个普通候选，随后按剩余名额从最差端裁掉，再按原 comparator 排序。正值列的保留和原 restricted 顺序不变；普通候选仍按 reduced cost、column ID 排序。列化外包列使用同一规则。系统属性 `twet.bpc.boundedReducedCostColumnSelection=false` 保留旧路径。

定向随机候选基准使用同 comparator、`K=5000`，并逐项核对输出 ID 与完整排序前 K 一致。去掉预热后的平均结果为：

1. `M=10000`：4.002 → 2.750 ms，快 1.455 倍；
2. `M=50000`：17.555 → 4.666 ms，快 3.762 倍；
3. `M=100000`：31.499 → 4.930 ms，快 6.390 倍；
4. `M=500000`：215.954 → 7.721 ms，快 27.971 倍。

40-2、ALNS 关闭、`maxNodes=2` 的完整开关对拍中，两侧均为 bound 22490、2 nodes、57 轮 pricing、18185 条列；node 1/2 的 strong candidateCount、top preview、左右 side bound、score 和最终选中 arc 逐项一致，总时间为 41.210/41.095 s。小池上整体收益不可见，但大池下选择热路径的定向收益明确，因此默认开启。

strong branching candidate 暂不改：候选通常至多 `O(n^2)`，并且 Tree 需要完整 candidateCount 和排序后的 preview。time-indexed root seed 也暂不改：当前排序后会跳过 main Pool 中已有 signature 并继续向后补足 200 个新 seed，同时可能改善已有列成本；简单前 200 heap 会改变补位和改善顺序。

### 6.8 rank-1 packed residual：保留

time-indexed rank-1 pricing 原来为每个 label 保存 `byte[activeCuts]`。每次 processing 扩展先复制整段数组，再逐 cut 判断 scope、node/arc memory 和奇偶 residual；join 与 dominance 也逐 cut 扫描。由于 residual 始终只有 0/1，新路径按 active-cut 顺序压入 `long[]`，并预先为 job/arc 构造 retain、toggle 和 scope mask。扩展先用位运算得到新状态，只对实际发生 carry 或 dominance correction 的 bit 按 cut 下标递增累加 dual，因此浮点累加顺序仍与旧循环一致。等待弧继续共享只读 residual，bucket、label ID、candidate 顺序和 sequence 恢复均未改变。

系统属性 `twet.bpc.packedRank1Residual=false` 保留旧 `byte[]` 后端用于 A/B；`twet.bpc.verifyPackedRank1Residual=true` 只用于诊断，会在真实 labeling 中同时执行旧公式，并逐次核对 forward、backward、source prepend、join 和 dominance。40-2 time-indexed + SRI root 的双算运行覆盖 89 条 active cuts，没有出现 shift、residual 或 dominance 不一致。

同一 `wet040_001_2m`、ALNS 关闭、strong branching 关闭、`maxNodes=1` 的顺序反转对照结果为：

1. 旧后端两次 exact pricing 累计分别为 49.366 s 和 49.115 s；
2. packed 后端关闭双算诊断后为 27.443 s，相对两次旧后端平均值下降约 44.3%；
3. 三组均得到 bound 22526.814815、286 次 pricing、46832 条 Pool/RMP 列、235 条 cutPool cut、89 条 active cuts和 `valid=true`；
4. packed 双算诊断组因同时执行两套公式为 68.852 s，只用于正确性，不用于性能比较。

`ActiveCutInheritanceTest`、`CutPoolActiveRank1MemoryMergeTest`、`CutPoolCoefficientCacheTest`、`LPRestrictedColumnMembershipTest`、`StrongBranchingPhaseOnePricingTest` 和 `TimeIndexedGraphOptimizationTest` 均通过。该项没有减少 horizon 或 residual 组合数量，但显著降低了每个 rank-1 label 的状态复制和 cut 扫描成本，因此默认开启并保留。

### 6.9 time-indexed candidate 重复扫描：只保留两项小改动

`ColumnPattern` 接入后，普通和 rank-1 time-indexed candidate 仍先显式构造一次 `SequenceSignature`，随后 `TWETColumn(sequence,...)` 又在新建 `ColumnPattern` 时复制 sequence 并构造第二次 signature。现在改为每个候选只构造一个 `ColumnPattern`，map key 直接取该 pattern 的 signature，轻量列头复用同一个 pattern。rank-1 dual-window 候选先完成 evaluator 回刷和负 reduced-cost 检查，只有真正保留时才构造 pattern/signature。该调整不改变 signature 内容、候选 ID、heap comparator 或返回顺序。

repeated-job 判定原来为每个候选新建 `boolean[n+1]`。现在每个 solver 只保留一份 `int[n+1]` marks，每检查一条候选递增 epoch；仍按原 sequence 顺序扫描并在第一次重复时返回。50 万条长度为 12、`n=60` 的定向基准中，原布尔数组路径为 25.437 ms，epoch 路径为 12.664 ms，局部约快 2.01 倍。20 万条候选的完整 pattern 构造基准中，原“独立 signature + 列内 pattern”路径为 40.467 ms，新路径为 34.221 ms，局部约快 1.18 倍。

没有给全部时空 state 增加 `depth`。该数组会增加 `O(nH)` 常驻内存，只为少量最终候选预分配 sequence 容量，不抵成本。也没有改变 pre-heuristic 对 repeated pseudo-schedule 的拒绝规则，exact pricing 仍允许原有 repeated sequence。

普通 time-indexed 40-2 root 得到 bound 22487.647059、211 次 pricing、45113 条列和 `valid=true`；rank-1/SRI root 仍得到 bound 22526.814815、286 次 pricing、46832 条列、89 条 active cuts 和 `valid=true`。后者 exact 时间受机器状态影响从前一轮 27.443 s 波动到 39.135 s，因此不把 root wall time 归因于本项，只采用上述同 JVM 定向基准评价局部收益。`ColumnPatternSharingTest`、`StrongBranchingPhaseOnePricingTest` 和 `TimeIndexedGraphOptimizationTest` 均通过。

### 6.10 time-indexed lookup 与 static table：测试后不改

primitive scratch 已在第 6.3 节证明没有收益并完整撤回。本项继续单独量化剩余两个建议。`Node.createTimeIndexedPricingOnlyArcLookup()` 当前只在节点存在时空禁弧时，把按 pair 保存的 `BitSet` 引用复制到长度 `O(n^2)` 的数组；不复制 BitSet，也不在热循环查询 HashMap。`n=40`、16400 条时空禁弧、10 万次构造的定向基准中，单次 lookup 平均约 10.023 微秒。即使按 40-2 SRI root 的 286 次 pricing 全部重建，合计也只有约 2.9 ms，远低于 pricing 与 LP 时间。跨轮缓存反而必须覆盖 forbid、replace、merge、copy、preprocessing writeback 和 fixing 中途修改等全部失效路径，因此不实施。

`StaticPricingData` 的完整 penalty/duration table 在同一实例上的单次构造平均约 0.905 ms。普通 time-indexed engine 在生命周期内只构造一次；rank-1 engine 已把同一份数据传给其 no-cut delegate。pre-heuristic 当前默认关闭，root preprocessor 又是独立短生命周期 context。把该表提升到全局 `TWETBPCContext` 只会增加大 penalty table 的存活时间，无法获得可见收益，因此不修改。

临时基准源码已删除，Node、Context、static table 与 lookup 失效语义均保持现状。

### 6.11 ng-DSSR repeated-segment scratch：测试后不改

当前 `addMinimumNewPairsRepeatedSegment()` 为每条待检查的非基本 route 分配 `lastPosition` 和
`middleMarks` 两个 `n+1` 整数数组。为区分“减少分配”和“改变扫描逻辑”的影响，本次只在临时
基准中实现了两种严格等价后端：其一把两个数组都改为 solver-local epoch scratch；其二只复用
数组，仍按原逻辑对 `lastPosition` 执行 `fill(-1)`，仅让 `middleMarks` 的 mark 跨调用递增。
20,000 组随机 sequence/ng-set 对拍中，两种后端与原方法的返回值、added-pair 顺序和最终
ng-set 逐项一致。

在 `n=60`、长度 21、含一个完整重复段的 500,000 次更新基准中，全 epoch 后端因每个 sequence
元素增加 epoch 判断，中位耗时反而由约 154.5 ms 增至约 182.5 ms。复用数组但保留 `fill` 的
后端中位耗时约 120.7 ms，局部快约 21.9%，但绝对节省只有约 68 ns/route。即使按极端
`candidate C=1000`、91 轮 DSSR 且每轮全部扫描计算，累计也仅约 6 ms；真实流程找到 K 个有效
更新后还会提前停止。因此该处分配不是可见热点，不值得给 solver 增加可重入 scratch 状态。
临时基准源码已删除，主线重复段选择、缺失 pair 数、span tie-break 和更新顺序均未修改。

### 6.12 集中回归

全部逐项测试结束后，重新定向编译并依次运行
`ActiveCutInheritanceTest`、`ColumnPatternSharingTest`、
`CutPoolActiveRank1MemoryMergeTest`、`CutPoolCoefficientCacheTest`、
`LPRestrictedColumnMembershipTest`、`StrongBranchingPhaseOnePricingTest` 和
`TimeIndexedGraphOptimizationTest`，7 项均通过。当前保留的代码修改只覆盖已得到定向或
端到端收益的诊断/实现；primitive scratch、cut membership 预编译、lookup/static table 和
ng-DSSR repeated-segment scratch 等无收益尝试均已撤回或从未接入主线。

### 6.13 packed residual 语义与旧 VRP 的 SRI 系数路径

packed residual 的每个 bit 不是“是否经过该 cut”，而是当前 limited-memory 有效段内 scope
命中次数除以 2 后的余数：0 表示偶数次、没有未配对命中，1 表示奇数次、留下一个未配对命中。
前向扩展先按当前 arc/job memory 清除不再保留的旧 bit，再对当前 scope job 翻转 bit；只有
保留后为 1 且本次需要翻转的 bit 才形成一次 `1 -> 0` carry，并减去对应 cut dual。scope、
retain 与 toggle mask 只依赖本轮 active cut 的 scope/memory 和扩展 job/arc，不依赖 label
或 dual 数值，因此可在一次 pricing 开始时预计算。`residualWordCount=(activePricingCuts+63)/64`，
一次 pricing 内固定；cut separation、inactive removal 或 dual 活跃集合变化后的下一次 pricing
会按新的 active cut 数重建 masks 和 label residual 长度。

旧 VRP `BPC.LP.LP` 没有 `(cutId,routeId)` 系数缓存。新增三元素 SRI 时，它利用
`cus2schedule` 的 customer-to-route 倒排表统计既有 route 命中 cut customer 的数量，命中至少
两个时写系数 1；新增 route 时，则逐条 active SRI 调用 `route.contains()` 检查三个 customer，
同样在命中至少两个时写系数 1。

这里所谓旧 VRP 的“持久 CPLEX 模型”，只指一个 `LP` 对象/节点的列生成与割平面循环：
`Construct()` 创建一次 `IloCplex`，后续 `AddColumn()` 和 `AddSRCut()` 直接向该模型增量追加
变量与约束，反复求解，直到 `Clear()` 才释放；它不是整棵分支树共享一个 CPLEX 模型。当前
TWET 在单个 `LP` 的普通列生成循环中同样会通过 `addColumns()` 增量加列，并用
`resolveCurrentModel()` 重解，因此“TWET 每轮 pricing 都重建 RMP”的说法不准确。

进一步检查后确认，TWET 原先在 cut loop 中重建 RMP 没有必要，已经按旧 VRP 的方式改为增量
维护：`addCuts()` 直接给当前全部内部列计算系数并向活模型增加 `IloRange`，后续新列仍由
`addColumnToCurrentModel()` 补该行系数；inactive cut 或 rank-1 memory 旧版本则直接从活模型
删除对应 row。PC 在这两种变化后统一调用 `resolveCurrentModel()`，不再执行 `buildModel()`。
`ActiveCutInheritanceTest` 新增了活模型验证：加入一条系数为 1、rhs 为 0 的 SRI 后当前 RMP
立即不可行；随后在 cut 已存在时再增量加入另一条同样违反该 cut 的新列，RMP 仍保持不可行；
删除该 row 后才恢复可行。这同时覆盖了“旧列补新 cut”和“新列补已有 cut”两个方向，证明修改
不只是更新 active cut ID。`CutPoolActiveRank1MemoryMergeTest`、`CutPoolCoefficientCacheTest`
和 `CutPoolImmutableIdTest` 也全部通过。

现在与旧 VRP 的单节点 cut/column 增量维护方式已经一致。仍然存在的模型重建只来自确实替换
restricted column set、repair/真实目标切换，以及每个正式节点和 strong-trial 的独立初始模型。
全局 immutable ID 缓存仍可跨这些 `LP` 对象复用 `(cutId,columnId)` 结果，但先前在“cut loop
每轮重建”口径下测得的缓存收益不能直接外推到新流程，后续性能比较应以增量 cut 版本重新测量。

为排除历史机器负载差异，使用相同当前 class、相同 40-2 time-indexed+SRI root 配置，只替换
提交前后的 `LP/PC` 顺序执行直接 A/B。旧重建版本为 73.384 秒、286 轮 pricing、6 次 rebuild，
累计 build 7165.678 ms，其中 cut rows 6046.391 ms；新增量版本为 55.942 秒、275 轮 pricing、
仅初始 1 次 rebuild，累计 build 80.095 ms。总时间下降 23.8%，明确消除的模型重建时间约
7.09 秒。新增量版本最终 bound 为 22527.007009，旧版为 22526.814815，二者 objective 均为
62989、cut rounds 均为 9、cut pool 均为 235 且 `valid=true`。bound 和 pricing 轮数的轻微变化
来自保留 basis 后退化 LP 选择了不同最优原始/对偶点，继而改变 separation 与列生成轨迹，不是
cut row 缺失；因此 23.8% 是该次端到端观测，不能全部解释成 Java 建模常数收益，稳定可归因项
是少掉 5 次完整 rebuild。

### 6.14 后续六项建议的当前代码复核

本节只复核当前代码和适用边界，没有修改算法或实现。

1. **rank-1 packed mask 重复构造：当前不存在。** `CutStateData` 先在循环中装载全部
   active cuts，循环结束后才调用一次 `buildPackedMasks()`。当前复杂度已经是
   `O(Cn^2)`，不是 `O(C^2n^2)`。清单中的行号正好落在循环结束后的调用和方法声明处，
   但误读了大括号层级，因此不应再次修改。
2. **SRI 系数缓存被历史 inactive cuts 占满：代码风险成立，但热点尚未实测确认。**
   `CutPool` 的分页缓存达到全局上限后只拒绝新页，没有回收。以 75,013 个 column ID
   估算，每条完整 cut row 实际需要 19 个 4096 项页，即 77,824 项；默认容量最多完整
   容纳约 215 条 cut，而不是忽略分页取整后约 223 条。历史 cut pool 达到 6,783 时，后期
   新 cut 确实可能无法建立缓存。不过当前没有 cache miss、拒绝分配和 active/inactive
   页占用统计，不能仅凭容量把它定为已证实 P0。若实现，应只在新页分配失败时释放当前
   active set 之外的整条 cut pages；当前 active pages 超过容量时继续按现状不缓存，不能
   引入逐 coefficient LRU。释放只导致后续重算，不改变系数语义，但可能在不同 queued
   node 的 active set 之间抖动，因此应先加低频统计再决定是否保留。
3. **domain strong trial seed 去重：成立且严格等价，但默认主线不使用。**
   `prepareDomainFilteredChildSeedColumns()` 的两个 `ArrayList.contains()` 确实会退化为
   `O(k^2)`；改成 `LinkedHashSet` 后按原循环插入并最终转回 `ArrayList`，顺序和集合均可
   保持不变。当前 `enableStrongBranchingDomainRepair=false`，默认 arc strong repair 走
   lightweight 路径，而后者已经使用 `LinkedHashSet`。因此该项应修，但优先级是低风险
   清理，不是当前默认求解热点。
4. **PathInsert/CrossExchange 小数组和全覆盖后空扫：成立且可严格等价修改。**
   两个算子的最内层确实反复构造 `{false,true}` 或 `{false}`，改成固定上界的整数循环可
   保持 `false -> true` 的枚举顺序。JIT 可能通过逃逸分析消除部分小数组，因此不能预先
   承诺收益；但代码改动很小。`commit()` 在所有机器已 covered 后使用 `continue`，后续
   operation 不可能再提交，改为 `break` 完全等价。该 `break` 对两台机器尤其直接；奇数
   机器下可能始终有一台未 covered，收益会更小。
5. **SRI row 批量 Concert 建模：成立，适合隔离 A/B。** 当前每个非零系数调用一次
   `IloLinearNumExpr.addTerm()`；本机 CPLEX API 明确提供
   `addTerms(double[], IloNumVar[])`。按 restricted-column 顺序收集非零项后一次写入，
   可以保持变量顺序、系数和 row 顺序完全一致。增量 cut loop 已消除正式节点重复重建，
   但每个独立 strong-trial LP 仍需重建 active cut rows，因此该项仍可能有收益。应只比较
   cut-row 构造耗时；若数组收集成本抵消 Concert 调用收益，则撤回。
6. **columnized outsourcing 两项：均成立，但只影响列化外包。** column-major coverage
   当前只覆盖内部机器列，外包列仍在每个 job 行内扫描全部 restricted outsourcing
   columns。可按 restricted outsourcing column 顺序扫描一次 job bitset 并分发到各行，
   且每行 term 顺序保持不变。`getPositiveOutsourcingColumnIds()` 也会在同一 parent LP
   的每个 strong side 再次逐列调用 `cplex.getValue()`；而 `solveCurrentModel()` 已在
   `readOutsourcingValues()` 中读取同一批变量。更合适的做法是在解读取时同步保存正值
   outsourcing column IDs，并在 re-solve、增删/筛换 restricted set 或关闭模型时失效。
   这两项对当前纯调度实验没有收益，但对后续列化外包主线值得实现。

`Pool.addOrImproveColumn(List,...)` 的重复 signature/pattern 构造也确实存在，但 pricing
candidate 已经走共享 `ColumnPattern` 的重载；剩余入口主要是初始列和少量非 pricing
调用，继续保持低优先级。当前建议的实施顺序调整为：VND 严格等价小改与 domain seed
去重先做；SRI `addTerms()` 单独 A/B；列化外包两项在外包实验前做；CutPool 回收先补容量
拒绝统计，确认真实发生后再实现。rank-1 mask 不再处理。

### 6.15 六项建议的逐项 A/B 与最终取舍

本轮按“只改变实现、不改变 pricing、DSSR、strong branching 和返回列语义”的边界逐项测试。
临时 benchmark 和无收益实验分支均已删除。

1. **rank-1 packed mask：不修改。** 再次核对 `CutStateData`，全部 active cut 装载完成后只调用
   一次 `buildPackedMasks()`；原清单中的 `O(C^2n^2)` 问题在当前版本不存在。
2. **CutPool inactive 页回收：保留。** 只有全局系数缓存达到容量上限时，才释放当前 LP
   active cut 集合之外的整页缓存。释放不改变 cut 或 column，只会使 queued node 以后重新启用
   该 cut 时重算系数。强制 4096 项容量的定向测试中，旧路径对后期 active cut 连续 200,000 次
   查询全部 miss；回收后只有首次 miss，五轮局部耗时由 32.1--39.0 ms 降至 2.4--2.9 ms，
   快约 11.2--15.8 倍。该收益只在缓存饱和后发生，普通未饱和路径直接返回。缓存边界、active
   memory 合并、immutable cut ID 和父子 cut 继承测试均通过。
3. **domain strong seed 去重：保留。** `ArrayList.contains()` 改为 `LinkedHashSet`，扫描顺序和
   最终 seed 顺序不变。8,000 条 child seed 与 12,000 条 parent restricted columns、合并后
   16,000 个唯一 ID 的定向测试中，716.799 ms 降至 6.273 ms，约快 114 倍。该路径只在
   `enableStrongBranchingDomainRepair=true` 时使用，默认 lightweight strong 不受影响。
4. **VND 两项：只保留全覆盖后的 `break`。** 将 `{false,true}` 小数组改写成整数循环后，
   `wet040_001_2m`、固定 5 次无改进上限的三轮交替 A/B 中，旧/新 solve 中位数为
   2.400/2.384 s，差异不稳定；算子调用、改进次数和目标 22750 完全一致，说明 JIT 已基本消除
   这些短命数组，因此该改写撤回。PathInsert/CrossExchange 在所有机器已经 covered 后，
   剩余 saved operations 不可能再提交，`continue` 改 `break` 严格等价并保留。
5. **SRI row 批量 `addTerms()`：撤回。** 80 条 row、20,000 个变量、每行 5,000 个非零项的
   Concert A/B 中，逐项 `addTerm()` 与批量数组路径中位数约为 74.3/72.1 ms，只快约 3%，
   且多轮结果会反转。完整数组分配和收集抵消了 JNI 调用减少，不能解释历史十几秒 cut-row
   时间，因此不保留双路径或新参数。
6. **columnized outsourcing：两项均保留。** coverage row 改为按 restricted outsourcing
   column 顺序扫描一次，并把其 job 集合分发到各行；每行 term 顺序不变。四个 SP1/SP2
   对拍算例的 objective、bound 和有效性全部一致；列化外包总时间四轮 A/B 的中位数约由
   0.468 s 降至 0.425 s，约下降 9%。同时在 `readOutsourcingValues()` 已读取 CPLEX 变量时
   保存正值外包列 ID；同一父 LP 的 strong sides 直接复用，模型改变、重新求解或求解失败时
   由 `lastSolution` 口径失效，不再二次逐列调用 `cplex.getValue()`。
7. **Pool List 入口预建完整 pattern：不修改。** 60,000 条高唯一率序列时预建 pattern
   快约 1.6--2.8 倍；但 200 条序列反复命中且成本不改善时，当前只建 signature 的路径反而
   快约 2.5--4.2 倍。正式 pricing 已走共享 `ColumnPattern` 重载，剩余 List 入口的重复率较高，
   统一改写会引入明确退化，因此保持现状。

最终整合版通过 `CutPoolCoefficientCacheTest`、`CutPoolActiveRank1MemoryMergeTest`、
`CutPoolImmutableIdTest`、`ActiveCutInheritanceTest`、`LPRestrictedColumnMembershipTest`、
`OutsourcingMoveConsistencyTest`、`TimeIndexedGraphOptimizationTest`、
`ColumnPatternSharingTest` 和四组 `OutsourcingModelComparisonTest`。本轮没有改变任何算法
流程、列选择顺序或数学模型。

### 6.16 提交后的正确性复核

重新按 LP 生命周期检查后发现一处缓存失效边界：标准 PC 在增量加入内部/外包列后会立即
resolve，因此第 6.15 节实验没有使用旧解；但 `LP.addColumns()` 和
`LP.addOutsourcingColumns()` 本身原来没有立即清空 `lastSolution`。正值外包列缓存接入后，
若在 add 与 resolve 之间直接调用 getter，可能返回上一 LP 解的正值外包列集合。现在两种增列
入口只要实际新增至少一列，就同时清空 `lastSolution` 和正值外包列缓存。该修改与“增量改变
模型后旧 LP 解失效”的既有语义一致，正常 PC 仍按原顺序立即 resolve。

新增 `PositiveOutsourcingColumnCacheTest`，使用真实列化外包 LP 验证：首次 solve 后缓存命中；
增量加入内部列后立即失效、resolve 后重建；增量加入外包列后再次失效、resolve 后重建；
close model 后缓存为空。CutPool 测试也补充了 inactive cut 被回收后重新激活、重新缓存并命中
的往返路径。再次检查确认：coverage 分发保持每个 job 行“内部列 restricted 顺序在前、外包列
restricted 顺序在后”；`LinkedHashSet` 保持 domain seed 的首次出现顺序；VND 的 `break` 只在
全部机器均 covered 时触发，后续 operation 按原逻辑必然全部被跳过；CutPool 回收只删除数值
缓存，不修改 cut、column、现存 CPLEX row 或 active-cut ID。

### 6.17 固定 seed、dual 生命周期与后续热点复核

本轮继续按真实调用链复核两项正确性边界和四项性能建议，没有修改算法代码。

1. **固定初始列的 incumbent 口径需要修正，但只影响固定 seed 模式。** 当前
   `InitialColumnBuilder.buildFixedSeed()` 会在目标实例上重新评价固定 sequence，并把这些列
   ID 写入 `InitialColumnBundle`；但 `Tree.incumbentCostFromInitial()` 仍优先返回
   `data.configure.bestSolution.curCost`。因此，若目标 `Data` 恰好保留了另一条 heuristic
   `bestSolution`，就会出现 UB 来自旧解、incumbent IDs 来自固定 seed 的不一致。现有
   `GCBBFullDomainComparisonTest` 为目标实例重新建 `Data`，且没有运行目标侧 heuristic，
   所以当前时间尺度实验没有触发；但该入口一旦触发会直接影响全树剪枝，后果高于普通实验
   配置错误。最小修正应当是：`fixedInitialColumnSeed != null` 时只按 bundle 中目标实例已重评
   的 incumbent 列求和；普通 seed 模式才允许复用 `bestSolution.curCost`。
2. **旧 dual 可读属于 API 生命周期缺口，不是当前主线错误，暂不引入全面 `dualValid`。**
   模型增列、增删 cut、目标模式切换后会清空 `lastSolution`，但 dual 数组仍保留上次成功求解
   的值。当前 PC 的所有正式路径都在这些 mutation 后立即 `solveRelaxation()` 或
   `resolveCurrentModel()`，成功求解后才进入 pricing；失败求解也会 `clearDuals()`，未发现
   mutation 后直接使用旧 dual 的生产调用链。给 `getJobDual()`、`getArcDual()` 等高频 getter
   全部增加运行时检查会把分支带入 pricing 热循环，收益和风险不匹配。当前应维持
   “模型变化后先重解、再读 dual”的生命周期约束；只有发现真实违规调用者时，再在 pricing
   入口或 snapshot 入口集中拒绝，而不是给每个 getter 增加检查或静默清零。
3. **paper rank-1 separation 的 visit-count 改写成立，且是当前最高优先级热点。**
   `rank1Value()` 构造 full-memory、multiplier 0.5 的临时 cut，并对每个三元 scope、每条正值
   列重新构造 membership 和扫描 sequence。对该固定口径，系数严格等于
   `floor((v_i+v_j+v_k)/2)`；单行 scope 为 `floor(v_i/2)`。`ColumnPattern` 已保存
   `jobVisitCounts`，可按当前 `LinkedHashMap` 迭代顺序预存正值列和值，再按原顺序累加，保持
   浮点求和、违反度、候选和加入顺序不变。`hasPositiveFullRank1Coefficient()` 等价于 scope
   总访问次数至少为 2。`Candidate` 的 scope signature 也可在创建时计算一次，避免排序比较器
   反复分配字符串。该替换只适用于 full-memory 违反度探测和正系数预判；后续构造 node/arc
   limited memory 时，访问顺序决定有效 memory，仍必须扫描原 sequence。历史 60-3 日志中
   separation 已占约 30.9 秒，因此该项既有复杂度依据，也有真实热点依据。
4. **rank-1 dual-window 的第一次 SRI 扫描是死计算，适合直接修改。** 当前候选先调用
   `objectiveCostFromReducedCost()` 扫描 sequence 和全部 active cuts；dual-window 分支随后
   用 evaluator 覆盖 cost，并调用 `reducedCost()` 再扫描一次。前一次结果没有副作用且必然被
   覆盖。把分支前移后，dual-window 只执行 evaluator 和真实 reduced-cost 重算，非 dual-window
   仍执行 reduced-cost 反推 objective，候选过滤和返回值严格不变。这是改动最小、风险最低的
   一项，但收益取决于每轮进入最终候选检查的负状态数。
5. **packed residual 的 legacy 矩阵确有冗余，但优先级低于前两项性能修改。** packed 开启且
   verification 关闭时，`scopeByCut`、`memoryByCut`、`arcMemoryByCut` 和 `zeroResidual`
   只用于构造 packed masks，labeling 随后不再读取。直接从 immutable `TWETCut` 构造 masks
   可以消除累计短命数组和 GC，但必须分别保持 full/node/arc memory 的 forward retain、
   forward toggle 和 backward retain 语义。该重写会形成 packed 与 legacy 两套初始化路径，
   代码复杂度明显高于节省一次候选扫描；应先补 constructor 分段计时或分配基准，再决定是否
   保留，不能仅凭 2309 次调用估算就直接接入主线。
6. **PathInsert 源侧删除结果可复用，但必须保持枚举顺序，不能简单重排循环。** 固定
   `(m1,a,len)` 后，源机器删除片段的硬窗可行性和 `costM1` 与 `m2/b/rev` 无关；当前确实在
   每个目标插入位置重复执行同一个 `merge2Segments()`。安全实现应在现有循环顺序内做惰性
   缓存，避免改变 `savedOperations` 的插入顺序，因为当前 comparator 对小于 1 的 cost 差可能
   返回 0，稳定排序会保留原枚举次序。当前 `Utility.curUpperBound` 在一次 `searchBest()` 内
   不变化，故同轮复用成本在数值上可行；仍需用现有慢速 sequence evaluator 对拍，并单独测量
   源侧 merge 占比。CrossExchange 的重复主要位于 Cmax 预检和正式评价之间，合并需要传递更多
   bridge/shift/duration 中间量，改动与收益都更不确定，暂不与 PathInsert 一起修改。

当前建议顺序为：先修固定 seed 的 incumbent 口径；随后实现并 A/B rank-1 separation 的
visit-count 路径；再删除 rank-1 dual-window 死扫描。旧 dual 生命周期先保留约束、不改热
getter；packed legacy 初始化和 VND 源侧缓存分别在取得分项计时后再决定。

### 6.18 固定 seed 与 rank-1 后续优化结果

本轮按第 6.17 节的边界完成三项严格等价修改，并对另外两项先计时再取舍。

1. **固定 seed 的 incumbent 口径已修正。** `Tree` 只有在普通 seed 模式下才允许直接复用
   `data.configure.bestSolution.curCost`；`fixedInitialColumnSeed` 非空时，UB 由目标实例上
   已重新评价的 bundle incumbent 列和外包成本求和。这样 incumbent cost、column IDs 和后续
   剪枝使用同一组解。普通 ALNS seed 流程不变。
2. **paper rank-1 separation 已改用 visit count。** 每轮分离先按当前
   `TWETMasterSolution.columnValues` 的既有迭代顺序保存正值列和值；一行和三行 full-memory
   违反度直接用 `floor(sum visitCounts / 2)` 计算，不再为每个 scope 构造临时 cut、membership
   并扫描完整 sequence。浮点值仍按原 restricted-column 顺序累加。limited node/arc memory
   的正式构造仍扫描 sequence，因为该步骤依赖任务访问顺序。候选 scope signature 也改为创建
   candidate 时计算一次，排序器不再反复构造字符串。
3. **rank-1 dual-window 的死扫描已删除。** dual-window 候选直接执行 evaluator 和真实
   reduced-cost 重算；非 dual-window 候选仍按 graph reduced cost 加回 dual 得到 objective。
   原来先执行、随后必然被 evaluator 覆盖的 `objectiveCostFromReducedCost()` 不再调用。
4. **packed residual legacy 初始化不继续改造。** 新增默认关闭的
   `twet.bpc.rank1CutStateTiming`，只在开启时记录 allocation、cut 装载、packed mask 构造和
   两套 payload 估算。40-2 root-only、3 个 cut round 的诊断中共构造 93 次，三部分累计仅
   `2.912/4.914/15.653 ms`，合计约 `23.48 ms`；同批 rank-1 exact pricing 为 `8.740 s`，
   构造占比约 `0.27%`。因此 legacy 数组虽有短命分配，但当前不是 pricing 热点，不值得拆成
   两套初始化后端。
5. **PathInsert 源侧缓存不实现。** 现有固定 ALNS 计时中整个
   `PathInsertOperator.searchBest()` 约 `0.753 s`，源侧重复 `merge2Segments()` 只占其中
   一部分；即使完全消除，收益上限也远低于当前 root、pricing 和 LP 热点。没有足够证据支持
   为它引入依赖枚举顺序的惰性缓存。

正确性验证方面，新增 `SubsetRowCutCoefficientTest`，对 `n=1..80`、每个规模 200 个随机
重复任务序列以及随机一行/三行 scope，将 visit-count 快速公式与通用
`SubsetRowCutEvaluator` 对拍，全部通过。受影响主源码的 focused 编译和当前
`Basic/Common/HEU/Output/TWETBPC` 全量编译通过；40-2 rank-1 root 诊断得到
`NODE_LIMIT, incumbent=22750, bound=22525.666667, valid=true`。既有
`TimeIndexedGraphOptimizationTest` 当前会在读取已含 `SETUP` 扩展块的 40-2 数据时按旧格式
解析并抛出 `NumberFormatException`，失败发生在进入本轮测试项之前，本轮未把该独立测试数据
入口问题混入算法修改。

### 6.19 提交后正确性复核

对提交 `6aed50fd` 再次沿实际调用链检查，未发现本轮三项修改改变当前主线结果语义。

1. 固定 seed 模式下，root trace、初始 incumbent、预处理 UB 和后续树剪枝均调用同一个
   `incumbentCostFromInitial()`；目标值由目标实例重新评价后的 incumbent column IDs 求和，
   不再混用 `data.configure.bestSolution.curCost`。当前入口
   `GCBBFullDomainComparisonTest` 使用 no-outsourcing 数据，固定 seed 中的内部列构成完整
   可行 incumbent。需要明确的适用边界是：`FixedInitialColumnSeed` 目前只保存内部 sequence，
   不保存 outsourced job、baseline 或 outsourcing cost，因此它不是通用的带外包 incumbent
   快照；后续若把该实验开关用于外包算例，必须先扩展该数据对象。
2. visit-count 快速公式只用于 paper 模式的 full-memory、multiplier=`1/2` 违反度探测，以及
   limited-memory 构造前“full coefficient 是否为正”的等价预判。node/arc memory 的实际
   构造仍按 sequence 顺序扫描，最终 LP/pricing cut 系数仍由统一 evaluator 计算。对重复访问
   序列，通用状态递推与 `floor(sum visits / 2)` 严格一致。
3. dual-window 分支删除的只是随后必然被 evaluator 覆盖的 objective 反推。该分支仍先按目标
   实例真实 PWLF 评价 sequence，再用 machine/job/arc/cut true dual 重算 reduced cost；非
   dual-window 和 Phase-I 原路径未改。
4. `rank1CutStateTiming` 关闭时不调用时钟、不计算 payload，只保留一次属性读取和空摘要返回；
   统计不会额外扫描 PWLF、sequence 或 cut。

本次使用新的临时 class 目录对 `Basic/Common/HEU/Output/TWETBPC` 共 171 个 Java 源文件
全量编译，并通过 `SubsetRowCutCoefficientTest`、`CutPoolActiveRank1MemoryMergeTest`、
`CutPoolCoefficientCacheTest`、`ColumnPatternSharingTest` 和
`ActiveCutInheritanceTest`。工作区中原有未提交实验文档、`PackedBitSet.java` 和数据文件均
未纳入本次复核修改。

### 6.20 固定 reference seed 与 scope signature 后续建议

继续核对四项建议后，本轮不修改求解器主线，取舍如下。

1. `GCBBFullDomainComparisonTest` 的 fixed-reference seed 确实会在每个 mode 的
   `runOne()` 中重新生成。只要未设置 `fixedInitialReferenceDir`，该路径完全不执行；它不是
   BPC 主线开销。但若后续继续用 fixed-reference 做 normal/full 或 time-scale 公平对照，
   必须在 mode 循环外只生成一次 immutable seed，并记录 sequence fingerprint。原因不是节省
   一次 ALNS，而是墙钟停止的 ALNS 即使重置相同随机种子，也不能保证两个 mode 得到完全相同
   的列集合。当前代码的 `both` 模式会先为 normal 运行一次 reference ALNS，再为 full
   重新运行一次；第二次运行可能因机器负载和单位时间内完成的迭代数不同而停在另一组历史列。
   此时两种 pricing 的起点已经不同，比较结果不能再解释为“只改变 pricing 方法”。当前尚未
   安排新的 fixed-reference 实验，因此本轮只记录，不扩展 runner。
2. 固定 incumbent 的 coverage、重复任务、机器数和外包边界验证属于实验接口防误用。当前唯一
   入口使用同规模 no-outsourcing 数据，且 source/target 列成本比例会在启动求解前校验，现有
   实验不触发该风险。成本比例校验只能证明 seed 中已有 sequence 的成本按预期变化，不能证明
   这些 sequence 在目标实例上恰好覆盖全部 job。例如误用较小 reference 时，已有列仍可能全部
   通过比例校验，但目标实例多出的 job 没有出现在 incumbent 中；`Tree` 随后仍会把这些列的
   低成本当作可行 UB，用于 root preprocessing、arc fixing 和节点剪枝。重复 job、内部列数
   超过机器数，以及带外包实例中遗漏 outsourced jobs 也是同类问题。若恢复 fixed-reference
   实验，验证应放在 seed 快照生成/应用边界，并与第 1 项一起完成；不为一个默认关闭的诊断入口
   扩大生产 `InitialColumnBuilder`。
3. 目标列成本重复评价只发生在上述固定 reference runner。避免第三次评价需要让 seed 携带
   target cost 或新增 target snapshot，接口复杂度高于当前收益；在第 1 项尚未启用前不处理。
4. paper rank-1 的 active-scope 查询确实仍为已排序的一元/三元 scope 构造短命
   `ArrayList<Integer>` 和字符串，但它只影响主线分离的常数。最新 40-2 三轮 cut separation
   累计约 `0.126 s`，没有证据表明它仍是热点。后续只有在更新后的 50/60-job SRI 日志再次显示
   separation 占用显著时，才考虑用已排序 scope 的直接编码替换；当前不为微小分配增加另一套
   scope key 口径。

后续讨论进一步收紧了上述边界。当前 fixed-reference 实验的明确契约是：reference 与 target
具有相同任务、机器和既定 incumbent 分配，只改变时间或成本尺度；reference incumbent 已由
启发式产生并验证可行。因此第 2 项的通用防误用校验不实现。第 1 项若恢复实验，则只修改
`GCBBFullDomainComparisonTest`：在进入实例/mode 循环前，用一个固定的 reference 配置运行
一次 ALNS，构造包含 initial sequences、incumbent sequences、source costs 和确定性 sequence
fingerprint 的不可变 runner-local snapshot；各个 `runOne()` 接收同一个 snapshot，只在各自
target 数据上重评成本并设置 `config.fixedInitialColumnSeed`，不再运行 reference ALNS。fingerprint
必须保持初始列顺序、列内任务顺序和 incumbent 列顺序，不能排序后再计算，否则无法证明两个
mode 的真实输入完全一致。

### 6.21 fixed reference seed 单次构造实现

已按第 6.20 节方案只修改 `GCBBFullDomainComparisonTest`，其余 fixed-seed、pricing 和
rank-1 代码不动。`main()` 现在在实例/mode 循环前调用一次
`prepareFixedInitialColumnReference()`；该方法用固定 normal 配置运行一次 reference seed
流程，保存 initial/incumbent sequences、source initial costs、source incumbent 和按原始顺序
计算的 SHA-256 fingerprint。normal、full 和 nodeJoin 的 `runOne()` 接收同一个不可变快照，
仍按各自 target 数据执行原有成本重评与比例校验，然后把同一个 `FixedInitialColumnSeed`
写入配置。CSV 新增 `fixed_seed_fingerprint` 列；未启用 fixed reference 时该列为空。

验证使用新的临时 class 目录全量编译 171 个 Java 源文件。随后在
`wet040_001_2m` 上关闭 ALNS、设置 `maxNodes=0` 运行 `mode=both` 的入口烟测：
`Prepared fixed initial reference` 只输出一次，half/full 两次应用日志和两行 CSV 均为同一
fingerprint `0c4fac674a77936a772354209d2e8d4bb92bdc5b7565466de8ed3f32a1a11a50`；
两边 incumbent 都是 `62989`，验证均为 `valid=true`。这证明本次修改消除了 mode 间重复
reference seed 生成，同时没有改变目标成本重评口径。另用同一实例关闭 fixed-reference
入口运行 normal 烟测，原初始列流程仍为 `valid=true`，新增 CSV fingerprint 字段为空。

### 6.22 Coverage、strong 筛列与 rank-1 后续建议复核

本轮继续沿实际调用链复核四项主建议，只形成修改顺序和正确性边界，未修改求解器代码。

1. **Coverage `addTerms()` 值得单独 A/B，但现有 82.4% 不能直接解释为逐项
   `addTerm()` 的耗时。** `masterLpPhaseCoverageNanos` 同时覆盖
   `buildCoverageConstraints()` 和 `buildMachineConstraint()`，历史 82.4% 是二者合计占
   rebuild 的比例，不是纯 coverage row 计时。当前 coverage 的确按 restricted-column 顺序，
   对每个非零 job 系数调用一次 `addTerm()`；machine row 也对每个内部列调用一次
   `addTerm()`。CPLEX 22.1.1 的 `IloLinearNumExpr` 已确认提供
   `addTerms(double[], IloNumVar[])`。安全的 A/B 应先把 coverage 与 machine row 分开计时，
   然后保持每行“内部列 restricted 顺序、列化外包 restricted 顺序、显式外包变量最后”的
   term 顺序，用逐 job 的紧凑 primitive/reference 数组一次加入。不能为每个 job 直接分配
   `restrictedColumnCount` 大小的满数组，否则 root 大列池会产生明显瞬时内存；应先计数后精确
   分配，或使用无装箱的可增长 row builder。此前 SRI row 的批量 `addTerms()` 只快约 3%，说明
   Concert 的逐项表达式构造未必对应一次 JNI，因此该项必须实测，不能因 coverage 占比高就
   直接默认保留。
2. **Strong-trial 筛列的批量读取是当前更确定的低风险优化。**
   `resetRestrictedColumnsByCurrentReducedCost()` 目前对每条列先扫描 sequence 做
   `isColumnCompatible()`，随后又分别调用 `cplex.getValue(var)` 和
   `cplex.getReducedCost(var)`。但该函数的三个生产调用点都位于一次成功 LP 求解之后；
   `lastSolution.columnValues` 正是同一求解按相同 `VALUE_TOLERANCE` 保存的正值列集合，
   `lambdaVars[idx]` 也始终与 `restrictedColumnIds[idx]` 对齐。可先用
   `lastSolution.columnValues.containsKey(columnId)` 保留正值列并跳过 compatibility 扫描，
   再对其余列做兼容性检查；reduced cost 则通过已确认存在的
   `cplex.getReducedCosts(lambdaVars)` 一次读取，并按相同下标使用。这样保持正值列优先、
   comparator、top-limit 和最终列顺序不变。批量 API 异常不应在每轮悄悄回退到逐变量查询；
   该状态表示当前 LP 解或变量数组生命周期不成立，应与现有建模错误一样集中抛出。若需要兼容
   独立测试入口，应在方法入口验证 `lastSolution` 为当前成功解，而不是在热循环加入 fallback。
   列化外包筛选仍有同类逐变量读取，但规模通常更小，可在内部列 A/B 确认后再统一。
3. **Rank-1 候选的 multi-cut 单扫数学上可行，优先级位于前两项之后。** 当前每个最终候选
   对每条 active cut 调用一次 `SubsetRowCutEvaluator.coefficient()`，每次重新分配 membership
   并扫描完整 sequence。`CutStateData` 已有按 cut index 打包的 forward retain/toggle masks；
   固定 sequence 可从零 residual 开始，对每条 `prev -> job` 弧执行与 labeling 完全相同的
   `retained = residual & retainMask`、`carry = retained & toggleMask`、
   `residual = retained ^ toggleMask`。每个 carry bit 对应相应 cut coefficient 加一。
   为保持浮点结果，不能在扫描 sequence 时立即累加 dual penalty；应先得到按 cut index 的
   `int[] coefficient`，再按原 active-cut 顺序执行 `coefficient * dual` 累加。该实现同时覆盖
   full、node-memory 和 arc-memory，但必须新增随机 sequence 对拍，逐 cut 比较通用 evaluator
   与 packed coefficient，而不能只比较最终 penalty。它只优化进入候选回刷的负 end states，
   不改变 labeling、候选 heap 或排序，因此收益取决于 SRI pricing 中实际回刷候选数量。
4. **重复 rank-1 memory union 确实存在，但暂不建议引入 merge plan。** Generator 为了让
   no-op memory 候选不占 family quota，会先扫描当前 active cuts 合并同 base memory；PC 随后
   又在 `CutPool.mergeRank1MemoryIntoActive()` 中扫描一次，以删除旧 active memory versions、
   创建不可变新 cut 并更新 active ID。两次扫描语义不同，不能简单删除前一次：否则 no-op
   候选会占分离额度并造成空转。携带 matched IDs/version fingerprint 的 plan 可以消除第二次
   union，但 PC 会在同一 separation result 中逐条更新 `activeAfterSeparation`，plan 必须验证
   同 base active versions 未变化，接口和失效处理明显复杂。当前 active pricing cuts 通常只有
   十几到二十余条，这两次线性扫描远小于 memory 构造、RMP row 和 SRI pricing；先补
   generator pre-merge 与 CutPool merge 的独立计时，只有累计达到可见热点再做。
5. **低优先级项继续保持不动。** Phase-I penalty ID set 可以替代一次 restricted sequence
   扫描，但只发生在 repair 成功恢复真目标前；lightweight seed preparation 已知累计约
   182 ms；Node time-indexed map/window COW 没有独立计时；branch rows 只有毫秒级。它们都
   不应先于 coverage A/B、strong 筛列批量读取和 rank-1 候选单扫。

当前建议顺序为：先拆 coverage/machine 计时并 A/B coverage `addTerms()`；同时实现
strong 筛列的 `lastSolution + getReducedCosts()` 批量路径；若 SRI 候选回刷计时确认可见，再
实现 packed multi-cut coefficient scan。memory-union plan 和其余低优先级项暂不修改。

### 6.23 Coverage、strong 筛列与 rank-1 候选单扫 A/B 结果

本轮按第 6.22 节的顺序逐项实现、对拍和取舍。正式代码只保留有稳定净收益且不增加多套算法
语义的部分；无收益实验路径已删除。

1. **Coverage `addTerms()` 不保留。** 先把原 `coverageMs` 拆成
   `coverageRowsMs` 和 `machineRowsMs`，确认历史高占比主要来自 coverage rows，而不是
   machine row。随后实现每个 job 两遍扫描 restricted columns：第一遍统计非零项并精确分配
   `double[]/IloNumVar[]`，第二遍填充后调用一次 `addTerms()`。40-2、root-only、一阶段 strong
   branching 的两轮交替 A/B 中，逐项版本的 coverage rows 分别为
   `1150.249/1316.573 ms`，批量版本为 `1340.297/1423.904 ms`，批量路径反而慢约
   `8.2%--16.5%`。原因是 Concert 的逐项 `addTerm()` 并非每项一次独立求解器调用，而批量
   路径额外增加两遍列扫描和临时数组分配。四组均为 `NODE_LIMIT`、bound `22490`、
   exact 4 次且 `valid=true`。批量实现和开关已完整删除；只保留默认关闭的 coverage/machine
   分项计时，供后续定位模型构造热点。
2. **Strong-trial restricted-column 批量读取保留并成为唯一实现。** 正值列直接读取
   `lastSolution.columnValues.keySet()`，因此在 compatibility 扫描前即可保留；全部
   `lambdaVars` 的 reduced costs 通过一次 `cplex.getReducedCosts(lambdaVars)` 读取，并按
   与 `restrictedColumnIds` 相同的下标使用。方法入口要求存在当前
   `LP_RELAXATION` 快照，数组长度不一致或 CPLEX 批量读取失败时直接抛错，不提供静默逐变量
   fallback。40-2 对照的 40 次筛列中，旧路径累计 `389.583 ms`、平均 `9.740 ms`；新路径累计
   `209.120 ms`、平均 `5.228 ms`，下降 `46.3%`。每次 input、positive、incompatible 和
   selected 数量逐项一致，两组均为 bound `22490`、exact 4 次、同一分支结果且
   `valid=true`。总求解时间为 `15.260/15.291 s`，说明该项是明确的小热点优化，不应解释为
   root 总时间的大幅改善。
3. **Rank-1 packed multi-cut candidate scan 不保留。** 临时实现从零 residual 开始，
   对 sequence 的每条弧同时应用全部 cut 的 retain/toggle mask；carry bit 逐 cut 累加整数
   coefficient，最后仍按原 active-cut 顺序乘 dual。开启逐候选双算校验后，40-2 两轮 cut、
   247 次 pricing 中没有发现 packed 与通用 evaluator 的逐 cut 系数差异。完整 8 轮 A/B 的
   4066 次候选回刷中，legacy coefficient 扫描累计 `19.651 ms`，packed 单扫为 `7.401 ms`，
   局部下降约 `62.3%`，但只节省约 `12.25 ms`，不足完整 root 的 `0.03%`。两组均为
   275 次 pricing、pool `46609`、cutPool `235`、88 条 active cuts、bound
   `22527.007009`，搜索轨迹一致；完整 wall time 的较大差异来自同机 labeling/LP 运行波动，
   不能归因于这段毫秒级代码。考虑新增 scratch、验证路径和两套系数实现的维护成本，相关代码
   和属性已全部撤回。

最终保留修改为：RMP coverage/machine row 的默认关闭分项计时，以及
`resetRestrictedColumnsByCurrentReducedCost()` 的当前解快照与批量 reduced-cost 读取。当前
相关源文件全量编译通过，`LPRestrictedColumnMembershipTest` 和
`SubsetRowCutCoefficientTest` 通过。

### 6.24 Strong-trial 批量筛列提交后正确性复核

本轮针对提交 `0430e38b` 重新沿 LP 生命周期检查，而不是只依赖前一轮计时对照。当前未发现
批量筛列引入的正确性问题。

1. `lastSolution.columnValues` 由当前一次 `solveCurrentModel()` 在读取 dual 后立即构造，
   并且只保存 `value > VALUE_TOLERANCE` 的内部列；这与旧实现逐变量调用
   `isPositiveCurrentColumn()` 的判断条件完全相同。模型增量加列、加删 cut、切换 repair
   目标等操作都会使 `lastSolution` 失效，因此入口要求当前状态为 `LP_RELAXATION` 可以阻止
   在旧解上筛列。
2. 无论完整建模还是增量加列，`lambdaVars[idx]` 与
   `restrictedColumnIds[idx]` 都按同一顺序创建或追加。一次
   `getReducedCosts(lambdaVars)` 返回后按相同下标读取，和旧版按 column id 查询对应变量的
   reduced cost 一致；筛选完成后才替换 restricted ID 集，不存在筛选过程中改变索引的问题。
3. 三个生产调用点均位于一次成功 LP 求解之后。正式 child 和 repair 后需要继续求解的路径会
   调用 `solveRelaxation()`，按筛后 ID 重建 RMP；lightweight strong trial 只把筛后 ID 作为
   child seed 返回，不会在仍含旧变量的活模型上继续 resolve。
4. 列化外包继续使用原有独立变量和筛选流程。内部列筛选不会修改外包变量；外包正值判断和
   reduced cost 仍直接读取同一次已求解的 CPLEX 模型，因此内部 `lastSolution` 失效不会改变
   外包筛选结果。
5. coverage/machine 分项计时只把原 `coverageMs` 拆成两个子项，
   `coverageMs=coverageRowsMs+machineRowsMs`；总计时仍只把完整 build 计入一次，没有重复
   记账。相关统计开关默认关闭，筛列热循环不执行额外计时。

验证方面，重新对当前 `Basic/Common/HEU/Output/TWETBPC` 的 171 个 Java 源文件执行带
CPLEX 22.1.1 classpath 的完整编译，编译通过；`LPRestrictedColumnMembershipTest` 与
`StrongBranchingPhaseOnePricingTest` 均通过，后者覆盖 time-indexed/ng-DSSR 与 no-cut/SRI
四种 Phase-I pricing 组合。剩余风险仅是本轮没有再重复运行完整长算例；严格等价的逐轮
input/positive/incompatible/selected 对拍和 40-2 root 结果一致性已经在第 6.23 节完成。

### 6.25 SRI cut posting 与列式 rebuild 的优先级判断

本轮只读比较当前 TWET 与旧 VRP 的真实建模路径，结论是：SRI cut posting 值得先补统计并
做独立 A/B；完整 RMP 列式 rebuild 当前不值得修改。

1. **SRI 行全量扫描是真实剩余热点。** 当前无论 rebuild 旧 active cuts，还是
   `addCuts()` 增量加入新 cut，都会对每条 cut 扫描全部 restricted internal columns。
   coefficient cache 只避免重新扫描 sequence，不会消除 `activeCuts * restrictedColumns`
   次循环、pool 查询和 cache lookup。已有 40 次 strong-trial 诊断中 cut rows 为
   `9.977s`，明显高于 coverage 的 `3.566s` 和 branch rows 的 `0.337s`，仅低于 CPLEX solve
   的 `29.754s`，因此它是当前 Java 建模侧最值得继续定位的部分。
2. **当前计时仍不完整。** rebuild 的 cut rows 已进入 `MasterLpPhaseTiming`，但 cut loop 中
   `lp.addCuts()/removeCuts()` 位于 generator 计时与后续 resolve 计时之间，新增/删除行的成本
   没有进入两边。下一步应先增加默认关闭的 cut-row 诊断，分别记录 rebuild/add/remove、
   restricted 列数、posting 候选数、cache hit/miss、实际非零 term 数和 Concert 写行时间。
   只有确认主要时间花在全量 scan/cache lookup，而不是 `addTerm/addLe`，才接入 posting。
3. **不能直接复制旧 VRP 的 `customer -> routeIds`。** 旧实现以 route 是否包含 scope customer
   为主，适合其 elementary route 口径；当前统一 SRI evaluator 按访问次数累加，同一个 scope
   job 重复访问两次也可能在 multiplier `1/2` 下产生系数 1。若只用不同 job posting 的交集，
   会漏掉这种列。当前 paper SRI 的安全候选位图应至少包含：
   `P_a&P_b | P_a&P_c | P_b&P_c | R_a | R_b | R_c`，其中 `P_j` 表示访问 job `j`
   至少一次，`R_j` 表示至少两次。full-memory 下它与“scope 总访问数至少 2”等价；
   limited-memory 下它是安全上集，最终仍交给统一 evaluator。非 `1/2` multiplier 应走通用
   安全路径，不能硬套该公式。
4. **LP-local position BitSet 的生命周期可控。** rebuild 时按当前 restricted 顺序构造；
   增量新列只在尾部追加 position；筛列后若继续求解，本来就会重建 RMP 和 posting。
   cut 候选按 position 升序遍历即可保持原 restricted-column term 顺序，全局
   `(cutId,columnId)` coefficient cache 继续保留。该结构只应在存在 active cut 或首次
   `addCuts()` 时延迟创建，no-cut 主线不承担分配成本。
5. **完整列式 rebuild 暂不修改。** 当前 coverage 默认实现已经按内部列逐条读取一次，再把
   term 分发到各 job 行；它不再是旧式的 `job * restrictedColumns` 扫描。改成先建空
   objective/coverage/machine rows，再用 `IloColumn` 建变量，主要减少 variables/objective/
   coverage/machine 的几次线性遍历并改变 Concert 建模顺序，但 branch/cut rows 仍需随后全量
   构造，无法处理当前 `9.977s` 的主要热点。它还会牵涉列化外包、tariff、Phase-I/repair
   objective 和分支行的建模顺序；此前 coverage `addTerms()` A/B 已说明 Concert API 形式
   更“批量”并不保证更快。只有完成 cut posting 后，新的分项计时仍显示
   variables/objective/coverage/machine 占据数秒级大头，才值得做隔离开关 A/B。

因此当前修改顺序应为：先补 cut-row 完整诊断；若 scan/cache lookup 占主导，再实现支持重复
访问的 LP-local posting 并做新旧逐 cut 系数、term 顺序和最终模型对拍。列式 rebuild 保持现状。
