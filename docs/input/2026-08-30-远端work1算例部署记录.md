# 远端 work1 算例部署记录

本次部署目标为 `codex-runner@100.68.243.112` 下的 `D:\ccx_work\考虑交付的机器调度\work1`。远端操作严格限制在 `D:\ccx_work\考虑交付的机器调度` 及其子目录；本次不修改远端账户、服务、注册表、系统环境变量或其他目录。

本次只部署正式性能实验需要的两类算例。无外包目录包含 `experiment-suite/formal/instances/data` 下的 1620 个 `.dat` 文件；外包目录包含 `experiment-suite/formal/instances/outsourcing-data/default-discount` 下的 1620 个 `.dat` 文件。`no-discount` 下的 90 个数量折扣灵敏度算例不在本次范围内。

远端目录组织为：

```text
work1/
  instances/
    no_outsourcing/
      data/
      instances.tsv
      metadata/
    outsourcing/
      default-discount/
      outsourcing-instances.tsv
      metadata/
  config/
    remote-runtime.properties
```

2026-08-30 的环境检查确认，远端可直接使用 Java 21.0.3 和 CPLEX Studio 22.1.1。Java 位于 `D:\software\Java\jdk-21`，CPLEX Java 库位于 `D:\software\IBM\ILOG\CPLEX_Studio2211\cplex\lib\cplex.jar`，native library 位于 `D:\software\IBM\ILOG\CPLEX_Studio2211\cplex\bin\x64_win64`，CP Optimizer Java 库位于同一 Studio 目录的 `cpoptimizer\lib\ILOG.CP.jar`。以 `work1` 为显式工作目录启动 CPLEX CLI 并立即退出，返回版本 22.1.1.0 和退出码 0。远端未找到 Maven 和 Git，因此当前配置仅完成算例与运行依赖定位；代码、编译产物和最终可执行 manifest 尚未部署。

运行口径预先记录为每个求解进程单线程、最多 4 个独立子进程并发、每个算例求解时限 10800 秒。后续部署代码时仍需核对编译产物、classpath、manifest 中的实例路径和 CPLEX native library，不应仅凭本次实例部署直接启动正式计算。

部署完成后在远端重新统计数据。无外包目录为 1620 个 `.dat` 文件、共 72,243,183 字节；默认折扣外包目录为 1620 个 `.dat` 文件、共 62,218,953 字节，均与本机源目录一致，且远端不存在 `no-discount` 子目录。两类数据分别抽取小、中、大规模的首部、中部和尾部样本进行 SHA-256 对照，6 个样本全部一致。两个实例索引、运行配置和两份逐文件校验清单也分别通过整文件 SHA-256 对照。因此，本次实例传输可判定为完整且未发生可见内容变化。

## 2026-08-31 代码与运行配置部署

在重新核对远端现状后，将当前工作树部署到：

```text
D:\ccx_work\考虑交付的机器调度\work1\deployments\20260831-49b0c4e7\TWETScheduling
```

该快照基于 commit `49b0c4e746cd46cb28a5446fbd15e39887e164e9`。部署时 `git status` 曾把 `src/TWETBPC/LP/LP.java` 标为修改，但 2026-09-01 复核确认其规范化 Git blob 与 `HEAD` 完全一致，`git diff` 和保存的 `source-working-tree.patch` 均无该文件内容差异；该状态来自工作文件混用 LF/CRLF，并非未提交的 LP 逻辑修改。部署前使用本机 JDK 22 按 `--release 21` 重新编译现行主线，排除已经与当前数据结构不兼容的历史 `src/BPC` 包。远端最终包含 226 个现行 Java 源文件和 654 个 class。`BestBpcProfilesTest`、`FormalExperimentRunnerStatusTest`、`ExperimentBatchSchedulerTest` 和正式实验基础设施 smoke 均在独立编译产物上通过；远端再次运行 `BestBpcProfilesTest` 也通过。

2026-08-31 后续审计确认，这不是最小生产部署。654 个 class 中只有 247 个是顶层 class，另外 407 个是 Java 编译器为内部类、匿名类和辅助对象生成的 class；226 个源码中按名称可明确识别的测试、诊断、实验 runner、生成器和校验器有 90 个，它们不属于单次正式求解的必要闭包。首次目录打包还把 7 个 `Basic` 包源码摊平到了远端 `src` 根目录，源码内容没有变化，但该源码快照不能直接作为可重编译工程。重新编译得到的 `target/classes` 目录结构正确，远端实际求解和全部 smoke 不受此包装问题影响。当前本地 HEAD 相对部署基线在 `src` 下没有提交差异，`BestBpcProfiles.java`、`LP.java`、`FormalExperimentRunner.java` 和 `TWETBPCSolver.java` 的本地、staging 与远端 SHA-256 均一致，因此可确认运行中的核心 solver 代码仍是当前版本；若正式长跑前要求干净交付，应另建精简版本目录，而不是继续把本目录描述为最小代码包。

部署包先以 ZIP 形式上传并完成 SHA-256 对照，本地与远端均为 `f213e0db7d52c97155e482ad8eba74d223293349fc0c89c34501b00b113e7afc`。远端账户可定位但无权执行系统 `tar.exe`，因此实际文件改用 `scp -r` 写入新的版本目录，没有覆盖旧代码或算例。后续修正了新建 `run-remote-formal.cmd` 的参数接口；远端每次覆盖前都在同一脚本目录保留了对应 `before-*` 备份。

远端专用 manifest 已把本地 `${WORKSPACE}` 路径替换为 `work1` 下的绝对路径。`pricing-comparison.tsv` 通过 `scan-only` 解析 6480 个任务，其中 NG-DSSR、time-indexed 和 time-indexed+SRI 各 1620 个；`outsourcing-performance.tsv` 通过 `scan-only` 解析 4860 个任务，其中 columns/masterVariables 各 1620 个 solve，并包含相应 seed 任务。两者分别读取已部署的 `instances/no_outsourcing/data` 和 `instances/outsourcing/default-discount`，正式输出统一写入 `work1/results/formal/20260831-49b0c4e7`，避免不同代码版本混写。由于 `instances/outsourcing/no-discount` 尚未部署，数量折扣灵敏度 manifest 仍明确标记为不可运行，不纳入本次配置。

远端运行脚本位于部署目录的 `scripts/remote`。`run-remote-smoke.cmd` 固定读取 n20 无外包算例，缺少 seed 时先生成共享初始列，再使用正式 NG-DSSR profile 运行最多 120 秒、100 个节点；`run-remote-formal.cmd` 读取指定分块 manifest，默认并发 4，第三个参数只允许为空或 `--scan-only`。正式 manifest 仍固定求解时限 10800 秒、节点上限 100000，`FormalExperimentRunner` 为每个子进程设置一个 CPLEX 线程。

真实远端 smoke 已完成。seed 生成耗时 4.008 秒，选中初始成本 34811、35 条初始列和 2 条 incumbent 列。随后 NG-DSSR 使用 profile `2026-08-30-v5` 正常结束为 `ROOT_PROCESSED`，`incumbent=bound=34811`，处理 1 个节点并形成 153 列；stderr 为空，`live.log`、配置快照、组件统计、节点、列池和 incumbent schedule 等结果均已写入：

```text
D:\ccx_work\考虑交付的机器调度\work1\results\smoke\20260831-49b0c4e7
```

当前活动配置为 `work1\config\remote-runtime.properties`。2026-09-01 将原部署编号 `20260831-49b0c4e7-workingtree-v2` 简化为 `20260831-49b0c4e7`：日期表示部署批次，短哈希表示可复现的仓库快照；单文件 `LP.java` 的 `6dd0e8e1...` 是 blob 哈希，不用于部署命名。改名前的活动配置备份为 `remote-runtime.before-rename-20260901.properties`，原 metadata 和两份 manifest 也保留对应 `before-*` 备份。配置已回读确认 Java 21.0.3、CPLEX 22.1.1、`Common.formal.FormalExperimentRunner`、`HEU.ExperimentBatchScheduler`、实例根目录、seed/result/manifest 根目录、4 进程并发和单进程 1 线程均指向本次部署；改名后两份 manifest 再次完成 6480/4860 条 `scan-only`，未启动正式批量计算。
