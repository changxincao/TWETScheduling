# BPC 整体效率优化候选方向

本文记录 2026-06-21 对当前 TWET BPC 组件进行的广泛复查。目标不是再随机叠加组件，而是结合当前代码中已经实现的 ng-DSSR、completion bound、pricingOnly subtree、midpoint probe、RMIH、外包列化和 partial dominance，判断下一步哪些方向最可能带来实质效率提升。

## 1. 当前判断

当前最值得优先尝试的不是继续加强单个 labeling 小剪枝，而是两个更上层的问题：dual stabilization 和 branching 策略。前者针对列生成迭代中的 dual churn，后者针对外包列化下 root bound 已经更强、但后续几乎全靠 ArcBrancher 慢慢抬下界的问题。

文献上，branch-and-price 的慢收敛通常和主问题退化、dual 振荡、分支规则不能有效抬下界直接相关。Pessoa、Sadykov、Uchoa、Vanderbeck 关于 column generation stabilization 的工作强调，LP-based stabilization 可以显著改善列生成收敛；Baldacci、Mingozzi、Roberti 的 ng-route relaxation 和 pricing strategy 说明 ng/DSSR 更适合作为 pricing 内部的可控松弛；VRP branch-price-and-cut 综述也把 dual stabilization、heuristic pricing、ng-route、robust branching 和 cut 管理放在同一层看，而不是只靠更强 dominance。

结合当前实验，ng、completion bound、probe、pricingOnly subtree 这些内部 pricing 技术已经有用，但它们没有改变外包列化下节点多的结构原因。外包 columns 模式在多段 tariff 下 root bound 更强，但后续成功分支几乎仍全是 arc branch，每个分支带来的下界提升很小。因此继续优化单个 label 操作只能降低单节点时间，不能解决节点数问题。

## 2. 优先级最高的方向

1. dual stabilization / smoothing

这是最值得先做的全局组件。当前主问题高度退化：内部列、外包集合列、set covering 覆盖行和分段外包成本都会让 dual 在相邻 pricing round 间波动。外包列化的 root bound 不差但 pricing rounds 多，也很符合 dual churn 的表现。

实现上可以先做低侵入版本：保留真实 LP dual 作为闭合证明口径，同时维护一个 stabilized dual center。启发式 pricing、外包 pricing 和前几轮 exact pricing 可以先用
`pi_stab = alpha * pi_current + (1 - alpha) * pi_center`
定价；如果 stabilized dual 下找不到列，必须再用真实 dual 跑 exact pricing，只有真实 dual 下也无负列时才能关闭当前 RMP。这样不会破坏精确性。

验证指标应看 pricing rounds、每轮生成列数、dual 变化幅度、root 时间和节点闭合时间，而不是只看单次 exact pricing label 数。

2. 外包列化下的 branching 改进

当前 columns 模式的主要问题不是 root bound 弱，而是没有类似旧模型 tariff segment branch 的强分支。`OutsourcingMembershipBrancher` 一次只切一个 job 是否属于外包集合，太细；`ArcBrancher` 又主要作用于内部机器列，对外包集合凸组合的分数性切得很慢。

下一步更合理的是做分支评分和条件分支，而不是固定顺序。可以先记录每类候选分支的 fractional score、预测子节点下界改善、历史 pseudo-cost，然后在 columns 模式下尝试：

1. 若外包 membership 分数性明显强于 arc 分数性，则优先外包 membership；
2. 若多个 job 的外包 membership 高度相关，则尝试 group / cluster membership branch；
3. 对多段 tariff，可以重新设计更稳的 baseline / tariff-regime 分支，但不能恢复之前那种低效 baseline 区间实现。旧实现语义可行，但测试中没有减少 pricing rounds，反而更慢。

这个方向直接针对节点数，优先级高于继续微调 subtree。

3. 外包列返回策略和列池管理

当前 `OutsourcingPricingEngine` 本身很快，但返回外包集合列后会放大主问题退化和 pricing round。直接把返回上限压得太小会恶化，因为会增加重解次数；因此更可行的是“多样性受控返回”而不是简单少返回。

可以按 baseline bucket、外包 job 数、reduced cost 桶保留代表列，避免同一类外包集合一次性灌入太多。闭合时仍需要一次真实 dual 下的完整外包 pricing，保证没有漏负列。

同时，restricted master 可以做列 aging：正值列、最近基列和低 reduced-cost 列保留；长期零值且 reduced cost 高的列在当前节点 restricted set 中移除，但不从全局 pool 删除。这个对 LP 解时间和子节点继承列规模可能有直接帮助。

## 3. 中等优先级方向

1. RMIH 调用频率自适应

RMIH 是上界启发式，不影响下界证明。当前它在很多节点上会被调用，但真正改善 incumbent 的次数有限。可以改成 root 强调用、incumbent 长时间未改善时调用、gap 低于某阈值时调用，或者每隔若干节点调用。时间限制 4 秒合理，但频率还可以更保守。

2. pricing engine 调度自适应

当前顺序基本是 `HeuristicPricing -> OutsourcingPricing -> exact`。这个顺序已经比 outsourcing-first 稳，但仍可做 stagnation counter：如果某个 engine 连续多轮只生成很少列，就临时跳过；如果 exact 连续发现很多负列，则减少前置启发式调用。闭合前仍必须跑真实 dual exact pricing。

3. pseudo-cost branching

当前可先不引入复杂强分支，但可以记录每次 branch 后的实际 bound improvement，形成 brancher 和具体 arc/job 的 pseudo-cost。对 arc branch 和外包 membership branch 都有价值，尤其适合解释“root bound 强但节点多”的情况。

## 4. 暂不建议作为主线的方向

1. full SRI / LM-SRI / arc-memory SRI 默认开启

前面的实验表明，SRI 能提升 root bound，甚至让部分算例在 root 闭合，但 label 状态膨胀明显，整体求解慢。它可以保留为诊断或特定难例开关，不适合作为当前默认效率路线。

2. time-indexed graph pricing 替代当前函数 pricing

无 cut 的 time-indexed graph pricing 单次 DP 很快，但会生成大量非基本 pseudo-schedule 列，根节点列数暴涨；当前能跑动主要依赖已有 elementary/ng 启发式补充。它适合作为论文方法对照，不适合作为当前 BPC 主线替换。

3. 直接 subtree on

已有结果显示 subtree on 有时能减少部分 pricing 难度，但也可能因为列信息不足、arc fixing 过早、后续节点重构成本变高而变慢。pricingOnly subtree 比 permanent subtree 更稳，但也不是解决 columns 节点多的核心。

## 5. 建议的下一轮实验顺序

第一步做 dual stabilization 的低侵入版本。它只改变 pricing 使用的 dual，不改变主问题模型和最终闭合证明，风险最低。实验先选两个口径：普通 ng-DSSR 无外包、columns 多段 tariff。重点看 pricing rounds 是否下降。

第二步做 columns 模式 branch diagnostic 和条件分支。不要先写复杂 group branch，先把 arc、membership、machine 三类候选的分数性、历史 pseudo-cost 和 child bound improvement 统计出来，再决定是否把 membership 提前。

第三步做外包列返回多样性策略。不要再简单调小 `maxOutsourcingPricingColumns`，而是保留 baseline/cardinality/reduced-cost 多样性，同时闭合前完整 pricing。

如果这三步都没有明显改善，再考虑更重的 cut 策略或并行 pricing。

## 6. Dual stabilization 的具体含义

对偶稳定的核心不是改变主问题，也不是改变最终下界证明，而是避免列生成过程中 pricing 被一组“抖得很厉害的 dual”牵着走。RMP 高度退化时，两个相邻 LP 最优解的目标值可能差不多，但 job dual、machine dual、arc dual 会出现较大跳动。pricing 根据这些 dual 找负 reduced-cost 列，dual 一抖，就容易一轮生成一批对当前 dual 很有利、下一轮又不太有用的列，导致 pricing rounds 多、列池膨胀、LP 反复重解。

在当前 TWET BPC 中，这个现象尤其可能出现在三类地方：set covering 覆盖行本身退化；外包 columns 模式下外包集合列数量多、分数凸组合多；ng-DSSR 和启发式 pricing 每轮会加入大量相近列。对偶稳定的作用就是让 pricing 使用一个更平滑的 dual，例如
`dual_stab = alpha * dual_current + (1 - alpha) * dual_center`，
先围绕一个稳定中心找列，而不是完全跟随当前 LP 极点的 dual。

实现上应先做安全的“两阶段口径”。第一阶段，启发式 pricing、外包 pricing 和可选 exact pricing 使用 stabilized dual 找列，目的是更快补充有用列。第二阶段，只要 stabilized dual 下找不到列，仍必须切回真实 LP dual 跑一次 exact pricing；只有真实 dual 下也没有负 reduced-cost 列，当前 RMP 才能被认为闭合。这样 stabilized dual 只影响“先找哪些列”，不影响下界正确性。

dual center 可以先用简单策略：root 或每个 node 的第一轮真实 dual 作为初始 center；当真实 LP bound 明显改善、或者真实 dual exact pricing 闭合一轮时，把 center 向当前 dual 移动。`alpha` 先固定在 `0.5` 或 `0.7`，后续再按列生成效果自适应。若 stabilized dual 下连续几轮生成很多列但真实 LP 改善很小，应增大 `alpha`；若真实 dual 波动大且 pricing rounds 多，应减小 `alpha`。

验证时不能只看单轮 exact pricing label 数。更关键的指标是 root pricing rounds、每轮 generated columns、dual 变化幅度、LP 重解次数、总列池大小、最终 node 数和总时间。若 dual stabilization 有效，应表现为 rounds 下降、列池更少或更稳定，且最终 bound 不变。

## 7. 旧 VRP 代码中的相关实现

旧 VRP 代码里确实有 stabilization 相关实现，但它不是 dual center smoothing，而是 slack penalty 型稳定化。位置在旧工程 `BPC/LP/LP.java`：LP 在 `vrptw` 模式下给每个 customer 覆盖行加一个非负 slack 变量，slack 的列系数为 1，目标系数初始很大。后续 `set_bounds()` 会把 slack 目标系数设置成 `max(0, mu[i] - init_value)`，`update_slacks()` 则在某个 slack 正值时把对应目标系数继续加 `step_size`，然后重新解 LP 并读取 dual。

这套逻辑的触发点在旧 `GCNGBB.Extend()`：如果 exact pricing 生成了负列，就正常加列；如果没生成负列且 slack 全为 0，则返回 false 表示当前 pricing 闭合；如果没生成负列但 slack 仍为正，就调用 `lp.update_slacks()` 提高 slack penalty，再返回 true 让列生成继续。PC 主循环中还会在每轮 heuristic/exact 之后调用 `lp.set_bounds()`，节点结束时调用 `lp.close_slacks()` 把 slack 上界关掉。

因此旧代码做的是“允许覆盖行暂时被 slack 放松，再逐步提高 slack penalty，把 dual 拉回合理范围”。它的优点是实现位置集中在 LP 内部，不需要改所有 pricing engine；缺点是它改变了当前 RMP 的可行域和目标函数，最后必须关掉 slack 重新验证。它更像一种 primal/penalty stabilization 或 feasibility smoothing，不是我们前面建议的“pricing 先用平滑 dual、闭合前回到真实 dual”的两阶段 dual smoothing。

对当前 TWET 项目，旧 VRP 的代码可以作为参考，但不建议直接照搬。原因是当前模型里覆盖行、机器数行、外包变量/外包列、tariff 约束和 repair slack 已经更复杂；再加一套覆盖 slack penalty 容易和现有 feasibility repair 混在一起。更稳的第一版仍应使用 dual provider 的方式，只改变 pricing 使用的 dual，不改变 LP 模型本身。

## 8. 最该参考的稳定化路线

当前最该参考 Pessoa、Sadykov、Uchoa、Vanderbeck 2018 的 LP-based stabilization 框架，而不是直接照搬旧 VRP 的 slack penalty。原因是这篇文章明确把 smoothing 和 proximal penalty 作为 column generation 的标准稳定化手段来分析，并强调参数可以自适应调整；这和当前 TWET 需要的“低侵入、可开关、先实验”的工程路线一致。

第一版建议只实现其中的 smoothing 部分。具体做法是维护真实 dual `pi` 和中心 dual `pi_bar`，pricing 先用加权 dual：
`pi_stab = alpha * pi + (1 - alpha) * pi_bar`。
若用 `pi_stab` 找到了负列，就加入 RMP；若找不到，必须切回真实 dual `pi` 跑 exact pricing。只有真实 dual 下也找不到负列，节点才算 pricing 闭合。这样实现只改变列生成顺序，不改变 LP 模型和最终下界证明。

`pi_bar` 的更新可以先保守处理：每个 node 第一轮 LP dual 初始化为 center；当真实 LP bound 改善明显、或者真实 dual exact pricing 完成闭合检查时，把 center 向当前 dual 移动。`alpha` 初始取 `0.5` 或 `0.7`。后续如果 stabilized dual 下生成很多列但 bound 改善很小，就增大 `alpha`，让 pricing 更接近真实 dual；如果真实 dual 轮间波动大、pricing rounds 多，就减小 `alpha`，加强稳定。

旧 VRP 的 slack penalty 可以作为第二阶段备选，因为它需要修改 RMP 并引入额外 slack 语义。当前更稳的第一步是 dual provider smoothing。

## 9. 主要参考

1. Pessoa, Sadykov, Uchoa, Vanderbeck, “Automation and Combination of Linear-Programming Based Stabilization Techniques in Column Generation”, INFORMS Journal on Computing, 2018.
2. Baldacci, Mingozzi, Roberti, “New Route Relaxation and Pricing Strategies for the Vehicle Routing Problem”, Operations Research, 2011.
3. Desaulniers 等， “Exact Branch-Price-and-Cut Algorithms for Vehicle Routing”, Transportation Science, 2019.
4. SCIP Ryan/Foster branching 文档和近年的 cluster branching 工作。它们共同说明：在列变量极多、普通变量分支弱的模型中，分支规则需要直接切到 fractional structure，而不是只依赖原始 arc。

## 10. 60 任务长尾诊断和处理顺序

2026-06-21 对 `wet060_001_2m` 的长运行监控显示，root 的 RMIH 失败状态为 `Unknown` 且耗时约 4.13s，基本对应默认 4s 时间限制；node 2 的 RMIH 状态为 `Infeasible`，不是时间不足。因此后续把大规模算例的 RMIH 默认时限放宽到 20s，阈值暂定为 `n>50`，用于减少 root 上界启发式过早放弃的问题。

ng-DSSR 的退出语义也需要明确：一轮 relaxed pricing 若返回 elementary 负列，会立即把这些列交给 RMP，不在同一次 pricing 中继续加强 ng-set；若没有 elementary 负列但存在 non-elementary negative route，才更新 ng-set 进入下一轮；若两者都没有，则当前 pricing 证明无负列并返回。因此“没有负列后继续加强 ng-set”不是当前流程，也不应作为长尾处理手段。

当前 60 任务 node 3 的现象更像 column generation tailing-off：多轮 exact pricing 每次还能返回少量负列，偶尔又重新出现较强负列。这通常和主问题退化、dual oscillation、尾部 reduced-cost 很小的列反复进入有关。下一步已增加每轮 `MasterLP` 目标值 trace，用于判断“加列后 LP objective 是否明显变化”。需要特别注意，RMIH 不在 pricing loop 内触发，而是在 `pc.solve(node)` 返回、当前节点定价闭合以后才作为上界启发式尝试；因此当前 root/node 的 pricing 长尾不能归因于 RMIH 调用频率。如果尾部大量加列但目标几乎不动，优先考虑 tail 阶段策略：减少启发式 pricing 调用、只保留 exact 闭合、做列有效性诊断，或做 dual stabilization/box stabilization。若目标仍持续改善，则不能简单截断，应优先改善 exact pricing 或分支策略。

## 11. 60 任务根节点 MasterLP 目标变化诊断

2026-06-21 使用 `wet060_001_2m` 做 root-only 诊断，配置为 normal ng-DSSR、nearestK8、BEST_UB、all-cycles completion bound、completion-bound arc fixing、pricingOnly subtree、midpoint probe reuse、ALNS seed、启发式 pricing，关闭 SRI 和 dual stabilization。运行中途停止，根节点尚未闭合；实时日志为 `test-results/bpc/tmp-wet060-root-masterobj-20260621/wet060_001_2m-halfDomain-bestUB-ng-nearestK8-top10.log`。

停止时 `MasterLP` 共记录 182 次，root LP 目标从初始 `36882.000000` 降到 `36744.530612`，总下降约 `137.469388`，列池从 `187` 增至 `48444`。前期启发式 pricing 连续加入大量列，但直到 pool 约 `40293` 前 LP 目标一直保持 `36882.000000`，说明这批列基本没有进入当前有效基结构。随后目标开始分段下降，前几个大幅变化包括 `-8.310138`、`-13.850653`、`-4.817218`、`-4.756965`。

后期表现更接近 tailing-off。停止前多轮 exact pricing 每次约 `6-16s`，仍能找到负 reduced-cost 列，但经常只返回个位数或几十条。例如后段出现 `addedColumns=1/1/9/7/6/1` 等 exact 轮次；对应 LP 目标有时完全不变，有时只下降 `0.002`、`0.004`、`0.018` 这类很小幅度。不过它并非完全无效，后段也仍出现过 `-3.991721`、`-1.226083`、`-0.763672` 等实质下降。这说明当前问题不是 pricing 死锁，也不是所有新增列都无用，而是“很多小负列贡献极弱，偶尔又出现有用列”的长尾。

启发式 pricing 在尾部的性价比较差。停止前后启发式单次仍约 `2.1-2.5s`，多数轮次返回 `0`，少数返回 `1-8` 条列；这些列经常不改变 LP 目标或只带来极小下降。因此后续可以优先尝试 tail 阶段自适应：当连续若干轮启发式无列或 LP 改善低于阈值时，临时跳过启发式，只跑 exact 闭合；当 exact 又出现明显改善后再恢复启发式。这不会影响最终下界正确性，因为闭合仍由真实 dual exact pricing 负责。

这次诊断还支持保留 `MasterLP` 逐轮目标 trace。它能直接区分“pricing 找不到列”“找到了列但 RMP 不动”“找到了列且确实推动下界”三种状态，比只看 generated columns 更能解释长尾。

随后关闭启发式 pricing，只保留 exact ng-DSSR 做同一 root-only 对照，日志为 `test-results/bpc/tmp-wet060-root-masterobj-exactonly-20260621/wet060_001_2m-halfDomain-bestUB-ng-nearestK8-top1.log`。运行中途停止时，exact 已执行 11 轮，列池从 `187` 增至 `564`，`MasterLP` 目标仍保持 `36882.000000`，没有任何下降。单轮 exact 已出现 `92.363s`、`41.428s`、`55.685s` 这类高耗时，且内部 `bestRC` 仍报告很负，例如 `-20276`、`-21917`、`-10698`、`-6538`。这说明前期 RMP 不动不是启发式独有问题；直接关闭启发式也不能更快越过初始退化区，反而因为 exact 单轮太贵而明显更差。

这组结果把后续优化重点缩窄到三点。第一，启发式不能简单关掉，它至少能用较低成本快速扩展列池；但尾部应自适应跳过，因为后期多数启发式调用无列或只带来极小 LP 改善。第二，需要在 pricing 返回列和 RMP 重解之间增加“真实有效性诊断”：记录 returned columns 的真实 reduced cost、进入 LP 后是否为正值、以及本轮 `MasterLP` delta。当前日志只显示 pricing 内部 inferred/best reduced cost，不能解释为什么一些极负候选进入 RMP 后目标不动。第三，若大量列真实 reduced cost 为负但 RMP 仍不动，则问题主要是主问题退化和列组合冗余；若真实 reduced cost 过滤后大量列并不负，则应在入池前过滤，减少无效列污染。

当前建议的下一步不是改启发式搜索本身，而是加两类低风险策略：一是 tail scheduler，当连续若干轮启发式无列或 `MasterLP` 改善低于阈值时临时禁用启发式，只用 exact 闭合；二是 column effectiveness trace/filter，至少统计每轮新增列的 checked reduced cost 和后续正值入基数量。前者减少尾部浪费，后者用于判断是否需要进一步做 checked-reduced-cost 过滤、列池 aging 或 dual/box stabilization。

进一步查阅 column generation tailing-off / degeneracy 相关资料后，需要修正一点判断：当前 60 任务根节点的核心不是启发式 pricing，而是主问题退化导致很多负列进入后几乎不改变 RMP。Frangioni 关于 stabilization 的综述明确指出，标准 CG 的慢收敛主要来自 dual 解序列缺乏局部性，tailing-off 会使 master 变得很大且昂贵；Lübbecke/Savelsbergh 也把 tailing-off 和 simplex 产生的极端 dual 代表联系起来，并指出成功的 stabilization 通常减少 CG 迭代数，但单次 pricing 可能变难。

因此后续优化优先级应改为：

1. 先做 checked reduced-cost 与入基诊断，而不是继续猜启发式。每轮 pricing 返回列后记录真实 cost 复算后的 reduced cost 分布、负列数、加入 LP 后正值列数和 `MasterLP` delta。若大量列真实 reduced cost 很负但不入基，说明是主问题退化和列冗余；若复算后很多列其实不负，说明应在入池前过滤掉这类候选。

2. 重点实现更强的 dual/box stabilization。当前已有的简单 dual smoothing 只是第一层，效果不明显；文献中更典型的是 box / penalty stabilization：维护 dual center，在中心附近不惩罚，偏离 box 后对 dual objective 加分段线性 penalty。等价到 primal RMP 通常是加入带惩罚的 artificial columns/slacks 来限制 dual 摆动。旧 VRP 的 slack penalty 其实更接近这一类，而不是普通 averaging。当前 TWET 后续应优先试一个 root-only 的 box stabilization 原型，闭合前仍回到真实 dual exact pricing，保证正确性。

3. 测试 barrier/interior-point dual 作为低侵入实验。资料中也建议过用 barrier 求 RMP 以得到更 central 的 dual，而不是 simplex 极点 dual。对当前代码可以先只在 root-only 做实验：用 CPLEX barrier 解 RMP 读 dual 做 pricing，再用普通 LP/simplex 路径做最终闭合验证。该方法实现轻，但不保证稳定有效，需要单独对比 pricing rounds 和每轮 `MasterLP` delta。

4. 对列池做 aging / active-set 管理。tailing-off 时 RMP 变大本身会拖慢重解。可以保留正值列、最近入基列、低 reduced-cost 列和 incumbent/RMIH 必需列；长期零值且 reduced cost 高的列只从当前 restricted set 移除，不从全局 pool 删除。这样不解决 dual 震荡，但能降低 RMP 重解成本和后续子节点继承压力。

5. early branching 只能谨慎使用。资料中提到在整数规划中可以在 tailing-off 时停止列生成并分支，但必须保持有效下界。对当前根节点，restricted RMP 的值不是完整 master 的有效下界，不能直接用来剪枝；非根节点若提前分支，也必须用父节点有效下界或其他独立 lower bound 维护队列，不能把未闭合 RMP objective 当作精确节点 bound。因此这不是当前 root 长尾的第一选择。

当前最清晰的下一步是做第 1 点诊断；若确认真实负列大量不入基，就转向第 2 点 box stabilization，而不是继续调整启发式 pricing。

## 14. 50 任务 2 机器试跑记录

2026-06-22 按当前默认加速配置试跑 `data/50-2/wet050_001_2m.dat`，配置为无外包、无 cut、normal ng-DSSR nearestK8、BEST_UB、all-cycles completion bound、completion-bound arc fixing、pricingOnly subtree、midpoint probe reuse、ALNS seed、启发式 pricing，明确关闭 dual stabilization。运行约 `29min38s` 后中止，日志保留在 `test-results/bpc/tmp-wet050-001-m2-ng-accel-nosri-20260622/wet050_001_2m-halfDomain-bestUB-ng-nearestK8-top10.log`。

本次没有求到最优，但已经能看清 50 任务的状态。根节点约 `439.5s` 闭合，root LP 从初始 `44607` 推到 `44353`，RMIH root 不可行；随后 node 2 的 RMIH 找到 `44448`，node 3 找到 `44385`，node 7 进一步找到 incumbent `44383`。中止前已处理到 node 14，最后完整写出的全局状态为 node 13 后 `incumbent=44383`、`bound=44377.545455`、gap 约 `0.0123%`，队列仍有 4 个左右节点。后续 node 14 的日志已开始显示 LP `44403.833333`，高于 incumbent，预计仍需要完成定价证明后才能剪枝。

这次和 60 任务相比明显更轻：root 能在几分钟级闭合，后续节点也能持续抬高全局 bound。但尾部形态和 60 一致，主要不是找不到可行解，而是在小 gap 下反复证明无负列。典型节点包括 node 7：LP 已为整数 `44383` 并可更新 incumbent，但仍花约 `223s` 做定价闭合；node 10 的 LP `44392.5` 已高于 incumbent `44383`，仍花约 `144s` 证明后才被 incumbent 剪掉。很多轮 exact pricing 只返回 1 到 4 条小负列，best reduced cost 已接近 0，但仍需要完整 exact 验证。

从组件耗时看，root 阶段启发式 pricing 很重，`139` 次约 `270s`，exact ng-DSSR `58` 次约 `158s`；进入深层节点后 exact 仍是主要证明成本，但启发式在尾部也有不少无效调用。subtree/pricingOnly arc fixing 在 root 固定了 `1464` 条弧，后续节点多为复用 bounds，扫描通常只有毫秒级，因此当前 50 任务的长尾瓶颈不在 subtree 构造，而在 RMP 退化下的多轮微弱负列与节点闭合证明。

当前结论是：50-2 可以继续作为比 60 更可控的长尾诊断算例。它已经暴露出同样的 column generation tailing-off，但单节点耗时较小，适合后续测试 tail scheduler、true-dual 闭合切换、column effectiveness trace/filter、active-set/column aging 或更强 stabilization。暂时不应把问题归因于 midpoint probe；多数慢节点 probe 选择后的正反 label 并未严重失衡，瓶颈主要是生成列进入主问题后的边际改善很弱。
## 12. 60 任务根节点 dual stabilization 对照

2026-06-21 继续用 `wet060_001_2m` 做 root-only dual stabilization 测试，配置沿用无外包、无 cut、normal ng-DSSR nearestK8、BEST_UB、all-cycles completion bound、completion-bound arc fixing、pricingOnly subtree、midpoint probe reuse、ALNS seed 和启发式 pricing，并显式开启 `dualStabilization=true`、`alpha=0.7`、`centerMoveWeight=0.3`。按用户要求运行到约 1 小时仍未闭合 root，因此中止。实时日志为 `test-results/bpc/tmp-wet060-root-masterobj-stab-a07-20260621/wet060_001_2m-halfDomain-bestUB-ng-nearestK8-top1.log`。

这次对比不能只和 root-only baseline 比，因为之前确实有一次完整 BPC run 已经闭合 root 和 node2，并进入 node3 后被中止。该参照日志为 `test-results/bpc/tmp-wet060-m2-ng-nearest-nosri-full-20260621/wet060_001_2m-halfDomain-bestUB-ng-nearestK8-top10.log`，文件时间约 `1:36:42`。旧 run 中 root 已闭合并分支，root 的最终 bound 约为 `36739.42806267805`，root pricing 统计为 exact ng-DSSR `124` 次、生成 `6370` 列、累计 `2535.755s`，启发式 pricing `278` 次、生成 `42387` 列、累计 `875.123s`。因此旧 root 的 pricing 累计约 `56.85min`，之后进入 node2 和 node3。

当前 stabilization run 在约 `1:01:11` 时仍停留在 root，尚未进入 `rmih.start`、subtree 或 branch。中止前 `MasterLP` 记录 `364` 次，最后目标为 `36739.480669`，列池为 `46993`，总下降 `142.519331`。这个 bound 已非常接近旧 run root 闭合值 `36739.42806267805`，只差约 `0.0526`，但还没有完成 root 闭合。更关键的是，全程 phase 都是 `after_pricing_stabilized`，没有出现 `after_pricing_true` 或真实 dual 闭合校验；说明当前实现只要 stabilized dual 还能找到极少量负列，就会继续加列，不会主动切回真实 dual 做确认。

从效果看，稳定化前中期是有作用的。它比 root-only baseline 更早推动 LP 目标：baseline 约到 pool `40293` 才第一次下降，而 stabilization 约到 pool `29429` 已经开始下降；在 pool `4.3-4.5` 万附近，stabilization 的 LP bound 也明显低于 root-only baseline。例如 stabilization 在 pool `42333` 已到 `36778.953947`，而 baseline 同量级列池仍约 `36815`；后续 stabilization 在 pool `45060` 到 `36758.848912`，继续优于 baseline 中途状态。

但这次也暴露出当前 smoothing 版本的主要问题：尾部会过度追逐 stabilized dual 下的微弱负列。1 小时附近多轮 exact ng-DSSR 单次耗时已经达到 `40-80s`，但常常只返回 `1-18` 条列；启发式也多次只返回 `1-6` 条列或无列。最后几轮仍在 stabilized phase 下出现 `addedColumns=1/7/14/2/2/18/1/1/9` 这类小批量返回，master 目标偶尔下降、但大部分轮次改善极小。也就是说，当前稳定化没有解决 root 尾部闭合问题，反而因为没有及时真实 dual 校验，可能把前期收益消耗掉。

当前结论为：dual smoothing 方向有信号，但第一版参数和调度策略不能直接作为默认。它能让 root 前中期更快下降，但必须增加 tail 阶段控制：当 stabilized exact 连续若干轮只产生少量列、`MasterLP` 改善低于阈值、或 stabilized bestRC 接近 0 时，应主动切回真实 dual exact pricing 做闭合确认；如果真实 dual 仍有明显负列，再回到正常循环，否则结束当前节点。后续若继续做 stabilization，优先改这个“stabilized pass 不停榨微弱列”的调度逻辑，而不是只调 `alpha`。

## 13. 60 任务长尾的后续可选处理

当前长尾的主要形态已经比较明确：不是单次 pricing 死循环，也不是启发式本身导致，而是 RMP 高度退化后，很多负 reduced-cost 列进入后几乎不改变主问题，同时偶尔又出现真正有效的列。因此后续方法要围绕“减少无效列循环、稳定 dual、控制 RMP 活跃列规模、改善分支结构”展开。

第一优先级仍是给 smoothing 增加尾部切换规则。当前第一版 dual stabilization 的问题不是前期无效，而是一直停留在 stabilized dual 下追微弱负列。更合理的流程是：stabilized pricing 只作为找列阶段；一旦连续若干轮新增列少、`MasterLP` 改善低于阈值、或者 exact best reduced cost 已接近 0，就立即切到 true dual exact pricing。true dual 下若无负列，节点闭合；若有明显负列，则加入这些列并刷新 center。这个改动最小，也最符合当前日志暴露的问题。

第二优先级是做 column effectiveness 诊断和过滤。每轮 pricing 后记录新增列的 checked reduced cost、加入 LP 后是否正值、是否进入当前基结构、以及本轮 `MasterLP` delta。如果大量列真实 reduced cost 为负但长期不入基，说明主要是退化冗余；应考虑只保留每轮最有效的一批列，或按 signature/长度/覆盖模式做多样性限制。如果很多 inferred 负列复算后贡献很弱，则应提高入池阈值或在候选层过滤。

第三优先级是 restricted master active-set / column aging。全局 pool 不删，但当前节点 restricted set 可以只保留正值列、最近正值列、低 reduced-cost 列、RMIH/incumbent 相关列和一部分多样性列；长期零值且高 reduced-cost 的列从当前 LP 移出。这个不解决 dual 震荡本身，但能减少 LP 重解成本，也能降低后续子节点继承的列规模。

第四优先级是更强的 box / penalty stabilization。简单 averaging 已经证明有前期信号，但闭合调度不够。若 true-dual 切换仍不够，应实现带 dual center 的 box stabilization 或等价 primal artificial/slack penalty 版本：dual 留在 center 附近不惩罚，偏离后逐段惩罚，并自适应缩放 box。它比简单平滑更接近文献里的稳定化，也比单纯调 `alpha` 更有希望缓解退化尾部。

第五优先级是 barrier / central dual 试验。RMP 用 simplex 给出的极点 dual 容易震荡；可以只在 root 试验用 CPLEX barrier 取更 central 的 dual 做 pricing，然后仍用真实 dual exact pricing 做最终闭合证明。该方法实现相对轻，但效果不确定，适合做短对照。

第六优先级是分支结构调整。当前 60 任务在 root 后能进入 node2/node3，但 node3 仍长尾，说明分支确实改变了问题但没有完全消除退化。后续可以记录每类 arc branch 的 child bound improvement 和 pricing 难度，形成 pseudo-cost；避免选择导致后续 pricing 极难但下界提升很小的弧。这个方向比新增复杂 cut 更稳，但需要积累统计。

暂不优先做的方向包括：直接关闭启发式 pricing、默认开启 SRI、用 time-indexed graph pricing 替代当前函数定价、以及未闭合就 early branching。前者已经在 exact-only root 对照中表现更差；SRI 和 time-indexed graph pricing在前面实验里主要增加状态或列数；early branching 如果没有独立有效下界，不能把未闭合 RMP objective 当成可剪枝下界。
## 15. incumbent 已优于当前 LP 时仍继续定价的现象

2026-06-22 复核 50-2 日志后确认，部分节点确实出现“当前 restricted LP 已经高于 incumbent，但仍要继续 pricing”的长尾。典型例子是 node 10：该节点在多轮加列后 LP 已长期停在 `44394.666667`、`44394.333333`、`44392.500000` 等值，均高于当时 incumbent `44383`，但 exact pricing 仍能返回负 reduced-cost 列，因此不能立即用 incumbent 剪枝。node 14 也类似，LP 从 `44470.551161` 逐步降到 `44403.833333`，仍明显高于 incumbent，却还没有完成无负列证明。

这里的关键点是，restricted LP 的值只是在当前列集上的下界候选，不是完整主问题在该节点的有效下界。只要 pricing 还能找到负列，完整 LP bound 仍可能继续下降；虽然这次日志中大量新列只让 LP 目标微小下降甚至不变，但算法不能仅凭“当前 restricted LP 已高于 incumbent”直接剪枝，否则存在漏掉后续负列把节点 bound 拉回 incumbent 以下的风险。

当前可以考虑的不是直接剪枝，而是加诊断和调度：记录 `LP - incumbent` 的安全余量、每轮新增列数、加列后的 `MasterLP` 下降幅度、best reduced cost 与连续无效轮数。若发现某个节点已高于 incumbent 很多，但连续多轮只返回接近 0 的负列且 LP 几乎不动，可以优先切换到更严格的 true-dual exact closure、减少启发式调用、或限制返回列数量；但最终仍需要一次真实 dual exact pricing 证明无负列，才能安全执行 incumbent 剪枝。
